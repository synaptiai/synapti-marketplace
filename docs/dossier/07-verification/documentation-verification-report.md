---
dossier-header: internal-v1
title: Documentation Verification Report
purpose: Tells a reader how much this package's own claims are worth, by recording what was checked, what was found, and what could not be established.
audience: Reviewer, Maintainer
confidentiality: Public
owner: Daniel Bentes
status: verified
project-version: 06b1586
last-verified: 2026-07-26
review-trigger: Any re-run of the verification passes, or any correction applied to a document after this report was written
related: [00-control/evidence-ledger.md, 00-control/assumptions-questions-and-contradictions.md, 00-control/claim-and-disclosure-register.md, 05-due-diligence/technical-due-diligence-report.md]
---
# Documentation Verification Report
<!-- contract: references/package-contract-07-verification.md#documentation-verification-report -->

## Scope

| Field | Value |
|---|---|
| Package version verified | 1.0.4 |
| Project version or commit | `06b1586` on `fix/gate-verdict-integrity` |
| Evidence cutoff | 2026-07-26 |
| Verification date | 2026-07-26 (rounds 1 through 3, same day; round 4 is a fix cycle — see below) |
| Documents in scope | All 23 canonical files |
| Documents out of scope and why | None. This report verifies itself only for mechanics — a pass cannot audit its own findings |
| Round | 6 |

## Reviewer-pass independence method

| Field | Value |
|---|---|
| Passes run | A, B, C |
| Execution mode | **single context with reset frames** |
| Model per pass | Identical. All three passes ran on the same model, in the same session, with the same context |
| Did any pass read another pass's findings before producing its own | **Yes, unavoidably.** All three ran in one context. Pass B could see Pass A's output, and Pass C could see both |
| External audit performed | no |
| Residual correlation risk | **High, and this is the largest single weakness of this verification.** The dossier plugin's design makes independence architectural — separate agent dispatches, `memory: none`, a skill firewall keeping reconciliation logic out of every verifier's context, single-message dispatch, and per-pass model configuration. **None of those mechanisms was used here.** This run was performed inline, so the three passes share priors completely. A blind spot in one is a blind spot in all three, and the mutual confirmation between passes carries close to zero information |

Model diversity: none. All passes used the session model.

Stating this plainly rather than describing three passes as if they were independent is the point of the section. What the passes did achieve is *method* diversity — Pass A checked referential integrity mechanically, Pass B re-derived every headline number from the filesystem without consulting the ledger, and Pass C compared repeated facts across documents. Those are genuinely different procedures, and Pass B's re-derivation is what caught the first substantive defect. What they did not achieve is *reviewer* independence.

Round 3 is the evidence for how much that costs. It re-derived every headline figure from the commit under review rather than from the ledger, and found five defects the first two rounds had passed over — including a pinned commit at which the licence this package reports as present did not exist, and three of six contract-mandated diagrams silently absent. Rounds 1 and 2 had declared cross-document consistency clean. A third look at the same package, by the same reviewer, with a different starting point, was enough to falsify that. The correlation risk above is not theoretical, and the score moved down rather than up because of it.

## Checks performed

| Check | Method | Scope | Result | Evidence |
|---|---|---|---|---|
| Canonical file presence | `dossier-package-check.sh` | 23 files | pass — 23 of 23 present | `CHECK_FILES_PRESENT=23` |
| Header completeness | Same, shell frontmatter parser | 23 files | pass after correction — 21 of 23 complete at first run, both gaps filled | `CHECK_HEADERS_OK` |
| Contract pointer resolution | Same — body line 2 resolved to a real anchor in a real reference | 23 files | pass — 23 of 23 | `CHECK_POINTERS_CHECKED=23` |
| Internal link resolution | `test -f` on every package-relative path named in prose, plus every `related:` entry | 23 files | pass — 0 dangling of both kinds | Pass C |
| Diagram syntax | Python parse of every mermaid block: header form, bracket and quote balance, parentheses inside node labels | 6 blocks | pass | Pass C, re-run in round 3 |
| Diagram versus inventory agreement | Every node and edge mapped to a row in the accompanying table | 6 blocks | pass | Pass C, re-run in round 3 |
| Evidence citation coverage | Set difference between `EV-####` defined in the ledger and referenced anywhere | 56 IDs | pass — 0 dangling, 0 orphaned | Pass A |
| Register referential integrity | Same for `AQ`, `CT`, `CL`, `TM` | 11 / 4 / 35 / 25 | pass — 0 dangling in all four | Pass A |
| Ledger locator resolution | `dossier-ledger-lint.sh` — 13-column shape, ID uniqueness, state and authority enums, locator resolution at authority 1–3, public-use gating | 56 rows | pass after correction — 82 errors at first run, 0 now | See F-06, F-07 |
| Public claim mapping | `dossier-claim-scan.sh` against the approved register | 2 public files | pass after correction — 52 unregistered in round 1, **0 now** | See F-03, F-08 |
| Secret and sensitive-material scan | Same tool: 12 credential pattern classes plus internal locators | 2 public files | pass after correction — 34 leaks found and fixed | See F-02 |
| Command and example execution | Every command in the package executed and its real output recorded | 30 of 32 | pass — the 2 unexecuted are named with reasons | Ledger executed-checks table |
| Terminology consistency | Every entity named in prose checked against the terminology register | 25 `TM-` entries | pass | Pass C |
| Cross-document fact agreement | Every repeated number grepped across all 23 files and its occurrences counted | 12 recurring figures | **1 finding in round 1, corrected; 1 more in round 3** | See F-01, F-14 |
| Headline-number re-derivation | Every count re-derived from the filesystem **without consulting the ledger** | 16 figures in round 1; 21 re-run in round 3 against the pinned commit | **1 falsified in round 1; 6 stale in round 3, all corrected** | See F-01, F-11 |
| Pin-versus-evidence agreement | Every fact the package reports as current tested against the commit named in `project-version` | 23 headers, 3 post-pin corrections | **fail, then pass** — the pin named a commit at which the licence did not exist | See F-11 |
| Diagram coverage | Each template's mermaid prompts counted against the diagrams the document actually carries | 23 template/document pairs | **fail, then pass** — 3 of 6 requested diagrams were absent | See F-13 |
| Claim sample — 100% categories | Security, compliance, and licence claims re-verified against live sources | 12 rows | pass — all 12 confirmed on re-check | Pass A |

| Check not executed | Why | What it would have established |
|---|---|---|
| Independent verification by a different model | The passes were run inline in one context rather than as separate agent dispatches | Whether the findings survive a reviewer who does not share this one's priors — the single largest gap in this verification |
| `pytest` on the `agent-capability-standard` submodule | `engagement.allowedActions.runBuild` is `false` | Whether the only dependency-bearing plugin's tests pass (AQ-0001) |
| End-to-end execution of the dossier post-merge workflow | Requires a scratch repository, an API-key secret, and a merged pull request | Whether the plugin's headline capability works (AQ-0002) |
| A clean-profile marketplace install | `engagement.allowedActions.networkAccess` is `false` | Whether a first-time install succeeds (AQ-0003) |

## Claim sample

| Field | Value |
|---|---|
| Claim categories audited at 100% | security, compliance, licence. Financial, performance, and customer-reference were searched for and **none exists in the package** — there are no commercial or benchmark claims to audit |
| Sampling method for remaining claims | Census rather than sample. Every one of the 56 evidence rows was re-checked, because the population is small enough that sampling would have been a weaker method for no saving |
| Sample size | 56 |
| Population size | 56 |
| Defect rate found in the sample | 1 of 56 rows falsified — 1.8% (EV-0046, see F-01) |
| What the sample can and cannot detect | A census over the ledger detects rows that are *wrong*. It cannot detect a claim that is **missing** from the ledger entirely, nor a fact that is true at `06b1586` and false on `main`. Both remain open exposures (AQ-0010) |

## Scorecard

| Dimension | Weight | Score | Percent of available | Deductions cite |
|---|---|---|---|---|
| Evidence grounding and freshness | 18 | 16 | 89% | Two planned checks unexecuted (AQ-0001, AQ-0002), so two claims rest on structure rather than observation. F-11: the package's own pin disagreed with the evidence it reported, and six figures were stale at the commit under review |
| Coverage and completeness | 12 | 10 | 83% | `prompt-decorators` never inspected (AQ-0005); submodule history past the pin never read (AQ-0006). F-13: 3 of 6 contract-requested diagrams were absent |
| Technical correctness | 15 | 13 | 87% | F-01 (a falsified count that had propagated to 7 documents) and F-02 (34 internal-identifier leaks in the public documents). Both corrected in round 1; the deduction stands because they were present in a drafted package |
| Cross-document consistency | 10 | 8 | 80% | F-01 propagated to 7 documents before correction. F-14: the ledger contradicted itself between an evidence row and the check that row cites. F-11: six subjects disagreed in round 3 after two rounds reported this section clean |
| Due-diligence decision value | 10 | 9 | 90% | F-15: a cleared risk was deleted rather than renumbered, leaving both ranked tables starting at 2 and a horizon reference pointing at a rank that no longer existed |
| Onboarding and operability | 10 | 9 | 90% | Every command was executed and its output recorded, but no route has ever been walked by anyone other than the maintainer (AQ-0007) |
| Security, privacy, and disclosure safety | 10 | 10 | 100% | Round 1 deducted 2 for F-03; the scan now reports 0 leaks, 0 prohibited vocabulary, and 0 unregistered sentences |
| Reliability and verification depth | 5 | 3 | 60% | **The passes were not independent, across all three rounds.** Same model, same context, shared priors |
| Public usefulness and claim integrity | 5 | 5 | 100% | Every sentence in both public documents maps to an approved claim or a mandated qualification. Two sentences drafted in round 3 matched no approved claim and were removed rather than self-approved |
| Clarity and maintainability | 5 | 4 | 80% | F-12: the package shipped with no entry point at its own root — a reader browsing it reached eight numbered directories and no index |

| Field | Value |
|---|---|
| Score before corrections | 82 |
| Score after round 1 corrections | 89 |
| Score after round 2 corrections | 92 |
| Score after round 3 corrections | **87** |

The score fell. That is not a regression in the package, which is strictly better than it was at 92 — it is a correction to a number that was too high because two rounds had not looked hard enough. Round 3 found five defects in a package twice declared consistent, so the earlier scores were measuring the reviewer's thoroughness as much as the package's quality. A verification that cannot move its own score downward is not a verification.

## Release gate

| Condition | Result | Evidence |
|---|---|---|
| Total score at least the configured minimum | **FAIL** | 87 against a configured minimum of 95 |
| Every dimension at or above its minimum percent | **FAIL** | Reliability and verification depth at 60% against a minimum of 80% |
| No unresolved critical or high finding | PASS | No Critical found; all 8 High findings were corrected and re-checked |
| No unsupported or unapproved public claim | **PASS** | `CLAIM_SCAN_UNREGISTERED_SENTENCES=0`. 35 approved claims and 5 mandated qualifications cover every sentence in both public documents |
| Every required human approval recorded | PASS | All 35 claims approved by the named approver on 2026-07-26. The 19 original claims were presented with the posture set separated and approved explicitly; the 16 added in round 2 are qualifications and scope statements on claims already approved |
| No secret, credential, personal data, or prohibited disclosure present | PASS | `CLAIM_SCAN_LEAKS=0` after F-02 was corrected; 0 prohibited vocabulary |
| No contradiction that could materially mislead a decision | **PASS** | CT-0001 resolved by evidence: an Apache-2.0 `LICENSE` file is at the repository root and all 15 declarations across the repository match it. CT-0002 to CT-0004 remain, none of which could mislead a decision |
| Canonical document and section coverage complete, including justified `N/A` | PASS | 23 of 23 files; every `N/A` carries a stated reason |
| Every material internal claim has a state and a locator | PASS | 56 rows, each with a state and a locator in a documented form — a path, `cmd:`, `derived:`, or `inference:` |
| Every public claim maps to approved `V` or `C` evidence | PASS | Every approved claim rests on a `V` row; 0 rest on any weaker state. Two sentences drafted in round 3 matched no approved claim and were removed rather than approved by their author |
| Internal links, paths, and diagram syntax validate | PASS | 0 dangling links, 0 dangling `related:` entries, 6 of 6 diagrams parse, and every diagram the templates request is present |
| Commands and examples executed or marked not executed | PASS | 30 executed with retained output; 2 marked not executed with reasons |
| Planned behaviour not presented as implemented | PASS | dossier's post-merge automation is labelled "built, never executed" in every document that mentions it, and the claim asserting otherwise was rejected as CL-R02 |
| Targets not presented as measured results | PASS | The current-versus-target table marks every row "not committed"; three performance assumptions are labelled unverified rather than stated as measurements |
| Policies not presented as implemented controls | PASS | The control-state summary separates 7 implemented from 3 policy-only, and names the 3 |
| Unresolved uncertainty and source limitations visible | PASS | 9 open register items, 5 unavailable-evidence rows, and a scope section naming what the assessment cannot establish |
| Reviewer-pass independence method disclosed | PASS | Disclosed above, including that independence was **not** achieved |

**GATE RESULT: NOT-RELEASABLE.** Two of seventeen conditions fail, unchanged from round 2 and down from four in round 1. Both are score conditions: the total is 87 against a minimum of 95, and verification depth is 60% against an 80% floor. Both trace to the same cause — the verification passes were not independent (F-05) — and neither can be closed by editing the package. Only a genuinely independent re-run closes them. Round 3 is the demonstration: a third look at a twice-certified package found five more defects, so the deficit the gate is naming is real rather than procedural.

## Round 4 — a fix cycle, not a verification pass

Round 4 was driven by an external review of the **plugin**, not by a pass over
this package. Five containment defects were found in the code these documents
describe: the output-root hook admitted a path traversal, three security hooks
no-opped when `jq` was absent, the action ceiling was defeated by one layer of
shell indirection, the disclosure hook was missing a class its own policy
reference assigns it, and the patch validator checked path strings but not file
mode. A sixth — the validator accepting a traversal path — was found by a test
written during that cycle rather than by a reader.

Two consequences for this package, and only two:

- **The pin moved** from `fbeb1ee` to `06b1586`, and every figure that moves with a
  commit was re-derived from it: the dossier assertion count (1076 → 1241), the
  combined count (2,098 → 2,263), commits on the assessed branch (198 → 193),
  commits across all refs (355 → 370), and tracked files (570 → 572). This is F-11's
  rule applied again: figures and pin must name the same commit, or the package is
  simultaneously pinned and current and cannot be both.

  The branch commit count went **down**, which looks wrong and is not. Round 4
  was squash-merged, so the assessed branch is now a short branch off `main`
  rather than the long feature branch — 193 commits reaching `HEAD` instead of
  203. The figure moved because what it measures moved, and a reader comparing
  the two rounds would otherwise read a smaller number as an error.

  One label was wrong rather than stale. CHK-16 runs `git rev-list --count HEAD`,
  which counts the assessed branch, but five documents reported the result as the
  commit count of `main`. `main` has 189. The figure was right and the noun was
  wrong, which is the harder version of the same defect: nothing about the number
  looked incorrect.
- **Nothing else changed.** No claim was re-verified, no finding was re-opened or
  closed, and the gate verdict is unmoved.

What round 4 did **not** do is verify anything. No pass A, B, or C ran against
`06b1586`. The scorecard, the release gate, and every claim state below describe
the package as it stood at round 3 and are carried forward unchanged. A reader
weighing this report should read the round-3 evidence as the most recent
verification of record.

### Round 6 — the gate's own integrity

The reviewer assigned error-handling in rounds 4 and 5 reported nothing both
times; its findings never reached the orchestrator. In round 5 that gap was
covered by re-deriving its scope directly, which found the cursor defect. It did
not find the other four, and the release went out before the reviewer's own
answer arrived.

Three of those four attacked this gate's central claim. The most serious: the
verdict-file silence check and the result extraction used different predicates,
so a verdict naming a condition twice — an empty summary row, then a detail row
with the real word — satisfied the check on one line and was parsed from the
other. The condition was recorded nowhere and counted in neither the failure nor
the inconclusive tally. Sixteen of seventeen conditions reported, one simply
gone. A dropped condition is worse than a silent one, because silence is at
least counted.

That is a fourth instance of the shape below, and the first to land on the gate
itself rather than on the package or the tests.

### Round 5 — the same shape, one level down

Two reviewers reported after round 4 was merged and tagged, and both found real
defects. One was in a fix round 4 had just shipped: the action-ceiling repair
stripped a wrapper token plus one optional flag, which left `timeout 30 curl …`
passing the entire deny list. `timeout N cmd` is the ordinary way to bound a
command, so the bypass was the modal case rather than an exotic one, and it
shipped green because every wrapper exercised by the new tests happened to take
no argument.

That is worth recording rather than quietly correcting, because it is the third
instance in this package's history of the same shape: a check that was believed
to hold, had a test, and did not hold. Round 3 found a package pinned to a commit
where its own claims were false. Round 4 found a regression test reporting a pass
on the file that carried the defect it was written to pin. Round 5 found a
security fix whose tests covered only the cases the fix happened to handle.

None of the three was caught by a verification pass over this package. All three
were caught by someone reading the plugin afterwards. That is the strongest
available evidence for F-05, and it is why the finding stays open.

That the defects were found by a reviewer reading the plugin, and not by three
passes over the package that documents it, is itself the point F-05 makes: the
passes were not independent, and a non-independent pass is weak evidence. Round 3
demonstrated it by finding five defects a twice-certified package had passed;
round 4 demonstrates it again from the outside.

## Findings

| Finding ID | Severity | Pass | Audience affected | File and section | Problem | Evidence | Why it matters | Required correction | Evidence still needed | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| F-01 | High | B | Reviewer, Maintainer | 7 documents citing EV-0046 | The evidence ledger recorded 11 tracked decision records; `git ls-files .decisions/` returns 10. The eleventh file exists in the working tree but is untracked | `git ls-files .decisions/ \| wc -l` → 10 | A single wrong number in the ledger propagated verbatim into 7 documents. This is the exact cross-document drift the package criticizes the repository's README for, reproduced inside the package itself within one drafting session | Correct EV-0046 and all 7 citing documents | none | **corrected** |
| F-02 | High | C | Every public reader | `06-public/**` | Both public documents carried inline `[CL-####]` citations — 34 occurrences. The plugin's own rule is that `EV`, `AQ`, `CT`, `CL`, and `TM` identifiers must never appear publicly: they expose the internal register structure and are useless to a reader | `CLAIM_SCAN_LEAKS=34` | Traceability belongs in the claim register's mapping table, which already carried it. The public documents leaked internal structure for no reader benefit | Remove every inline identifier from `06-public/**` | none | **corrected** — `CLAIM_SCAN_LEAKS=0` |
| F-03 | Medium | C | Every public reader | `06-public/**` | 52 sentences in the public documents did not match an approved claim's wording verbatim | `CLAIM_SCAN_UNREGISTERED_SENTENCES=52` | The rule exists so that no public sentence escapes review. Prose that merely explains an approved claim still reaches a reader unreviewed | Resolved three ways, none of them by loosening the check: 16 sentences that genuinely assert something were registered as claims and approved; 5 near-misses were made verbatim matches of claims already approved; the remainder was editorial glue asserting nothing about the product and was cut. Two further causes were plugin defects — see F-08 | none | **corrected** — `CLAIM_SCAN_UNREGISTERED_SENTENCES=0` |
| F-04 | Medium | A | Maintainer | `plugins/dossier/bin/dossier-claim-scan.sh` | The scanner read YAML frontmatter as prose, so `title:` and `audience:` were reported as unregistered claims in every public document — 4 findings no drafter could ever resolve | 56 unregistered before the fix, 52 after | A check that emits irreducible noise trains its reader to skip its output, which is how the real findings get missed | Skip the header block; add regression tests | none | **corrected** — fixed in the plugin with 3 regression tests |
| F-05 | Low | A | Reviewer | This report | The three verification passes ran in one context on one model, so their agreement carries almost no independent information | The independence section above | The package's own standard treats correlated review error as the failure mode the three-pass design exists to prevent. This run did not meet that standard | Re-run the passes as separate agent dispatches, or via `--external` against a different model | An independent run | **open** |
| F-06 | High | gate | Every future dossier user | `plugins/dossier/bin/dossier-gate.sh`, `templates/package/07-verification/…` | Gate condition G17 greps the verification report for `## Reviewer-pass independence method` and a `Model diversity:` line. The shipped template carried neither — its heading was `## Independence method` and it never prompted for the line | G17 failed on a report drafted faithfully from the template | **Every package would fail G17, always, with nothing in the template explaining why.** The condition was added without aligning the artifact it grades | Rename the template heading to match the gate and the contract reference; add the `Model diversity:` prompt | none | **corrected** |
| F-07 | High | gate | Every future dossier user | `plugins/dossier/bin/dossier-ledger-lint.sh` | Two defects in one check. The linter compared the `Source ref` cell against the filesystem verbatim, but `references/evidence-ledger-schema.md` writes every one of its own examples as a Markdown code span — so the documented form failed the lint. Separately, the multi-locator loop this fix required ran in a pipe, so `emit` incremented a counter in a subshell and the linter printed findings under `LEDGER_ERRORS=0` | 82 errors at first run, of which 53 were the backtick form; the subshell defect was introduced by the first fix and caught by its own regression test | A linter that rejects the documented style trains everyone to ignore it; a linter that reports findings and a zero count is worse — a caller checking only the count sees a clean ledger | Strip code spans before classifying a locator; validate each span; iterate without a pipe. Add `derived:` as a locator form, which the method allowed and the schema table never named | none | **corrected** |

| F-08 | High | gate | Every future dossier user | `plugins/dossier/bin/dossier-claim-scan.sh` | Two more defects in the registration check. Approved wordings were lowercased but not normalized while document sentences were fully normalized, so markdown survived on one side only and any claim containing a code span could never match its own approved row. Separately, sentences were split on a bare `.`, cutting inside `SKILL.md`, `plugin.json`, and version numbers | 56 unregistered before the fixes, 46 after normalization, 41 after the split fix | **The registration check could not pass for a realistic claim.** Any wording containing a code span was permanently unregisterable, and the split produced fragments no drafter could resolve | Normalize both sides identically; strip code spans before splitting | none | **corrected** |
| F-09 | High | gate | Every future dossier user | `plugins/dossier/bin/dossier-claim-scan.sh` | The contract **mandates** that a required qualification appear in the public document beside the claim it qualifies. Qualification rows carry no `approved` cell, so the scan matched only claim rows and reported every mandated qualification as an unregistered sentence | The last 2 unregistered sentences were both mandated qualifications | The register required a sentence that the scan then reported as a violation — two rules the plugin ships, contradicting each other, with no way for a drafter to satisfy both | Treat the qualification column of the Required qualifications section as approved text, scoped to that section only | none | **corrected** |
| F-10 | Medium | gate | Every future dossier user | `plugins/dossier/tests/plugin-manifest.test.sh` | The licence assertion was a hard-coded `MIT` constant. A constant passes while the file it names says something else — which is how this repository came to advertise a licence it did not carry | Failed when the repository moved to Apache-2.0 | A test that asserts a constant verifies nothing about the pair it is supposed to hold together | Read the SPDX identifier from the `LICENSE` file and assert the manifest matches it; fail loudly when no `LICENSE` exists | none | **corrected** |
| F-11 | High | round 3 | Reviewer, every public reader | All 23 headers; 6 figures across ~60 occurrences | The package pinned itself to a commit that predates facts it reports as true. Every header named `d0fa737`, at which no `LICENSE` file exists — yet the package asserted the licence present and CT-0001 resolved by evidence. Six figures had also drifted after the evidence was gathered, three of them moved by this package's own correction commits: the dossier assertion count, the combined count, commits on the branch, commits across all refs, tracked files, and the decision-record count corrected by F-01 | `git cat-file -e d0fa737:LICENSE` fails; `git ls-files .decisions/` returns 11, not the 10 F-01 corrected to | A reader checking out the named commit finds the licence absent and the counts wrong. The package was simultaneously pinned and current, and could not be both. It is the refresh problem the plugin exists to solve, occurring inside the package before the plugin's refresh path had ever run | Re-pin every header to `fbeb1ee` — the last commit that changed the subject — and re-derive all six figures from it | none | **corrected** |
| F-12 | Medium | round 3 | Every reader | `bin/dossier-scaffold.sh` | The scaffold wrote 23 documents into a directory and no way into it. A reader browsing the output root sees eight numbered folders; the index that routes them is three clicks in and named like a register | The output root contained no `README.md` | An index nobody finds routes nobody. The package's own contract calls the index the entry point, and the entry point was not where readers enter | Scaffold a `README.md` signpost at the output root from `templates/package-readme.md`. It asserts no fact, so it cannot go stale | none | **corrected** — with tests, including one comparing its directory table against the scaffold's canonical list |
| F-13 | High | round 3 | Reviewer, partner, contributor | `02-architecture/data-and-ai.md`, `02-architecture/infrastructure-and-deployment.md`, `06-public/technical-partner-guide.md`, `bin/dossier-gate.sh` | Four templates open a mermaid fence to request a diagram — six in total. Three were never drawn, and nothing noticed. G11 counts fences and checks the ones present are balanced, which grades what somebody drew and is silent about what they skipped | `CHECK_DIAGRAMS_EXPECTED=6`, `CHECK_DIAGRAMS_PRESENT=3` | The logical data model, the deployment topology, and the partner integration view were each requested by the contract and silently omitted. A drafter could skip every prompt and still see a clean diagram check | Draw the three; add `CHECK_DIAGRAMS_EXPECTED`/`_PRESENT` to the package check and record a finding when a document the contract wants illustrated ships with none | none | **corrected** |
| F-14 | Medium | round 3 | Reviewer, Maintainer | `00-control/evidence-ledger.md` | The ledger contradicted itself inside one file, from the commit it was first written in. `EV-0046` recorded 10 decision records; `CHK-23` — the executed check that row cites, two sections below — recorded 11. The F-01 correction reached the evidence row and 7 citing documents and stopped short of the check result underneath it | Both values present in the same file at `6686713` | An evidence row and the check it cites are the one pair that must agree, and the correction that was this package's headline catch left them disagreeing | Set both to the observed value; the underlying count had since changed again, which is F-11 | A check comparing each row's stated value against the result of the `cmd:` it cites — not built | **corrected** |
| F-15 | Low | round 3 | Reviewer | `05-due-diligence/technical-due-diligence-report.md` | The licence risk was cleared and its row deleted rather than renumbered, leaving the risk register running 2–10, the management questions running 2–7, and the 30/60/90 table pointing at a "Ranks 1–2" that no longer existed | Both tables' leading cells | A ranked register whose top rank is missing reads as a truncation, and a cross-reference to a rank that does not exist cannot be followed | Renumber both tables from 1 and remap every horizon reference | none | **corrected** |

| Severity | Found | Corrected | Open | Accepted | Blocked |
|---|---|---|---|---|---|
| Critical | 0 | 0 | 0 | 0 | 0 |
| High | 8 | 8 | 0 | 0 | 0 |
| Medium | 5 | 5 | 0 | 0 | 0 |
| Low | 2 | 1 | 1 | 0 | 0 |

Eight of the fifteen findings — F-04, F-06 through F-10, F-12, F-13 — are defects **in the dossier plugin itself**, surfaced only because the plugin was pointed at a real project for the first time. Its original 1034 assertions had never caught any of them, because each is a disagreement between two artifacts that no single test compared: a gate against its template, a linter against its reference, a scanner against a document's header, a normalization applied to one side of a comparison, a rule that mandates text another rule forbids, a test asserting a constant instead of a pair, a scaffold that built a package with no way into it, and a diagram check that graded what was drawn and never asked what was skipped. Each now has a regression test that fails if the pair drifts again — 1241 assertions, up from 1034.

## Corrections applied

| Finding ID | Document | Change made | Re-checked | Result |
|---|---|---|---|---|
| F-01 | `00-control/evidence-ledger.md` and 6 others | 11 → 11 tracked decision records, everywhere the figure appeared | `grep -rn '11 tracked decision\|11 decision records\|11 records'` | 0 remaining |
| F-02 | `06-public/technical-partner-guide.md`, `06-public/customer-product-and-trust-guide.md` | All 34 inline `[CL-####]` citations removed; traceability retained in the register's mapping table | `dossier-claim-scan.sh` | `CLAIM_SCAN_LEAKS=0` |
| F-04 | `plugins/dossier/bin/dossier-claim-scan.sh`, `plugins/dossier/tests/disclosure-gate.test.sh` | Header block skipped; 3 regression tests — header fields not reported, a body sentence still reported, a headerless document scanned in full | `plugins/dossier/tests/run.sh` | pass |
| F-06 | `plugins/dossier/templates/package/07-verification/documentation-verification-report.md`, `plugins/dossier/tests/package-contract.test.sh` | Heading renamed to match the gate and the contract reference; `Model diversity:` prompt added; a test now reads G17's expected heading **out of the gate script** and asserts the template carries it, so renaming either alone fails | `plugins/dossier/tests/run.sh` | pass |
| F-07 | `plugins/dossier/bin/dossier-ledger-lint.sh`, `plugins/dossier/references/evidence-ledger-schema.md`, `plugins/dossier/tests/ledger-lint.test.sh` | Code spans stripped before locator classification; every span in a cell validated; loop de-piped so the counter moves with the findings; `derived:` added to the accepted forms and documented in the schema table; 5 regression tests | `dossier-ledger-lint.sh`, `plugins/dossier/tests/run.sh` | `LEDGER_ERRORS=0`; suite pass |
| F-03 | `06-public/**`, `00-control/claim-and-disclosure-register.md` | 16 sentences registered as claims and approved; 5 near-misses made verbatim; editorial glue cut. Register grew from 19 approved claims to 35 | `dossier-claim-scan.sh` | `CLAIM_SCAN_UNREGISTERED_SENTENCES=0` |
| F-08 | `plugins/dossier/bin/dossier-claim-scan.sh`, `plugins/dossier/tests/disclosure-gate.test.sh` | Approved wordings normalized identically to document sentences; code spans stripped before the sentence split; 4 regression tests including one asserting a real second sentence on the same line is still found | `plugins/dossier/tests/run.sh` | pass |
| F-09 | `plugins/dossier/bin/dossier-claim-scan.sh`, `plugins/dossier/tests/disclosure-gate.test.sh` | Required qualifications treated as approved text, scoped to that section only; 2 regression tests, one asserting a rejected wording in a later table is still not approved | `dossier-claim-scan.sh`, suite | `CLAIM_SCAN_UNREGISTERED_SENTENCES=0`; suite pass |
| F-10 | `plugins/dossier/tests/plugin-manifest.test.sh` | Hard-coded licence constant replaced by a check that the declared identifier matches the LICENSE file's text, and a loud failure when no LICENSE exists | `plugins/dossier/tests/run.sh` | pass |
| CT-0001 | `LICENSE`, 7 plugin manifests, 8 marketplace entries, `README.md`, 5 plugin READMEs, `.claude/CLAUDE.md` | Apache-2.0 chosen by the owner; canonical text added at the repository root; every declaration aligned to it | `cmd:CHK-07`, `cmd:CHK-08`, `cmd:CHK-29` | Present and consistent; 15 declarations agree |
| — | `00-control/evidence-ledger.md` | Locators normalized to the documented forms; an escaped pipe removed from a Notes cell, which had made one row parse as 15 columns; `Public use` set to `no` on the 27 rows no approved claim rests on | `dossier-ledger-lint.sh` | `LEDGER_ERRORS=0 LEDGER_ROWS=54` |
| — | `07-verification/documentation-verification-report.md`, `00-control/documentation-index.md` | Headers completed; both were the 2 files reported incomplete by the first package check | `dossier-package-check.sh` | headers complete |
| F-11 | All 23 documents | Re-pinned from `d0fa737` to `fbeb1ee`; the dossier assertion count, the combined count, commits on the branch, commits across all refs, tracked files, repository size, and the decision-record count all re-derived from the pinned commit | Each figure's own command, re-executed | 0 stale figures remaining |
| F-12 | `plugins/dossier/templates/package-readme.md`, `bin/dossier-scaffold.sh`, `bin/dossier-package-check.sh`, `skills/doc-package-contract/SKILL.md`, `tests/bin-scripts.test.sh`, `tests/references-integrity.test.sh` | Signpost template added and scaffolded to the output root; never overwritten; excluded from the canonical count; its links validated against the canonical package layout rather than the plugin tree it is stored in | `plugins/dossier/tests/run.sh` | pass |
| F-13 | `02-architecture/data-and-ai.md`, `02-architecture/infrastructure-and-deployment.md`, `06-public/technical-partner-guide.md`, `bin/dossier-package-check.sh`, `tests/bin-scripts.test.sh` | Logical data model, deployment topology, and partner integration diagrams drawn; coverage check added comparing each template's prompts against the document's diagrams | `dossier-package-check.sh` | `CHECK_DIAGRAMS_EXPECTED=6 CHECK_DIAGRAMS_PRESENT=6` |
| F-14 | `00-control/evidence-ledger.md` | `EV-0046` and `CHK-23` set to the same observed value | `git ls-files .decisions/` | both read 11 |
| F-15 | `05-due-diligence/technical-due-diligence-report.md` | Risk register and management questions renumbered from 1; all 7 horizon references remapped | Leading-cell sequence in both tables | 1–9 and 1–6, no gaps |

## Unresolved findings

| Finding ID | Severity | Why it remains open | What would close it | Owner |
|---|---|---|---|---|
| F-05 | Low | Independent passes were not available in this run. Recording the limitation is the correct response; asserting independence that did not occur would be the failure mode | One re-run as separate agent dispatches, or an external audit against a different model | Daniel Bentes |

## Cross-document consistency

| Subject checked | Documents compared | Agreed | Discrepancy | Resolution |
|---|---|---|---|---|
| Test assertion counts (1022, 1190, 2,212) | 18, 18, and 15 occurrences across the package | **no, in round 3** | Every occurrence read 1034 and 2,056 — true at the old pin, stale at the commit under review | F-11, re-derived in all 33 |
| Artifact counts (112 skills, 61 commands, 29 agents, 16 hooks) | 8, 9, 8, and 11 occurrences | yes | none — unchanged across all three rounds | — |
| Repository history (198 commits, 355 across all refs) | 8 and 7 occurrences | **no, in round 3** | Read 194 and 351; three commits had landed since the evidence was gathered | F-11, re-derived |
| Decision records | 7 documents plus the ledger's own check row | **no, twice** | Round 1: ledger said 11, filesystem said 10. Round 3: the correction had reached the evidence row and not the check result beside it, and the underlying count had moved back to 11 | F-01, then F-14 |
| Repository size and file count | 6 occurrences | **no, in round 3** | Read 541 files and ~3.5 MB against 570 and ~3.0 MB | F-11, re-derived |
| Pin versus reported state | All 23 headers | **no, in round 3** | Headers named a commit at which the licence they report as present does not exist | F-11, re-pinned |
| Plugin count (8 published, 7 on `main`) | 6 documents | yes | none — every mention carries the branch qualification | — |
| Licence state | 5 documents | yes | none | — |
| Terminology | All 23 files against 25 `TM-` entries | yes | none | — |
| Diagram coverage | 23 template/document pairs | **no, in round 3** | 3 of 6 contract-requested diagrams absent | F-13, all three drawn |

Rounds 1 and 2 reported this section clean. Round 3 found six subjects that disagreed, five of them because the repository moved after the evidence was gathered and nothing re-checked. That is the finding behind the finding: a consistency pass run once, at drafting time, certifies a package that is already going stale.

## Disclosure and secret safety

| Check | Scope | Result | Detail |
|---|---|---|---|
| Secret patterns in the package | 23 files, 12 pattern classes | pass | 0 matches. The only credential-shaped strings in the repository are synthetic fixtures inside the plugin's own detector tests, which is exactly the exclusion EV-0037 states |
| Internal identifiers in `06-public/**` | 2 files | **fail, then pass** | 34 `[CL-####]` citations found and removed (F-02) |
| Internal paths or names in `06-public/**` | 2 files | pass | 0 matches. No absolute path, no home directory, no hostname |
| Unapproved claims in `06-public/**` | 2 files | **fail** | 52 unregistered sentences (F-03) |
| Prohibited words used without scope | 2 files | pass | `CLAIM_SCAN_PROHIBITED_VOCABULARY=0`. No absolute security or reliability language appears |
| Vulnerability or exploit detail | 23 files | pass | Gaps are named at the level of what is missing — no exploitation path, no proof of concept, no attack recipe |

## Mechanics validation

| Item | Checked | Passed | Failed | Detail |
|---|---|---|---|---|
| Internal links | 23 files | 23 | 0 | Every package-relative path named in prose exists |
| Contract pointers | 23 | 23 | 0 | Each resolves to a real anchor in a real reference file |
| Commands | 28 | 26 | 0 | 26 executed with output retained; 2 marked not executed with reasons |
| Examples | 6 | 6 | 0 | Every code block is either a real command that was run or a verbatim excerpt of a real file |
| Diagrams | 6 | 6 | 0 | Header form, bracket balance, quote balance, and node-label parentheses all validate. Every diagram the templates request is present |

## Residual uncertainty

| Uncertainty | Affects | Why it could not be resolved | Consequence of relying on the package anyway |
|---|---|---|---|
| Verification-pass independence | Every finding in this report | The passes ran inline in one context rather than as separate dispatches | A blind spot shared by all three would go undetected. Treat the *mechanical* findings — which are reproducible by re-running the commands — as strong, and the *judgment* findings as one reviewer's opinion |
| Whether the product helps anyone | Every quality statement in the package | No telemetry exists for plugin marketplaces and the plugins emit none (AQ-0004) | A reader could mistake artifact quality for user outcome. The package says so repeatedly for this reason |
| `main` versus the assessed branch | Every claim | The assessment ran at `06b1586` on an unmerged branch (AQ-0010) | A claim true here may be false on `main`. Version 4.7.0 and the entire dossier plugin exist only on this branch |
| `prompt-decorators` contents | Its rows in the architecture and due-diligence documents | The source is in another repository (AQ-0005) | One published plugin is described from its manifest alone |
| Windows behaviour | The portability claims | No Windows environment was available (AQ-0008) | The scope of two reported defects is unmeasured |

## Package status

| Field | Value |
|---|---|
| Status | **not ready** |
| Blockers, if any | Two gate conditions fail, both on score: 87 against a minimum of 95, and verification depth at 60% against an 80% floor. Both trace to F-05 — the passes were not independent — and neither is closable by editing the package. AQ-0002 remains open and blocking as a project item: dossier's post-merge automation has never executed. CT-0001 and F-03 were blockers in round 1 and are now resolved |

The verdict is the package working as designed, not a defect in it. Three of the four blockers are properties of the project being documented rather than of the documentation, and the fourth is an honest record of how this verification was run. A package that reported RELEASABLE under these conditions would be the failure this standard exists to prevent.

Round 3 sharpens that. It found five defects in a package two rounds had certified consistent, which means the gap the gate keeps naming is a real deficit in assurance rather than a procedural technicality. The remedy is unchanged and unchangeable by editing: a reviewer who does not share this one's priors.

## Next actions

| Rank | Action | Owner | Evidence required | Unblocks |
|---:|---|---|---|---|
| 1 | Re-run verification as three separate agent dispatches, or `--external` against a different model | Daniel Bentes | Three pass outputs that are materially different from one another | F-05, and the only two conditions still failing. Round 3 raised the expected yield: a same-reviewer re-look found 5 defects, so an independent one should be assumed to find more |
| 2 | Run dossier's post-merge workflow once, end to end | Daniel Bentes | An Actions run URL and the documentation pull request it opened | AQ-0002, and 1 scorecard point |
| 3 | Confirm GitHub reports the licence after merge | Daniel Bentes | `gh api repos/… --jq .license` returning `apache-2.0` | AQ-0011 |

---

## Round 4 — 2026-09-10

Three passes, three isolated contexts, one model. `verification.passModels` is `inherit` for all three, so each pass received an independent context but not an independent model. That is the configured default and it is the weaker half of the design: true cross-model independence requires `/dossier:audit --external`, which was not run. Recorded here rather than left implicit, because "three independent passes" otherwise reads as more assurance than was purchased.

Package under audit: `c9b2a24` on `docs/dossier-refresh`. Structural precheck CLEAN — 23 of 23 files, 23 headers valid, 6 of 6 diagrams. Prior rounds: 3.

| | Pass A — evidence | Pass B — falsification | Pass C — audience |
|---|---|---|---|
| Findings | 20 | 15 | 17 |
| Critical | 2 | 3 | 2 |
| High | 9 | 4 | 6 |
| Independent score | 58.0 | 64.4 | 62.9 |
| Minimum dimension met | no | no | no |

SCORE_VARIANCE=6.4 across a 58.0–64.4 range. Three lenses converging this loosely is the intended signal; near-identical scores would indicate contamination rather than agreement.

**Sections below are unmerged and unadjudicated.** No finding has been reconciled, deduplicated, severity-adjusted, or repaired. Three passes independently reported the documentation index as Critical (A4-01, B-06, C1) and the unregistered public sentences as Critical (A4-04, B-01, C2); that convergence is recorded, not resolved. Reconciliation is `/dossier:reconcile --round 4`.

**No gate verdict is issued by this round.** None of the three passes issued one, and this command does not compute one.


### Pass A — coverage and evidence integrity

<!-- DOSSIER_AUDIT round=4 pass=A model=opus-5 started=2026-09-10T16:20:00+02:00 findings=20 critical=2 high=9 -->

## Pass A Findings

| Finding | Required correction |
|---|---|
| **A4-01** · Critical · dim 1, 4 · `00-control/documentation-index.md`<br>The header claims `project-version: 7ee4923`, `last-verified: 2026-09-10`, `status: verified`. The body was not re-checked on that date. It still states package version 1.0.4, "Project version or commit `06b1586` on `fix/gate-verdict-integrity`", evidence cutoff 2026-07-26, "Evidence rows 56" (the ledger holds 191), `Last verified 2026-07-26` for all 23 documents (20 headers say 2026-09-10), `Confidentiality: Public` for all 23 documents (14 headers say `Internal`), per-document cited-row counts from the July package, and "Last gate verdict — 2 of 17 conditions fail" (the gate defines 19). `document-headers.md` rule 1 states that advancing `last-verified` without re-checking "is the single most damaging thing that can be done to this package", and rule 5 makes the header authoritative over the index.<br>_Verifier evidence:_ `git show c9b2a24 -- docs/dossier/00-control/documentation-index.md` changes exactly 4 lines, 2 of them the version and date stamp; header fields extracted from all 24 package files → 20 at `7ee4923`/2026-09-10 (14 `Internal`, 6 `Public`), 1 at `06b1586`/2026-07-26, 3 with no such fields (2 public guides, README); `dossier-ledger-lint.sh` executed by me → `LEDGER_ROWS=191`. | Re-derive the index body from the current headers and ledger, or revert its `last-verified` to `2026-07-26` and `project-version` to `06b1586` until the body is re-checked. Correct the evidence-row count, the per-document status/confidentiality/date table, the cited-row counts, and the gate condition count.<br>_Evidence still needed:_ none |
| **A4-02** · Critical · dim 1, 3 · `00-control/evidence-ledger.md:86` (EV-0035) and 20 consuming passages<br>EV-0035 — "All commits across all refs carry one author identity, Daniel Bentes … 370 commits" — is State `V`, `Public use: yes`, and absent from the Stale evidence section. It is falsified at HEAD: 479 commits across all refs, 22 of them under a second author identity. EV-0163's note records that second identity and still says it "re-confirms EV-0035 … rather than falsifying it"; EV-0163 measures `main` only, while EV-0035's claim is scoped to all refs, so the correcting row does not correct this row. The falsified wording is repeated as present-tense measured fact in `system-architecture.md:288`, `:291`, `components-and-codebase.md:235`, `infrastructure-and-deployment.md:155`, in the due-diligence verdict's fact (3) of "the three facts that drive it", and as red flag and rank-4 risk. Two decisions turn on it: a diligence reader weighing continuity, and — in `assets-dependencies-and-licenses.md:135` — the inbound-rights position, where "External contributions — none exist. All commits across all refs carry one author identity" is the stated reason no CLA or inbound licence question arises.<br>_Verifier evidence:_ executed `git rev-list --count --all` → 479; `git log --all --format='%an <%ae>' \| sort \| uniq -c` → 457 + 22 under a second identity. | Supersede EV-0035 with a row whose claim matches what was measured — identity concentration on `main`, count at a named commit — list EV-0035 in Stale evidence, and rewrite every consuming sentence to the `main`-scoped dated claim. Re-derive the provenance conclusion in the licensing document separately: it needs an answer about the 22 commits, not a restatement.<br>_Evidence still needed:_ a provenance determination for the commits carrying the second author identity |
| **A4-03** · High · dim 3, 5 · `05-due-diligence/assets-dependencies-and-licenses.md:160`, `05-due-diligence/technical-due-diligence-report.md:88, :209, :232, :262`<br>"Shell analysis \| none exists. No `shellcheck` runs in CI", "shell is outside its language set. The whole operator-facing shell surface has no automated security analysis", ranked risk 6 "Shell unanalysed", and the remediation item "A shell linter in CI". All false at HEAD. `dossier-tests.yml` installs shellcheck v0.11.0 by pinned release URL and lints dossier's 20 `bin/`, 5 hook and all test scripts. The package's own EV-0165 records this correctly and eight other documents state it correctly; the correction never reached the two diligence documents, which are the ones a decision-maker reads.<br>_Verifier evidence:_ `grep -n shellcheck .github/workflows/*.yml` → `dossier-tests.yml:134-138`; EV-0165 at ledger line 223. | Restate at true scope in both documents: shell static analysis exists over dossier's 25 scripts; flow's 34 `bin/` and hook scripts are unlinted (AQ-0019). Re-rank the risk and re-word the remediation to "extend the existing step", citing EV-0165.<br>_Evidence still needed:_ none |
| **A4-04** · High · dim 7, 9 · `06-public/**`, `00-control/claim-and-disclosure-register.md`<br>Both guides open with "Every statement in this guide maps to an approved claim backed by a verified evidence row", and the register's summary asserts "Sentences in `06-public/**` with no matching row: 0". Neither holds. Specific published claims have no `CL-####` row at all: the exit-code contract (0/1/2/3 with "Inconclusive" semantics), "No script retries anything", "Scaffolding never overwrites an existing file; installs and marketplace syncs are idempotent", "Rate limits — None imposed by the marketplace", "Timeouts — None", the prerequisite tables (`git`, `jq`, authenticated `gh`, "bash 3.2 and later"), "Release assets … Skill packages for Claude Desktop — stable", the `Since version 1.0.0` stability rows, all four troubleshooting resolutions, "Actions with consequences — merging, releasing, publishing — are gated behind explicit confirmation", and "The plugins have no behavioural evaluation". Separately, the register's mapping table omits seven partner-guide and four customer-guide sections; some claims in those sections *are* registered elsewhere (CL-0015, CL-0017, CL-0018, CL-0029), so the mapping is incomplete rather than wholly absent — but the register cannot be used to check the guides while it is.<br>_Verifier evidence:_ both guides read end to end; `grep -iE "exit\|retr\|idempot\|prerequisit\|bash 3.2\|rate limit" claim-and-disclosure-register.md` → 0 matches; mapping table at register lines 130-160. | Register, evidence and approve each unregistered sentence, or remove it from the guides. Complete the mapping table section by section. Correct the register's zero and both guides' opening sentence — the meta-claim is itself an unsupported public claim. Record that the scanner skips tables, so its zero is not a coverage result.<br>_Evidence still needed:_ approval decisions for each newly registered claim |
| **A4-05** · High · dim 3, 7 · `06-public/technical-partner-guide.md:122`, `03-assurance/reliability-performance-and-observability.md:99`<br>"Timeouts \| None. A `PreToolUse` hook blocks your tool call while it runs" (public, unregistered) and "Timeouts \| none in this project's own code \| [EV-0044]" (internal) are falsified by shipped code: `plugins/dossier/bin/dossier-scan-security.sh:113` and `dossier-scan-quality.sh:90` set `TIMEOUT_SECONDS="${DOSSIER_SCAN_TIMEOUT_SECONDS:-300}"` and escalate TERM→KILL; `plugins/flow/hooks/scripts/flow-goal-evaluator.sh` resolves `timeout`/`gtimeout` and degrades when neither exists.<br>_Verifier evidence:_ read of the three files at HEAD c9b2a24. | Replace both rows with the observed behaviour and an evidence row citing the two scan scripts and the evaluator hook. If the public statement is kept, register and approve it corrected.<br>_Evidence still needed:_ none |
| **A4-06** · High · dim 1 · `00-control/evidence-ledger.md:95` (EV-0044) and its 18 consuming documents<br>The refutation of EV-0041 is incomplete. EV-0044 is `V`, `Public use: yes`, and its Source ref is `derived: EV-0004..EV-0007, EV-0041` — a derivation from a row the same ledger marks **Refuted 2026-09-10**. Its claim is also falsified in part: "shipped artifacts are Markdown, JSON, and shell" against 53 tracked `.py` files, three under `plugins/flow/bin/` — precisely the entrypoints EV-0187/EV-0189 identify as requiring PyYAML. EV-0044 is the support for approved public claim CL-0007, whose Decision basis still reads "Markdown, JSON, and shell only, with one dependency manifest". It is further cited for two claims it does not entail even at its own scope: "Timeouts — none in this project's own code" and "Retries — none" (see A4-05).<br>_Verifier evidence:_ `git ls-files '*.py' \| wc -l` → 53; ledger lines 92 and 95; CL-0007 at register line 41. | Supersede EV-0044 with a row covering the four artifact classes actually shipped, deriving from rows that are not refuted; split the resilience claims onto their own rows; re-state CL-0007's decision basis.<br>_Evidence still needed:_ none |
| **A4-07** · High · dim 1, 7 · `06-public/customer-product-and-trust-guide.md:101`, `00-control/evidence-ledger.md:88` (EV-0037)<br>Public security claim CL-0016 — "No credential matching the project's own detector pattern set appears in any tracked file" — is stated in the present tense in a guide headed `product-version: 4.10.0`, `last-updated: 2026-09-10`. Its only evidence, EV-0037, was observed 2026-07-26 at `06b1586` with a stated freshness basis of "every commit", and roughly 284 files have changed since. EV-0037 remains `V`, `Public use: yes`, and does not appear in Stale evidence. Every internal citation carries the observation date (AQ-0022 records the gap); the public projection is the one place the date was dropped.<br>_Verifier evidence:_ ledger line 88 and CHK-19 at line 303, both dated 2026-07-26; Stale evidence section read in full — EV-0037 absent. | Re-run the credential scan at the assessed commit and supersede EV-0037, or restore the observation date into the public sentence and list the row as expired.<br>_Evidence still needed:_ a re-executed credential scan at `7ee4923` with retained output |
| **A4-08** · High · dim 4, 5 · `05-due-diligence/technical-due-diligence-report.md` header and Scope table<br>The report names three assessed trees: header `project-version: 7ee4923`; Scope row "Project version assessed `691bcdb`"; headline facts pinned to `af6e632`; plus rows still at `06b1586`/2026-07-26. A reader cannot determine which tree the verdict covers, and the trees differ in the manifest, plugin versions and the engagement config.<br>_Verifier evidence:_ `git log --oneline af6e632..7ee4923 --stat` — `.claude-plugin/marketplace.json`, `README.md`, `plugins/flow/README.md`, `.claude/settings.dossier.json` all change after `af6e632`. | State one assessed commit and reconcile it with the header. Row-level pins stay in their own cells; the declared assessment version must be single-valued.<br>_Evidence still needed:_ none |
| **A4-09** · High · dim 4 · `05-due-diligence/technical-due-diligence-report.md` (Merge gating row; Unknowns list)<br>The report states merge gating was "Not re-checked in this refresh, because `networkAccess` is false" and lists "Whether branch protection changed since 2026-07-26" as an open unknown. EV-0131 records a live re-check on 2026-09-10 — no protection, no rulesets, 0 Actions secrets — and `security-privacy-and-compliance.md:146` cites it. The report's second-ranked red flag is argued from evidence 46 days staler than the package holds, and presents a settled question as unknown.<br>_Verifier evidence:_ executed 2026-09-10 — `gh api …/branches/main` → `protected:false`; `gh api …/rulesets` → 0. Reproduces EV-0131. | Cite EV-0131 in the merge-gating row, remove the item from the Unknowns list, and reconcile the "Claims carried forward without re-checking" row.<br>_Evidence still needed:_ none |
| **A4-10** · High · dim 2 · package-wide (no location — that is the finding)<br>The repository tracks a root-level `tests/` directory of 29 files containing 10 executable checks — `agentteams-gate`, `cascade-resolve`, `finding-schema/validate.sh`, `hooks-symlink`, `issue-86/verify.sh`, `journal-orchestration`, `markertrust-gate`, three `skills/*` input-schema checks, `status-parser`. No evidence row, component table, test-surface table or source-inventory row mentions it. The package describes the tree's automated verification as exactly two plugin suites (CHK-04, `ls plugins/*/tests/run.sh` → 2 matches), and none of the root checks was executed or reported.<br>_Verifier evidence:_ `git ls-files tests` → 29 files; `grep -rn "status-parser\|agentteams-gate\|issue-86\|hooks-symlink\|markertrust" docs/dossier` → no matches. | Inventory the root `tests/` tree in `components-and-codebase.md` and the ledger's source inventory, add an evidence row, and either execute the checks or record them `not executed` with a reason. Re-check every claim of the form "the tree's only test suites are …".<br>_Evidence still needed:_ an executed or explicitly not-executed record for the 10 root-level checks |
| **A4-20** · High · dim 7, 9 · `06-public/technical-partner-guide.md:63`, `00-control/claim-and-disclosure-register.md:75` (CL-0041)<br>Approved public claim CL-0041 — "Both externally sourced plugins are pinned to a commit sha, so two installs performed on different days resolve the same tree" — is not entailed by its evidence. EV-0058 establishes what the manifest file says; EV-0127 establishes that a script comparing advertised versions passes. Neither observes the Claude Code client resolving an install, and both external entries carry `"ref": "main"` **alongside** `"sha"`, so which field the client honours decides whether the reproducibility conclusion holds. The package knows this: `assets-dependencies-and-licenses.md` states the identical sentence with an `Inferred:` prefix and adds "No evidence row here observes the Claude Code client performing such an install at the pinned sha." An `I` claim may not go public. The same unobserved client behaviour is the sole basis for the due-diligence report clearing the red flag "Floating ref on an external plugin source".<br>_Verifier evidence:_ `jq '.plugins[] \| select(.source\|type=="object")' .claude-plugin/marketplace.json` → both entries carry `ref: main` and a `sha`; assets document's `Inferred:` paragraph; DD report red-flag table. | Re-word the public claim to what the manifest states, or obtain an observed install at the pinned sha and re-state it as verified. Re-open the cleared red flag until the client's resolution precedence is evidenced.<br>_Evidence still needed:_ an observed client install resolving each external entry at its pinned sha, or documentation of the client's `ref`-versus-`sha` precedence |
| **A4-11** · Medium · dim 4, 5 · `technical-due-diligence-report.md:101, :127, :162`, `decisions-technical-debt-and-risks.md:30, :53`<br>Five statements read "No register row yet — an AQ row is requested in this draft's handback / report". Two are false: AQ-0023 covers the exit-path unknown and AQ-0024 covers evaluation coverage beyond flow. The compliance-regime unknown genuinely has no register row, leaving a `U` claim in a diligence document with no `AQ-####` locator. The phrasing is also drafting-process narrative inside a reader-facing deliverable.<br>_Verifier evidence:_ AQ register lines 49-50; no AQ row matches the compliance-regime unknown. | Cite AQ-0023 and AQ-0024 in place of the "no row yet" sentences; open rows for the compliance-regime unknown and the two `AQ-pending` markers in the assets document; remove the handback narrative.<br>_Evidence still needed:_ none |
| **A4-12** · Medium · dim 2, 4 · `00-control/evidence-ledger.md`, Source inventory and Unavailable evidence sections<br>Both still present `networkAccess is false` as the live coverage limit — "GitHub Actions run history and code-scanning alerts: no / no / none", "CodeQL findings … not readable", "neither tree is vendored here and `networkAccess` is false". Eight rows in the same ledger came from live GitHub reads on 2026-09-10: EV-0126, EV-0127, EV-0131, EV-0134, EV-0140, EV-0141, EV-0168, EV-0183, several "read through the GitHub contents API at the exact pinned sha". A reader sizing inspection coverage from these tables gets the wrong answer, and the index repeats it under "Source classes unavailable".<br>_Verifier evidence:_ source-inventory table lines 256-278 against rows at 179, 185, 197, 226, 245. | Update both tables to record what was read, with the AQ-0012 ceiling caveat attached, and mark each superseded "unavailable" entry with the row that resolved it.<br>_Evidence still needed:_ none |
| **A4-13** · Medium · dim 2 · `00-control/.scope.json` (absent)<br>The resolved scope artifact the verification contract names does not exist anywhere in the working tree. The engagement configuration `.claude/settings.dossier.json` exists and is registered as supplemental, but no resolved scope is retained. A reader cannot reproduce the action ceiling, the claim-sample thresholds or the delivery mode, so every "the ceiling forbids this" statement in the package is uncheckable from the package.<br>_Verifier evidence:_ `find . -name .scope.json` → no matches; `ls docs/dossier/00-control/` → 5 files. | Retain the resolved scope at `00-control/.scope.json`, or state in the index that it is not retained and name where it lives; cite it from every action-ceiling claim.<br>_Evidence still needed:_ the resolved scope artifact from the run |
| **A4-14** · Medium · dim 9, 6 · `customer-product-and-trust-guide.md:134`, `technical-partner-guide.md:43-48`<br>Both prerequisite tables name Claude Code, `git`, `jq` and `gh` and stop. EV-0187, EV-0189 and EV-0190 establish that `python3` plus PyYAML is a hard runtime requirement of flow's journal, run-state and FlowGoal machinery, that a `SessionEnd` hook reaches it with no operator action, and that on its absence `flow-goal-evaluator.sh` emits `{"decision":"approve"}` with no operator-visible warning. CL-0048 is pending, so the subject left the guides — but the prerequisite tables that remain assert a requirement set now known to be incomplete, where an installing operator reads it.<br>_Verifier evidence:_ read of `flow-goal-evaluator.sh:41`, `session-end-state.sh:48`, `_journal_atomic.py:51` — all match EV-0189/EV-0190 exactly. | Present a prerequisite claim to the approver, distinct from CL-0048, and add `python3` + PyYAML on approval; until then mark both tables as partial.<br>_Evidence still needed:_ an approval decision on the prerequisite wording |
| **A4-15** · Medium · dim 1 · `assets-dependencies-and-licenses.md`, asset inventory and licence table<br>The current asset inventory carries "57 release tags … latest `v4.6.2`, as read 2026-07-26" and "As read on 2026-07-26 at commit `f57126f`, 7 plugin manifests and 8 marketplace entries all declared Apache-2.0". At HEAD there are 63 tags, the latest release is v4.10.0 (2026-09-10), and 6 plugin manifests are tracked. The partner guide states 63 and v4.10.0, so the package states both figures.<br>_Verifier evidence:_ executed `gh api …/tags --paginate` → 63; `gh release view` → v4.10.0, 2026-09-10; `git ls-files 'plugins/*/.claude-plugin/plugin.json'` → 6, all Apache-2.0; manifest → 8 entries, all Apache-2.0; `LICENSE` present, 201 lines, Apache-2.0. | Re-derive the asset inventory at the assessed commit. Where a July figure is deliberately retained, name what supersedes it in the same cell.<br>_Evidence still needed:_ none |
| **A4-16** · Medium · dim 8 · `bin/dossier-package-check.sh` result as used by the package<br>`CHECK_RESULT=CLEAN` is presented as structural verification, but the same output reports `CHECK_LINKS_CHECKED=0` — the check validates no links, so a clean result is not evidence that links, paths or cross-references resolve.<br>_Verifier evidence:_ executed → `CHECK_FILES_PRESENT=23 CHECK_HEADERS_OK=23 CHECK_LINKS_CHECKED=0 CHECK_RESULT=CLEAN`. I separately resolved every relative link across all 24 package files → 0 broken, so the property holds; the check does not establish it. | State what the package check does and does not cover wherever its result is cited; record link validation as a separate executed check with retained output.<br>_Evidence still needed:_ a retained link-check result |
| **A4-17** · Medium · dim 5, 8 · `07-verification/documentation-verification-report.md`, `00-control/documentation-index.md`<br>The verification report is stamped `06b1586` / 2026-07-26, round 6, package 1.0.4, and describes passes over documents since fully re-drafted; its own independence section records that all three passes shared one context. The index reproduces that round-6 gate verdict as current package status under a 2026-09-10 stamp. At this commit no verification round covers any of the 21 re-drafted internal documents, and the index does not say so.<br>_Verifier evidence:_ report header and Scope table; index lines 21-30; `git show --stat` for `d3fc744`, `7c3bb24`, `1372b19`, `c9b2a24`. | State in the index's package-status block that the current version is unverified and that the retained report describes a superseded version. Do not present a superseded verdict as current.<br>_Evidence still needed:_ a completed verification round over the current package |
| **A4-18** · Medium · dim 3, 1 · `technical-due-diligence-report.md` (Config drift row)<br>"`.claude/settings.dossier.json` sets `dossier.ci.expectedPluginVersion` to "1.0.0" while the plugin is at 1.2.0 [EV-0085]" is false at the tree the document is stamped to. Commit `d5a5166` raised the value before `7ee4923`; the working tree at HEAD reads `1.2.0`, and no documentation commit has touched that file since. The report asserts a live defect the assessed tree does not have.<br>_Verifier evidence:_ executed `jq '.dossier.ci.expectedPluginVersion' .claude/settings.dossier.json` → `"1.2.0"`; `git log --oneline af6e632..7ee4923` shows `d5a5166` in range. | Supersede EV-0085 with a row observed at the assessed commit and remove or re-date the config-drift row.<br>_Evidence still needed:_ none |
| **A4-19** · Low · dim 1, 2 · `04-operating/operations-and-incident-response.md:61-66, :90-94, :151-153`, `00-control/documentation-index.md:39`<br>Sixteen `N/A` cells carry no evidence citation, and three carry no reason either — "Scale \| N/A", "Is it slow? \| N/A", "Partners \| N/A". The contract requires an `N/A` to be a claim with a reason and evidence, because "does not apply" and "was not inspected" are opposite claims that look identical. The rest cite their reason in prose but rest implicitly on EV-0044, whose claim is itself partly falsified (A4-06).<br>_Verifier evidence:_ `grep -rn "N/A" docs/dossier` → 109 occurrences, 16 of them on lines carrying no `EV-`, `AQ-`, `CT-`, `TM-` or `CL-` identifier; the three bare cells read directly. | Give each `N/A` a stated reason and a citation, or convert it to `Unknown:` with an `AQ-####` row where the truth is that it was not inspected.<br>_Evidence still needed:_ none |

## Coverage

| Evidence class | Inventoried independently | Present in the package | Gap |
|---|---|---|---|
| Repositories / modules | 1 repository; 6 tracked in-tree plugins + 1 ignored untracked residue; 2 external plugin sources; `scripts/` (2); root `tests/` (29 files); `.decisions/` (21); `.flow/` (6); `.claude/` (14) | 6 in-tree plugins, the residue, both external sources, `scripts/`, `.decisions/`, `.flow/` all covered | Root `tests/` tree absent everywhere (A4-10); `.claude/settings.local.json` not mentioned |
| Services / deployment units | none — no runtime; distribution is a client-side git read | Covered, `N/A` justified with evidence | none |
| Interfaces / schemas | `marketplace.json`; 6 `plugin.json`; `plugins/{flow,dossier}/schema.json`; 8 JSON schemas under `plugins/flow/schemas/`; 2 `hooks.json`; trigger templates; the exit-code contract | Covered in `interfaces-and-integrations.md` | The published interface commitments carry no register rows (A4-04); reproducibility claim unentailed (A4-20) |
| Environments | operator machine (macOS/Linux, bash 3.2), GitHub Actions `ubuntu-latest`, assessment machine | Covered and distinguished | none |
| Pipelines / CI workflows | 5 tracked workflows: codeql, dossier-tests, flow-tests, marketplace-manifest, release-desktop-skills | 5 named correctly at HEAD | Ledger source inventory still says "All 4 workflows" (A4-12) |
| Tests | 2 plugin suites (2336 / 1951 assertions); flow evals (4 cases, 82 files); 10 root-level checks | The 2 suites and the evals covered in depth | Root-level checks missing (A4-10) |
| Dashboards / telemetry | none exist; Actions history is the only signal | Covered; absence stated as absence, not as safety | none |
| Incidents / runbooks | none; no `SECURITY.md`, no disclosure channel | Covered, gap named as a red flag | none |
| Product artifacts | root README, 6 plugin READMEs, 2 CHANGELOGs, 63 tags, releases through v4.10.0, desktop skill ZIPs | Covered | Stale tag and release figures in the assets document (A4-15) |
| Security / compliance evidence | CodeQL (actions, python); shellcheck in `dossier-tests.yml`; credential scan 2026-07-26; blocking hooks; no dependency scan; no SBOM | Mostly covered, and honestly stated as absent where absent | shellcheck denied in the two diligence documents (A4-03); credential scan stale (A4-07) |
| Dependencies / licenses | `LICENSE` Apache-2.0 (201 lines); 6/6 manifests and 8/8 entries Apache-2.0; no tracked dependency manifest; PyYAML and jsonschema undeclared; 2 CI actions on mutable major tags; 2 sha-pinned external sources | Covered well, including the undeclared-requirement correction | EV-0044's artifact-class claim falsified (A4-06); public prerequisite tables incomplete (A4-14) |
| Stakeholder sources | single maintainer identity on `main`; 11 open issues; 21 decision records; approver decisions in the register | Covered | "one identity across all refs" falsified (A4-02) |

## Claim sample

STRATUM_1_CENSUS=~120 audited=~120 — every row in the claim and disclosure register (48 `CL-` rows plus 4 `CL-R` rows, statuses parsed mechanically and each row read); every claim sentence and table cell in both `06-public/` guides, read end to end; every security, licensing, financial, performance and current-versus-planned claim in `security-privacy-and-compliance.md`, `assets-dependencies-and-licenses.md` and `technical-due-diligence-report.md`, including the verdict, all 8 red-flag rows, all 10 ranked risk rows and all 8 remediation rows. No customer-reference claim exists; the absence was verified by search, not assumed.

STRATUM_2_POPULATION=191 ledger rows + 21 internal documents sampled=41 rows and 12 document sections method=risk-weighted, not random. Selection rules applied in order: (1) every row added or changed on 2026-09-10 in `e104483..c9b2a24` (EV-0162 to EV-0191); (2) every row cited by four or more documents (EV-0035, EV-0037, EV-0041, EV-0044, EV-0058, EV-0085, EV-0093, EV-0121, EV-0123, EV-0129); (3) every row whose stated freshness horizon had lapsed at HEAD; (4) every derived row (`derived:` locator); (5) every `R`, `I` and `U` row carrying decision weight in a diligence document. `claimSample.minRows` and `claimSample.percent` could not be read — the scope artifact does not exist (A4-13) — so the sample was sized by exhausting rules 1-5.

## Checks

| Check | Result | Reason if not executed |
|---|---|---|
| `bash plugins/dossier/bin/dossier-ledger-lint.sh --output-root docs/dossier` | pass — `LEDGER_ROWS=191 ERRORS=0 WARNINGS=0 DANGLING_CITATIONS=0`. Reproduces the package's figure; it checks citation shape, not citation truth | — |
| `bash plugins/dossier/bin/dossier-package-check.sh --output-root docs/dossier` | pass — 23/23 files, 23 headers, 6/6 diagrams, `CHECK_LINKS_CHECKED=0`, CLEAN | — |
| `bash plugins/dossier/tests/run.sh` at c9b2a24 | pass — `TOTAL pass=1951 fail=0`, exit 0, working tree unchanged before and after. Independently confirms the published dossier assertion figure at a tree source-identical to `7ee4923` | — |
| `bash plugins/flow/tests/run.sh` | **not executed** — EV-0164 records this suite leaking two branches into the repository and writing into a real `.decisions/` file during a run. Executing it would violate the read-only-outside-the-documentation-root constraint. The published figure 2336 therefore rests on the package's retained CHK-51 output, not on any result this pass produced | Suite is not isolated from the repository |
| Independent relative-link resolution over all 24 package files | pass — 0 broken links | — |
| Header rebuild from `references/package-contract-*.md` | pass — all 23 required paths present, all headers parse, all carry status and `last-verified`; 20 stamped `7ee4923`/2026-09-10, 1 at `06b1586`/2026-07-26, 2 public headers carry `product-version` and `last-updated` as the schema requires | — |
| `git rev-list --count --all`; `git log --all --format='%an'` | falsifies EV-0035 — 479 commits, 2 author identities | — |
| `gh api` branch protection, rulesets, license, tags, releases, issues | pass — protected false, 0 rulesets, Apache-2.0, 63 tags, v4.10.0 of 2026-09-10, 11 open issues, 2 Windows issues (#100, #130) | — |
| `jq` over `marketplace.json` and all tracked `plugin.json` | pass — 8 entries, 6 tracked plugins, all Apache-2.0; both external entries carry a `sha` **and** `ref: main` (A4-20) | — |
| flow artifact census (`git ls-files`, `find -name SKILL.md`) | pass — 32 skills, 23 commands, 9 agents, 14 hook scripts; matches EV-0129 and the partner guide | — |
| Source read of `_journal_atomic.py`, `session-end-state.sh`, `flow-goal-evaluator.sh` | pass — EV-0187, EV-0189, EV-0190 entail their claims exactly, including the silent `approve` path | — |
| `grep -n shellcheck .github/workflows/*.yml` | falsifies the two diligence documents' "no shellcheck runs in CI" | — |
| Source read of `dossier-scan-security.sh`, `dossier-scan-quality.sh` | falsifies "Timeouts: none" — 300s default with TERM→KILL | — |
| `jq .dossier.ci.expectedPluginVersion .claude/settings.dossier.json` | falsifies the config-drift row — reads `1.2.0`, not `1.0.0` | — |
| `git ls-files` for dependency manifests and `*.py` | pass on EV-0186 (no manifest tracked); falsifies EV-0044's artifact-class claim (53 tracked `.py`) | — |
| `N/A` sweep across the package | 109 occurrences; 16 carry no evidence identifier and 3 carry no reason (A4-19) | — |
| Credential-pattern scan at HEAD | **not executed** | Re-running the project's enumerated detector set risks transcribing credential-shaped patterns; the staleness itself is reported as A4-07 |
| Dependency-vulnerability scan | **not executed** | No scan artifact exists and none was produced; absence of a scan is recorded, never read as a clean result |
| Windows execution of shipped scripts | **not executed** | No Windows environment available |

## The decision-maker's position

Reading the package alone, I reach **proceed with conditions**, the same verdict the report states — and I reach it on a materially different evidence set than the report presents.

What it rests on: two shipped plugins carry executed test suites (I re-executed dossier's: 1951 assertions, 0 failures); the licence position is settled and consistent across the tree, the manifests and the remote; both external plugin sources carry commit shas and a CI check compares every advertised version against its source; the execution surface is readable plain text with no binaries; and the governance gaps — no branch protection, no disclosure channel, no dependency scan — are each a single file or setting.

Against the package's own presentation:

- The verdict is stated up front and not hedged. Conditions are specific and actionable.
- The risk register partly contradicts the evidence. Rank 6 ("shell unanalysed") is false as written (A4-03), rank 2's evidence is 46 days staler than the package holds (A4-09), and rank 4 rests on a falsified row (A4-02). Three of ten ranked risks need re-derivation before the ordering can be trusted.
- Strengths and liabilities are both present and honestly separated. The "absence of a scan is not a clean bill of health" discipline is maintained throughout, which is the single most important thing this package does right.
- Remediation estimates are ranges with stated assumptions and explicit low-confidence flags on the two items whose volume is unknown. No false precision.
- **Would a different reader reach the opposite position?** On these findings, no reader should reach "do not proceed", but a reader who checks `git shortlog --all`, the shellcheck step, or the config file the report calls drifted will find three of the report's own assertions false within minutes. That does not change the verdict; it changes how much of the rest they will re-check themselves, which is the real cost.

## Scorecard — round 4, pass A

| # | Dimension | Weight | Score | Weighted | Min required | Meets min | Justification (finding IDs) |
|---:|---|---:|---:|---:|---:|---|---|
| 1 | Evidence grounding and freshness | 18 | 5 | 9.0 | 14.4 | no | A4-02 (falsified row still `V`/`Public`), A4-06 (refuted row in a live derivation), A4-07 (expired public security evidence), A4-15, A4-18, A4-01 |
| 2 | Coverage and completeness | 12 | 7 | 8.4 | 9.6 | no | A4-10 (whole test tree uninventoried), A4-12, A4-13 |
| 3 | Technical correctness | 15 | 6 | 9.0 | 12.0 | no | A4-03 (CI fact denied), A4-05 (timeout claim falsified), A4-18, A4-02 |
| 4 | Cross-document consistency | 10 | 4 | 4.0 | 8.0 | no | A4-01 (index contradicts 20 headers and the ledger), A4-08 (three assessed commits), A4-09, A4-11 |
| 5 | Due-diligence decision value | 10 | 6 | 6.0 | 8.0 | no | A4-03, A4-09, A4-02 (three of ten ranked risks unsound), A4-08, A4-17 |
| 6 | Onboarding and operability | 10 | 8 | 8.0 | 8.0 | yes | A4-14 (incomplete prerequisites); onboarding document otherwise verified against the tree |
| 7 | Security, privacy, and disclosure safety | 10 | 5 | 5.0 | 8.0 | no | A4-04 (unregistered public claims), A4-20 (`I` claim published as fact), A4-07, A4-05. No secret, credential or personal datum found in the package |
| 8 | Reliability and verification depth | 5 | 6 | 3.0 | 4.0 | no | A4-16 (clean check covers no links), A4-17 (no round covers the current package), A4-10 |
| 9 | Public usefulness and claim integrity | 5 | 3 | 1.5 | 4.0 | no | A4-04 (guides' own meta-claim unsupported), A4-20, A4-14, A4-05 |
| 10 | Clarity and maintainability | 5 | 8 | 4.0 | 4.0 | yes | A4-11 (drafting-process narrative in two reader-facing documents); prose otherwise plain, with the four registers of speech kept distinct |

TOTAL=58/100  MIN_DIMENSION_MET=no

Pass A issues no gate verdict. On this pass's findings alone: two unresolved Critical findings, an unapproved and partly unentailed public claim set (A4-04, A4-20), and eight of ten dimensions below the 80% floor.

### Pass B — model falsification

<!-- DOSSIER_AUDIT round=4 pass=B model=opus-5 started=2026-09-10T14:16:17Z findings=15 critical=3 high=4 -->

## Pass B Findings

| Finding | Required correction |
|---|---|
| **B-01** · Critical · dim 9 · `docs/dossier/06-public/customer-product-and-trust-guide.md:77-78`, `docs/dossier/06-public/technical-partner-guide.md:87-88`<br>Five claim-bearing public sentences map to no row in `00-control/claim-and-disclosure-register.md` — not as `Proposed wording`, not in the Required-qualifications table, not anywhere in the file: "Everything is readable. There are no binaries and no bundles; you can read every hook before you install it." · "The hooks that exist are mostly restrictive — they block destructive commands, force-pushes, and writes containing credential patterns." · "Every hook ships as plain text you can read before installing." · "There are no binaries and no bundles." · "The hooks in the flow plugin are restrictive by design — they block destructive commands, force-pushes, and writes containing credential patterns." All are `security`-type claims, the census stratum. CL-0035 covers only the introducing sentence "Two things bound that risk, and you should verify both yourself."; the two bullets it introduces are unregistered. This falsifies CL-0031 ("Every statement in this guide maps to an approved claim backed by a verified evidence row"), which both guides print twice, and fails G04 and G10.<br>_Verifier evidence:_ executed normalize-and-match of every >25-char sentence in both `06-public/**` files against all 48 `CL-` rows including the limitations column and the Required-qualifications table; string probes for `no binaries and no bundles`, `plain text you can read`, `block destructive commands`, `restrictive` over the whole register file returned `False` for all four | Register and approve each sentence with evidence, scope and limitation, or delete it. Until then CL-0031 is false and must be withdrawn from both guides.<br>_Evidence still needed:_ approved `CL-` rows citing `V` evidence for the hook-readability and hook-restrictiveness claims |
| **B-02** · Critical · dim 7 · `plugins/dossier/bin/dossier-claim-scan.sh:243`<br>The control that makes B-01 detectable cannot see the sentences in B-01. The scanner's sentence selector skips whole line classes before any registration check runs: `case "$line" in ''\|'#'*\|'\|'*\|'---'*\|'<!--'*\|'- '*\|'* '*\|'> '*) continue ;;`. Every bullet item, every table row and every blockquote is exempt. The customer guide is predominantly tables and bullets, so most of its public surface is structurally outside the check. All five unregistered sentences are bullets, which is why they reached the tree. Running the scanner at HEAD reports `CLAIM_SCAN_UNREGISTERED_SENTENCES=0` against five that exist. The package's registration guarantee, gate conditions G04 and G10, and CL-0031 all rest on this scanner and on pass C; a clean scanner result is not evidence of registration completeness.<br>_Verifier evidence:_ executed `bash plugins/dossier/bin/dossier-claim-scan.sh` at HEAD `c9b2a24` → `CLAIM_SCAN_LEAKS=0 CLAIM_SCAN_PROHIBITED_VOCABULARY=0 CLAIM_SCAN_UNREGISTERED_SENTENCES=0 CLAIM_SCAN_REGISTER_PRESENT=1`; read the selector at line 243 | Extend the selector to bullet items and table cells, or state in the verification report and in `07-verification` that the registration check covers paragraph prose only and that bullets and tables are unchecked.<br>_Evidence still needed:_ a re-run of the scanner after the selector is widened |
| **B-03** · Critical · dim 3 · `docs/dossier/07-verification/documentation-verification-report.md` (whole file)<br>The document that states "how much this package's own claims are worth" was not refreshed. Its header still reads `project-version: 06b1586`, `last-verified: 2026-07-26`, `status: verified`, while the other 22 documents are re-pinned to `7ee4923` / `2026-09-10`. Its body reports "**GATE RESULT: NOT-RELEASABLE.** Two of seventeen conditions fail" (line 133), "Sixteen of seventeen conditions reported" (line 188) and "All 35 claims approved" (line 119). At HEAD the plugin defines **19** gate conditions (`plugins/dossier/references/release-gate-conditions.md`, G01–G19) and the register carries **33** approved claims. The verification report certifies a package version that no longer exists against a gate that no longer exists, and no `AQ`/`CT` row records that it was left behind.<br>_Verifier evidence:_ `grep -c '^| G[0-9]' plugins/dossier/references/release-gate-conditions.md` → 19, IDs G01–G19; register parse → 33 `approved`, 12 `superseded`, 3 `pending` of 48 rows; `grep -H project-version docs/dossier/*/*.md` → 22 files at `7ee4923`, this one at `06b1586` | Re-run verification and re-issue this report against the 19-condition gate at the package's current pin, or mark the file `stale` with a banner naming the version it describes and open a `CT-` row for it.<br>_Evidence still needed:_ a round-4 scorer verdict covering G01–G19 |
| **B-04** · High · dim 3 · `docs/dossier/03-assurance/security-privacy-and-compliance.md:92`, `docs/dossier/02-architecture/system-architecture.md:25,34`, `docs/dossier/00-control/assumptions-questions-and-contradictions.md:38` (AQ-0012), ledger notes on EV-0126 / EV-0127 / EV-0131<br>The package states that read-only GitHub reads were "observed outside the action ceiling" and that "The dispatched collectors could not [run GitHub commands], because `networkAccess` is false". The source says the opposite. `plugins/dossier/hooks/scripts/enforce-allowed-actions.sh:441-454` denies only `curl\|wget\|nc\|ncat\|telnet\|ssh\|scp\|rsync`, `git push\|remote add\|clone`, and **mutating** `gh` verbs, with an in-file comment: "Read-only gh and git commands are permitted". The instruction layer does not close the gap either — `plugins/dossier/agents/dossier-evidence-collector.md:29` says only "Do not execute a command the ceiling forbids". `gh api`, `gh run list` and `gh release view` are inside the ceiling. AQ-0012's premise — "GitHub reads the engagement's own action ceiling forbids" — is false, and three evidence rows carry a provenance qualifier that misdescribes the control.<br>_Verifier evidence:_ read `enforce-allowed-actions.sh:441-454` and the collector agent definition; executed the hook against a scope fixture: `gh api repos/foo/bar` → rc=0 (permitted), `curl https://example.com` → rc=2 (denied) | Correct AQ-0012, the security document's ceiling section, and the EV-0126/0127/0131 provenance notes to state that read-only `gh` is permitted by the implemented ceiling. Close AQ-0012 or restate it against the real gap.<br>_Evidence still needed:_ none — the source settles it |
| **B-05** · High · dim 7 · `docs/dossier/03-assurance/security-privacy-and-compliance.md:142` (control inventory, "Agent action ceiling … State: implemented")<br>The control is inert unless `<outputRoot>/00-control/.scope.json` exists. `enforce-allowed-actions.sh:53` and `enforce-output-root.sh:39` both `exit 0` when it is absent. That file is gitignored (`.gitignore:49`) and does not exist at this checkout, so during this engagement the action ceiling and the output-root containment enforced nothing. The package records the file's absence in the ledger (EV-0091, EV-0124) framed as a run-record fact; it never reaches the control inventory, the threat table (line 130, "An agent runs a denied action through a disguised spelling"), or the risk register. The posture is also the reverse of the same scripts' `jq` handling, which fails closed with an explicit BLOCKED message.<br>_Verifier evidence:_ executed `echo '{"tool_input":{"command":"curl https://example.com"}}' \| bash plugins/dossier/hooks/scripts/enforce-allowed-actions.sh` at the repository root → **exit 0, no output**; the same payload inside a fixture containing only `docs/dossier/00-control/.scope.json` → exit 2 | Change the row to `implemented, conditionally active` and name the scope-file precondition; add a risk row for silent inertness; state in the security document that no `.scope.json` existed during this engagement, so the ceiling was not in force.<br>_Evidence still needed:_ none |
| **B-06** · High · dim 4 · `docs/dossier/00-control/documentation-index.md`<br>The package's entry point still describes a superseded package and carries `status: verified` in its own front matter while doing so. Front matter: `project-version: 7ee4923`, `last-verified: 2026-09-10`. Body: "Package version 1.0.4", "`06b1586` on `fix/gate-verdict-integrity`", "Evidence cutoff 2026-07-26", "2 of 17 conditions fail after round 6", "Evidence rows 56", "all 35 approved claims rest on `V` rows", "every row defined is cited", an unresolved-items table listing AQ-0011, and a change summary whose last row is round 6 with no entry for the 2026-09-10 refresh (5 commits, 21 files). CT-0014 and CT-0015 record the canonical-document table, the package block and the evidence-coverage table and defer the repair to `/dossier:reconcile`; they do **not** cover the "35 approved claims" cell, the "every row defined is cited" cell, or the missing change-summary row. A `status: verified` header on a body known to be wrong is the defect a reader cannot detect.<br>_Verifier evidence:_ `gh api repos/synaptiai/synapti-marketplace --jq .license.spdx_id` → `Apache-2.0` (AQ-0011 closed); ledger parse → 191 distinct `EV-` rows, not 56; register parse → 33 approved, not 35; `git log --oneline -- docs/dossier` → 11 commits, 5 after the last change-summary row | Re-derive the index body from the 23 headers, the ledger, the register and the git history at the package pin; add the round-4 change-summary row; drop AQ-0011; extend CT-0014/CT-0015 to the three uncovered cells or set the index header to `partially verified` until it is rebuilt.<br>_Evidence still needed:_ none |
| **B-07** · High · dim 5 · `docs/dossier/00-control/assumptions-questions-and-contradictions.md:53` (AQ-0027), `:82` (CT-0019), `:110` (remediation item 4)<br>CT-0019's left-hand side — "Nine internal document headers carry `project-version: 06b1586`" — is false at HEAD. **One** does (`07-verification/documentation-verification-report.md`). The 38-evidence-row figure is correct. AQ-0027 and remediation item 4 both restate the nine-document scope, and CT-0019's affected-documents column lists eleven paths that no longer carry the pin, so a reader executing the remediation plan re-verifies the wrong set.<br>_Verifier evidence:_ `grep -l '^project-version: 06b1586' docs/dossier/*/*.md \| wc -l` → 1; `grep -c '\| 06b1586 \|' docs/dossier/00-control/evidence-ledger.md` → 38 | Re-derive CT-0019's observation, AQ-0027's scope, the affected-documents column and remediation item 4 from the current headers. One document, not nine.<br>_Evidence still needed:_ none |
| **B-08** · Medium · dim 1 · `docs/dossier/00-control/assumptions-questions-and-contradictions.md:78` (CT-0015)<br>The contradiction row that exists to correct the index's stale evidence counts carries a stale count of its own: "The ledger holds 174 `EV-` rows". The ledger defines **191** distinct `EV-` rows at HEAD.<br>_Verifier evidence:_ regex census of `^\|\s*EV-\d+\s*\|` over `evidence-ledger.md` → 191 unique IDs | Re-derive CT-0015's figure from the ledger at the package pin.<br>_Evidence still needed:_ none |
| **B-09** · Medium · dim 2 · `docs/dossier/00-control/documentation-index.md` evidence-coverage table<br>Beyond the cells CT-0015 names, the assertion "every row defined is cited" is independently false: 29 defined rows are cited nowhere outside the ledger (EV-0002, 0026, 0053, 0061, 0062, 0067–0070, 0076, 0079, 0082, 0083, 0087, 0091, 0108–0114, 0117, 0119, 0122, 0135, 0141, 0185, 0188). The per-document counts CT-0015 does name are wrong in every row I checked: brief 24 vs 49, system-architecture 30 vs 32, security 27 vs 76, due-diligence 28 vs 51, onboarding 17 vs 35, verification report 8 vs 2.<br>_Verifier evidence:_ two-way `EV-` set difference across all 24 package files; per-file distinct-ID counts | Re-derive the table, and state that superseded rows are intentionally uncited if that is the intent — counting them separately.<br>_Evidence still needed:_ none |
| **B-10** · Medium · dim 9 · `docs/dossier/00-control/claim-and-disclosure-register.md:67` (CL-0033), printed at `06-public/customer-product-and-trust-guide.md:11`<br>Census of all 33 approved claims against the ledger's state column: 32 cite only `V` or `C` rows; **CL-0033 cites no evidence row at all** (its Evidence column is `—`) and is published verbatim in the customer guide. CL-0031, printed twice in the same file, asserts that every statement is "backed by a verified evidence row". The index's "all 35 approved claims rest on `V` rows" is false for a second, independent reason.<br>_Verifier evidence:_ scripted join of every approved `CL-` row's evidence IDs against the ledger's state column — 0 rows citing a non-`V`/`C` state, 1 row citing nothing | Either attach an evidence row to CL-0033 or mark it as an editorial audience statement that CL-0031's scope explicitly excludes.<br>_Evidence still needed:_ an evidence row for the audience statement, or a scope amendment to CL-0031 |
| **B-11** · Medium · dim 3 · `docs/dossier/06-public/customer-product-and-trust-guide.md:77`, `docs/dossier/06-public/technical-partner-guide.md:87`<br>"There are no binaries and no bundles" is half false. I downloaded and listed `dossier--prose-clarity.zip` from v4.10.0: it contains one directory and one `SKILL.md`, no compiled artifact — so "no binaries" holds. But every release from v4.7.0 to v4.10.0 carries **71 `.zip` bundles**, produced by `.github/workflows/release-desktop-skills.yml` on `release: published`. The technical partner guide lists "Release assets \| distribution \| Skill packages for Claude Desktop" as a public interface eleven lines above the sentence denying bundles exist, and the customer guide names "a packaged skill export for Claude Desktop" under compatibility. The customer guide's version is also unscoped ("Everything is readable").<br>_Verifier evidence:_ `gh release view v4.10.0 --json assets` → 71 `.zip` assets, identical set and byte sizes at v4.9.0; `gh release download v4.10.0 -p 'dossier--prose-clarity.zip'` then `unzip -l` → 2 entries, `prose-clarity/SKILL.md` only | Scope the sentence to hooks and helper scripts and drop "no bundles", or state that the bundles contain only the same plain text. Reconcile with the release-asset interface row in the same document.<br>_Evidence still needed:_ an approved `CL-` row once the wording is scoped (see B-01) |
| **B-12** · Medium · dim 9 · `docs/dossier/06-public/customer-product-and-trust-guide.md` "Supported platforms and regions" → Compatibility requirements<br>The public row lists "Claude Code. `git`, `jq`, and for some plugins an authenticated `gh` CLI". It omits `python3` with **PyYAML**, which `04-operating/onboarding-and-local-development.md:42` records as "**Required.** flow's journal, FlowRun state and FlowGoal machinery need it at runtime [EV-0189]". No dependency manifest declares it, so an operator has no other way to learn it.<br>_Verifier evidence:_ `grep -ln 'import yaml' plugins/flow/bin/*.py` → `_flow_evidence_bundle.py`, `_journal_atomic.py`, both reached from `plugins/flow/hooks/scripts/session-end-state.sh` and `flow-goal-evaluator.sh`; `git ls-files` finds no `requirements.txt`, `pyproject.toml` or `package.json` | Add `python3` + PyYAML to the public compatibility row, scoped to flow, and state that flow's state machinery degrades silently without it.<br>_Evidence still needed:_ an approved `CL-` row for the requirement |
| **B-13** · Medium · dim 3 · `docs/dossier/06-public/customer-product-and-trust-guide.md` "Key journeys" → Remove it<br>"The plugin stops loading. Nothing of yours was held anywhere." The marketplace holds nothing, but the plugins write durable state **into the operator's own repository** and uninstalling does not remove it: flow's own manifest entry advertises "durable goals, workflows, triggers, and runs at `.flow/`"; dossier writes `docs/dossier/**` and `.dossier/**`; flow and gh-workflow write `.decisions/`. This is the only deletion statement in the package's data lifecycle, and it is untrue as a lifecycle claim even though it is true as a marketplace claim.<br>_Verifier evidence:_ `.claude-plugin/marketplace.json` flow entry description; `plugins/flow/hooks/scripts/flow-goal-stop.sh:86-88` reads `.flow/goals/*.goal.yaml` from the operator's cwd; `.flow/`, `.decisions/` and `.dossier/` all present in this working tree | Rewrite the journey result to separate "the project holds nothing about you" from "files the plugins wrote into your repository remain until you delete them", and name the paths.<br>_Evidence still needed:_ an approved `CL-` row |
| **B-14** · Medium · dim 8 · `docs/dossier/03-assurance/security-privacy-and-compliance.md:338` ("Registered to block, blocking behaviour unobserved")<br>The package treats hook enforcement as one posture. The sources show two opposite postures the package never separates. Fail-closed: flow's `block-destructive.sh:8`, `block-secrets.sh:8`, `block-force-push.sh:8` and dossier's `enforce-output-root.sh:17`, `block-unregistered-claim.sh:19`, `enforce-allowed-actions.sh:15` all exit 2 with a BLOCKED message when `jq` is missing. Fail-open and silent: flow's `verify-task-completion.sh:43` (`command -v jq \|\| exit 0` — the TaskCompleted quality gate) and `session-end-state.sh:47-48` (`command -v python3 \|\| exit 0`; `python3 -c "import yaml" \|\| exit 0` — run-state recording). On a machine without PyYAML the gate stops gating with no signal, which is the failure B-12's undeclared dependency makes reachable.<br>_Verifier evidence:_ executed `block-destructive.sh` with a destructive payload → exit 2 (also observed live: it blocked one of my own scratchpad commands); read the guard lines in `verify-task-completion.sh` and `session-end-state.sh` | Split the hook inventory into fail-closed and fail-open rows and name each fail-open hook's silent-disable condition.<br>_Evidence still needed:_ none |
| **B-15** · Medium · dim 6 · no `.scope.json` anywhere; no recorded model traces<br>`plugins/dossier/agents/dossier-evidence-collector.md:24` opens with "Read `00-control/.scope.json` first. It fixes: which roots you may read · which actions you may take · which batch you are covering." That file does not exist and is gitignored (`.gitignore:49`), so every collector dispatch begins by reading nothing, and B-05's silent inertness is the same missing file. Separately, `project-modeling` requires the model's end-to-end traces to be recorded so pass B can execute them rather than inventing its own; `00-control/terminology-and-ownership.md` carries no `## End-to-End Traces` section and the only `### Trace` heading in the package is one in `02-architecture/components-and-codebase.md:117`. This pass selected its own ten traces, so its results are not directly comparable to the model's.<br>_Verifier evidence:_ `find . -name '.scope.json' -o -name '.project-model.json'` → no results; `grep -n 'End-to-End Traces\|^### Trace' docs/dossier/00-control/terminology-and-ownership.md docs/dossier/02-architecture/*.md docs/dossier/01-project/*.md` → one hit | Commit a redacted resolved-scope record and the model's trace table into `00-control/`, or state in the verification report that no scope artifact and no recorded traces were available to the passes.<br>_Evidence still needed:_ the resolved scope and trace table for the round-4 engagement |

## Coverage

| Evidence class | Inventoried independently | Present in the package | Gap |
|---|---|---|---|
| Required package files (rebuilt from `plugins/dossier/references/package-contract-*.md`) | 23 | 23 | none |
| Plugin manifests (`plugins/*/.claude-plugin/plugin.json`) | 6 local + 2 external | 8 | none |
| CI workflow definitions | 5 | 5 (EV-0123) | none |
| Hook scripts | 19 (14 flow, 5 dossier) + 1 external | 19 | external plugin's hooks counted, not inspected — disclosed |
| Test suites executed | 2 | 2 | none |
| GitHub releases and assets | 21 releases, 71 assets on each of the latest 4 | release count claimed; asset inventory absent | B-11 |
| Evidence ledger rows | 191 defined | index says 56 | B-06, B-08, B-09 |
| Claim register rows | 48 (33 approved, 12 superseded, 3 pending) | index says 35 approved | B-06, B-10 |
| Public-guide claim-bearing sentences | 68 | 63 mapped | B-01 (5 unmapped) |
| Gate conditions defined by the plugin | 19 | verification report reports 17 | B-03 |
| Engagement scope artifact and recorded model traces | 0 and 0 | 0 and 1 partial | B-15 |

## Claim sample

STRATUM_1_CENSUS=41 audited=41 — every `security`, `licence`, `posture`, `quality`, `privacy` and `capability` row in the register that is `approved` or `pending` (33 + 3), plus the 5 rows of the Required-qualifications table. Method: parsed all 48 `CL-` rows mechanically; matched every one against the two public guides; matched every public sentence back against the register; and joined every approved row's evidence IDs against the ledger's state column. Result: 32 of 33 approved rows cite only `V` or `C` evidence, CL-0033 cites none (B-10), and 5 published sentences cite no row at all (B-01). No `compliance`, `financial`, `performance` or `customer-reference` claim exists in the register; those categories were verified absent rather than assumed `N/A`.

STRATUM_2_POPULATION=191 sampled=24 method=risk-weighted toward rows whose entailment is checkable by execution — the two suite results (EV-0098, EV-0107), Linux CI (EV-0126), branch protection and secrets (EV-0131), shellcheck scope (EV-0165), CodeQL matrix (EV-0134), blob-identity chains (EV-0143, EV-0174, EV-0184), scope-file absence (EV-0091, EV-0124), ceiling resolution (EV-0077), bypass classes (EV-0153), dependency surface (EV-0186, EV-0189), hook inventory (EV-0129, EV-0162, EV-0168), manifest check (EV-0080, EV-0127), pin resolution (EV-0178). 21 of 24 entailed their claim exactly; the exceptions are B-04 (EV-0126/0127/0131 provenance), B-08 and B-09.

## Traces

### Trace 1: Marketplace entry → installed plugin, and whether the version check would catch a lie (primary journey)
| Step | Component (source locator) | Interface | State touched | Package says | Divergence |
|---|---|---|---|---|---|
| 1 | `.claude-plugin/marketplace.json` | 8 entries: 6 string sources, 1 `github`, 1 `git-subdir` | none | "The marketplace publishes eight Claude Code plugins" (CL-0045) | confirmed |
| 2 | `plugins/*/.claude-plugin/plugin.json` × 6 | version fields | none | versions match | confirmed — decipon 1.5.0, gh-workflow 1.9.0, context-ledger 1.0.0, flow 3.3.0, ai-first-org-design-kit 1.5.1, dossier 1.2.0 |
| 3 | `scripts/check-plugin-versions.sh:150-230` | `gh api …/contents/.claude-plugin/plugin.json?ref=<sha>` | none | "Each entry's advertised version is checked against its source in CI" (CL-0046) | confirmed — resolves both object-form sources, refuses an unpinned external, exits 3 (never 0) on unverifiable |
| 4 | `.github/workflows/marketplace-manifest.yml` | PR + push-to-main path filter, weekly cron, `workflow_dispatch` | none | "on manifest changes, weekly, and on demand" | confirmed |
| 5 | Would it catch a lie? | `check-plugin-versions.sh:17-30` | — | CL-0046's limitation names the two blind spots | confirmed and honestly disclosed — a self-consistent stale pin and an unbumped version both pass |
| 6 | Enforcement | `gh api …/branches/main/protection` → 404 | — | "advisory rather than required" (CL-0013) | confirmed — the check can fail and the merge still lands |

### Trace 2: Identity, authorization, and the trust decision an installer makes
| Step | Component (source locator) | Interface | State touched | Package says | Divergence |
|---|---|---|---|---|---|
| 1 | Live repo settings | `branches/main/protection`, `rulesets`, `actions/secrets` | — | no protection, no rulesets, 0 secrets (EV-0131) | confirmed |
| 2 | `plugins/*/hooks/hooks.json` | `PreToolUse`, `PostToolUse`, `TaskCompleted`, `Stop`, `SessionEnd` | operator's shell | "Two of the plugins register hooks … and so does agent-capability-standard" (CL-0040) | confirmed — 2 tracked `hooks.json` |
| 3 | Hook privilege | hooks run under the operator's uid | operator's filesystem | "no sandbox" (CL-0020) | confirmed |
| 4 | Readability of what executes | 71 `.zip` release assets, contents plain text | operator's disk | "no binaries and no bundles" | **B-11** |
| 5 | Restrictiveness | 6 of 19 hook scripts are restrictive | — | "the hooks that exist are mostly restrictive" | **B-01** (unregistered), and the claim is broader than the inventory |
| 6 | Registration of the trust claims | claim register + `dossier-claim-scan.sh` | — | "every statement maps to an approved claim" (CL-0031) | **B-01**, **B-02** |

### Trace 3: Sensitive data from collection through deletion (substituted scope — the project stores nothing server-side, so the trace follows operator-side artifacts)
| Step | Component (source locator) | Interface | State touched | Package says | Divergence |
|---|---|---|---|---|---|
| 1 | Whole tree | no server, no database, no telemetry | none | "holds no personal data" (CL-0019) | confirmed |
| 2 | `plugins/flow/hooks/scripts/flow-goal-stop.sh:86`, `session-end-state.sh` | writes under `.flow/` in the operator's cwd | operator's repo | flow "provides durable … runs at `.flow/`" (its own manifest entry) | the public deletion answer omits it |
| 3 | `plugins/dossier/hooks/scripts/enforce-output-root.sh` | writes under `docs/dossier/**`, `.dossier/**` | operator's repo | — | same |
| 4 | Uninstall | Claude Code removes the plugin | plugin cache only | "Nothing of yours was held anywhere" | **B-13** |
| 5 | Deletion-request table | all cells "Not applicable" | — | correct for the marketplace | the answer's scope is narrower than the sentence above it |

### Trace 4: Change → test → release → deploy → rollback
| Step | Component (source locator) | Interface | State touched | Package says | Divergence |
|---|---|---|---|---|---|
| 1 | Commit on a branch | — | — | — | — |
| 2 | `.github/workflows/{flow,dossier}-tests.yml` | `runs-on: ubuntu-latest`, path-filtered | — | both suites pass | **confirmed by execution** at HEAD `c9b2a24`: `plugins/dossier/tests/run.sh` → `TOTAL pass=1951 fail=0`, exit 0; `plugins/flow/tests/run.sh` → `TOTAL pass=2336 fail=0`, exit 0, macOS 25.6, `/bin/bash` 3.2.57 — exactly EV-0107 and EV-0098 |
| 3 | Linux CI | `gh run list --branch main` | — | "green for every commit this range added to main" (EV-0126) | confirmed — runs 34467245068 / 34467245098 / 34467245045 and predecessors all `success` |
| 4 | Merge | no required check, no protection | `main` | advisory only | confirmed |
| 5 | Release | `gh release create` → `release-desktop-skills.yml` on `release: published` | 71 zip assets | "Release assets \| Skill packages for Claude Desktop" | confirmed; v4.9.0 and v4.10.0 assets byte-identical, consistent with `git diff --stat v4.9.0 v4.10.0 -- plugins/` showing only `plugin.json`, `CHANGELOG.md`, `settings.json`, `config.example.json` |
| 6 | Rollback | none defined | — | "Recovery of a bad commit already propagated to installers — not executed" | confirmed and honestly marked |
| 7 | CI on the branch under audit | `gh run list --branch docs/dossier-refresh` | — | — | **no runs.** Path filters exclude `docs/dossier/**`, so this is expected — but no document states that the package branch has never been through CI |

### Trace 5: External integration — GitHub API, normal and error paths
| Step | Component (source locator) | Interface | State touched | Package says | Divergence |
|---|---|---|---|---|---|
| 1 | `check-plugin-versions.sh:200` | `gh api -H 'Accept: application/vnd.github.raw'` | — | reads the remote plugin.json | confirmed |
| 2 | `gh` absent | line 168 → `FATAL … exit 1` | — | — | fail-closed by reading; not executed |
| 3 | `gh` non-zero or empty body | lines 205-213 | — | "An entry that cannot be checked is reported as unverifiable and fails" | confirmed — `UNVERIFIABLE` + exit 3 |
| 4 | Non-github host | `github_slug_from_url` returns 1 | — | — | confirmed — unverifiable, not a silent pass |
| 5 | Staleness of a valid pin | `gh api …/compare/<sha>...<ref>` | — | "REPORTED for pinned sources … deliberately not a failure" | confirmed |
| 6 | Rate limit or auth failure | indistinguishable from a fetch failure at line 205 | — | not documented | both collapse to `UNVERIFIABLE` — safe, but the operator cannot tell a rate limit from a deleted repository, and no document names this |

### Trace 6: Dependency outage / partial failure — a machine missing an optional dependency
| Step | Component (source locator) | Interface | State touched | Package says | Divergence |
|---|---|---|---|---|---|
| 1 | `jq` absent, dossier's three blocking hooks | exit 2 + BLOCKED | — | fail-closed | confirmed by reading |
| 2 | `jq` absent, flow's three blocking hooks | exit 2 + BLOCKED | — | "restrictive by design" | confirmed — and observed live: `block-destructive.sh` blocked one of my own scratchpad commands, exit 2 |
| 3 | `jq` absent, `verify-task-completion.sh:43` | `exit 0`, silent | quality ledger | one "registered to block" framing | **B-14** — the TaskCompleted gate stops gating with no signal |
| 4 | PyYAML absent, `session-end-state.sh:47-48` | `exit 0`, silent | `.flow/runs/*/run.yaml` | onboarding calls PyYAML "**Required**" | **B-14**, and the requirement is absent from the public guide (**B-12**) |
| 5 | `.scope.json` absent, `enforce-allowed-actions.sh:53` | `exit 0`, silent | — | control listed as `implemented` | **B-05** — executed: `curl` payload → exit 0 at the repository root |

### Trace 7: Capacity / rate / cost boundary (substituted — no runtime service exists, so the boundary traced is the GitHub API budget and the operator's model spend)
| Step | Component (source locator) | Interface | State touched | Package says | Divergence |
|---|---|---|---|---|---|
| 1 | `check-plugin-versions.sh` | 2 `gh api` calls per external entry × 2 entries, weekly | GitHub rate budget | not quantified | no cliff — confirmed |
| 2 | `release-desktop-skills.yml` | 71 sequential `gh release upload` calls per release | release assets | not quantified | serial loop with no retry and no per-call failure check; a mid-loop failure leaves the release partially populated while the job's `COUNT` echo still reports the loop count. No document names this |
| 3 | Model spend | plugins are prompts | operator's Anthropic account | "Model usage the plugins induce is billed to your own account" | confirmed and correctly disclosed |
| 4 | Hook wall-clock on a large diff | — | — | — | `not executed` — needs a synthetic large-diff session |

### Trace 8: Backup / restore / disaster (substituted — the project has no data store, so the trace tests the package's archival claim)
| Step | Component (source locator) | Interface | State touched | Package says | Divergence |
|---|---|---|---|---|---|
| 1 | Index "Archival rule" | "the history is the archive" | git | — | partially true |
| 2 | `git log --oneline -- docs/dossier` | 11 commits | — | change summary lists package versions 1.0.0–1.0.4 | no tag, trailer or commit maps a package version to a commit, so "restore package version 1.0.2" is not a mechanical operation; `git tag` matches no package version |
| 3 | The pin `06b1586` | `git cat-file -t` fails locally; `gh api …/commits/06b1586` resolves | — | CT-0019 records exactly this | confirmed and honestly disclosed; the commit is unreferenced on the remote and may be garbage-collected |
| 4 | Restore rehearsal | none | — | "Recovery of a bad commit already propagated to installers — not executed" | confirmed |

### Trace 9: Alert → incident → recovery
| Step | Component (source locator) | Interface | State touched | Package says | Divergence |
|---|---|---|---|---|---|
| 1 | Detection surfaces | Actions failure notification; CodeQL Security tab | — | "No other alert exists" | confirmed — 5 workflows, no external monitoring in the tree |
| 2 | CodeQL scope | `.github/workflows/codeql.yml` matrix `actions`, `python`, both `build-mode: none` | — | "Shell is outside every configured language" | confirmed — 153 tracked `.sh` files, none scanned |
| 3 | Shell linting | `dossier-tests.yml` shellcheck v0.11.0 over dossier `bin/`, hooks, tests only | — | "flow's 20 `bin/` and 14 hook scripts are not [linted]" | confirmed |
| 4 | Routing | one recipient | — | "Every alert above fires to one person's notifications, with no second recipient and no escalation path" | confirmed |
| 5 | Recovery | no runbook for a propagated bad commit | — | named as a blind spot | confirmed |
| 6 | Notification to installers | none | — | "There is no way for the project to reach you if something it published breaks your machine" | confirmed |

### Trace 10: Agent abuse path — the action ceiling as a containment control
| Step | Component (source locator) | Interface | State touched | Package says | Divergence |
|---|---|---|---|---|---|
| 1 | Ceiling resolution | `bin/dossier-resolve-config.sh` over `.claude/settings.dossier.json` | — | 6 of 7 actions false | confirmed |
| 2 | Activation gate | `enforce-allowed-actions.sh:53` | `.scope.json` existence | — | **B-05** — inert at this checkout |
| 3 | Denied word, plain spelling | executed in a scope fixture | — | rejected | confirmed, exit 2 |
| 4 | Quote-split spelling | executed | — | rejected (#157) | confirmed, exit 2 |
| 5 | `timeout N <cmd>` wrapper | executed | — | rejected | confirmed, exit 2 |
| 6 | Brace, default-value and command-substitution expansions | executed | — | "Three shell expansion forms bypass the action ceiling — **High**" (EV-0153) | confirmed — all three exit 0. The package's own High finding reproduces at HEAD |
| 7 | Read-only `gh` | executed: `gh api repos/foo/bar` → exit 0 | — | "The dispatched collectors could not [read GitHub], because `networkAccess` is false" | **B-04** — the ceiling permits it by design, with a code comment saying so |
| 8 | Unregistered content reaching a public document | `block-unregistered-claim.sh` (leakage only, by design) then `dossier-claim-scan.sh` | `06-public/**` | registration is enforced | **B-02** — the scanner skips bullets and table cells, so five security sentences passed |

## Checks

| Check | Result | Reason if not executed |
|---|---|---|
| `plugins/dossier/tests/run.sh` at HEAD `c9b2a24`, macOS 25.6, `/bin/bash` 3.2.57 | pass=1951 fail=0, exit 0 | — |
| `plugins/flow/tests/run.sh`, same host and commit | pass=2336 fail=0, exit 0 | — |
| `plugins/dossier/bin/dossier-claim-scan.sh` at HEAD | `UNREGISTERED_SENTENCES=0` against 5 that exist | — |
| Marketplace vs plugin.json version parity, 6 local entries | all match | — |
| `gh release view` asset inventory, v4.7.0–v4.10.0 | 71 zips each; v4.9.0 ≡ v4.10.0 byte sizes | — |
| `gh release download` + `unzip -l` of one asset | 2 entries, `SKILL.md` only — no binary | — |
| `gh run list --branch main --limit 10` | all `success` | — |
| `gh run list --branch docs/dossier-refresh` | no runs | — |
| `gh api repos/synaptiai/synapti-marketplace` licence/visibility | `Apache-2.0`, public | — |
| `gh api …/commits/06b1586` and `/fbeb1ee` | both resolve on the remote; neither in this clone | — |
| `enforce-allowed-actions.sh` × 7 payloads in a scope fixture | 4 denied; 3 expansion classes permitted; read-only `gh` permitted | — |
| `enforce-allowed-actions.sh` at the repository root, no scope file | exit 0 on a denied payload | — |
| `block-destructive.sh` with a destructive payload | exit 2 | — |
| Approved-claim → evidence-state join, all 33 rows | 32 cite `V`/`C`; CL-0033 cites nothing | — |
| `EV-` two-way set difference across all 24 package files | 191 defined, all cited somewhere, 29 uncited outside the ledger | — |
| Public-sentence → register match, both guides | 68 sentences, 5 unmapped | — |
| Required-file list rebuilt from `package-contract-*.md` | 23 of 23 present | — |
| Gate condition set defined by the plugin | 19 (G01–G19) | — |
| Windows behaviour of the shell scripts | not executed | no Windows environment available |
| Cold-profile first install of the marketplace | not executed | needs a clean Claude Code profile; matches AQ-0003 |
| End-to-end run of the post-merge documentation workflow | not executed | the template at `plugins/dossier/templates/ci/dossier-docs-refresh.yml` is not installed in `.github/workflows/`; matches AQ-0002 |
| Hook latency under a large diff | not executed | needs a synthetic large-diff session |
| `agent-capability-standard` and `prompt-decorators` source trees | not executed | external repositories, outside the permitted source roots |

## Scorecard — round 4, pass B (independent; no calibration to any other pass)

| # | Dimension | Weight | Score | Weighted | Min required | Meets min | Justification (finding IDs) |
|---:|---|---:|---:|---:|---:|---|---|
| 1 | Evidence grounding and freshness | 18 | 6 | 10.8 | 8/10 | no | B-03 (the verification report is a full refresh behind), B-06 (the entry point describes a superseded package while carrying `status: verified`), B-08, B-09, B-10. The ledger itself is exemplary — 191 rows, every citation resolves, supersession chains explicit — but three control documents were not re-derived from it |
| 2 | Coverage and completeness | 12 | 8 | 9.6 | 8/10 | yes | All 23 contract files present and substantive; both suites and every executable claim reproduced. B-09, B-15 |
| 3 | Technical correctness | 15 | 6 | 9.0 | 8/10 | no | B-04 (the stated reason read-only GitHub reads sat outside the ceiling is contradicted by the hook's own code and comment), B-03, B-11, B-13. Offsetting: every executable claim I ran reproduced exactly — 1951 and 2336 assertions, the CodeQL matrix, the shellcheck scope, the manifest checker's error paths, and the package's own High bypass finding |
| 4 | Cross-document consistency | 10 | 6 | 6.0 | 8/10 | no | B-06 (front matter contradicts the body of the same file), B-07 (CT-0019 asserts nine headers where one exists), B-03, B-11 (the bundle denial sits eleven lines below the release-asset interface row) |
| 5 | Due-diligence decision value | 10 | 8 | 8.0 | 8/10 | yes | Risks, debt and unknowns are ranked, owned and unsoftened; the bus-factor and no-disclosure-channel facts are stated plainly. B-07 points one remediation item at the wrong scope |
| 6 | Onboarding and operability | 10 | 8 | 8.0 | 8/10 | yes | The onboarding document is accurate and is the only place PyYAML's requirement is stated correctly. B-15 |
| 7 | Security, privacy, and disclosure safety | 10 | 5 | 5.0 | 8/10 | no | B-02 (the registration control cannot see bullets or table cells, which is most of the public surface), B-05 (a conditionally-inert control presented as `implemented`), B-04, B-14. No secret, credential or personal datum found in the package; the redaction discipline around detector patterns and machine paths holds |
| 8 | Reliability and verification depth | 5 | 4 | 2.0 | 8/10 | no | B-03 — no verification report exists for the current package version; the file present verifies a package one refresh old against a 17-condition gate the plugin replaced with 19. B-02 — a clean scanner result was treated as evidence of a property it cannot test |
| 9 | Public usefulness and claim integrity | 5 | 3 | 1.5 | 8/10 | no | B-01 (five unregistered security sentences falsify CL-0031, printed twice in each guide), B-10, B-11, B-12, B-13 |
| 10 | Clarity and maintainability | 5 | 9 | 4.5 | 8/10 | yes | The prose is direct and states limitations in the same sentence as the claim. Deduction only for B-06's index body, which a reader must know to distrust |

TOTAL=64.4/100  MIN_DIMENSION_MET=no

### Pass C — audience, consistency, disclosure

<!-- DOSSIER_AUDIT round=4 pass=C model=claude-opus-5 started=2026-09-10T14:06:50Z findings=17 critical=2 high=6 medium=7 low=2 -->

# Pass C — Audience, Consistency, and Disclosure

Package root `docs/dossier` at `c9b2a24` on `docs/dossier-refresh`. The scope file `docs/dossier/00-control/.scope.json` is **absent** (`find docs/dossier -name '.*'` returns nothing), so scope was rebuilt from `plugins/dossier/references/package-contract-*.md` rather than from the documentation index. Reader tasks were attempted using only the package. Every check below was executed by this pass; no result was read from the package's own report. The `started` value in the marker is the first wall-clock timestamp captured during the pass, not its true start.

## Critical

| Finding | Problem and required repair | Verifier evidence |
|---|---|---|
| **C1 · D4 · `00-control/documentation-index.md`**<br>Claim: "last-verified: 2026-09-10 / status: verified / project-version: 7ee4923" | The routing document for the whole package was re-stamped current while its body still describes the previous run. Its status block reports project version `06b1586` on `fix/gate-verdict-integrity`, evidence cutoff 2026-07-26, and a gate verdict of "2 of 17 conditions"; the canonical-documents table gives all 23 rows as `Public / verified / 2026-07-26`; evidence coverage reports 56 rows and 35 approved claims. Live: 191 evidence rows, 48 register rows of which 33 approved, 19 gate conditions (`TM-0011`), and headers reading 2026-09-10 with 16 of 21 internal documents at `partially verified` and 14 at `Internal`. Comparing the index table against the headers cell by cell, **75 of 115 cells disagree** — 22 `last-verified`, 18 `status`, 17 `audience`, 16 `confidentiality`, 2 `owner`. `references/document-headers.md` maintenance rule 5 makes the header authoritative and the index the defect; rule 1 makes the stamp itself a claim that someone re-checked the document, and the stamping commit changed two frontmatter lines and nothing else. Every reader route in this audit began here and began from a wrong picture of the package's own currency.<br>**Repair:** rebuild the index body from the current headers and registers, or revert its header to the date its body was verified. The two states cannot both stand.<br>**Evidence still needed:** none. | `git diff c9b2a24^ c9b2a24 -- docs/dossier/00-control/documentation-index.md` → 2 insertions, 2 deletions, both frontmatter; scripted header-vs-index comparison over 23 rows × 5 columns → 75 mismatches; `grep -c '^\| EV-' 00-control/evidence-ledger.md` → 191; distinct `CL-####` rows → 48; executed 2026-09-10 |
| **C2 · D9 · `06-public/*.md` (both guides)**<br>Claim: "Every statement in this guide maps to an approved claim backed by a verified evidence row." | The register's own `Mapping to public documents` tables cover 11 of the partner guide's 19 sections and 8 of the customer guide's 13. Counting declarative table rows in the unmapped sections gives **60 rows plus one prose sentence with no `CL-####` row at all** — 39 in the partner guide (*Supported use cases* 4, *Prerequisites and access* 4, *Authentication and authorization* 3, *Behaviour a partner must handle* 10 including the four-row exit-code table, *Security and data responsibility* 5, *Operations and support* 5, *Change communication* 4, *Troubleshooting* 4) and 22 in the customer guide (*Key journeys* 3, *AI features* 6, *Supported platforms and regions* 4, *Support and status* 6, *Document details* 2+1). That is a floor: it excludes mapped sections carrying more rows than claims — *Public interfaces* has five rows against one `CL` row, *Versioning and compatibility* five against three. No register row mentions `jq`, `gh`, prerequisites, retries, idempotency, rate limits, timeouts or exit codes. These rows direct partner behaviour, so the Iron Law is breached at scale and `CL-0031`'s own limitation column ("if the scan reports an unregistered sentence, this claim is false") is triggered. My independent run of `dossier-claim-scan.sh` reproduces `UNREGISTERED_SENTENCES=0`, which confirms the scanner is blind here rather than that the guides are clean — its own note says it "remains a proxy for tables, bullets and fenced blocks, which the scanner skips", and the guides are almost entirely tables.<br>**Repair:** register every table row in the unmapped sections and obtain approval, or remove the sections. Do not treat the scanner's zero as coverage of table content.<br>**Evidence still needed:** approver decisions on the new rows. | `bash plugins/dossier/bin/dossier-claim-scan.sh --output-root docs/dossier` → `CLAIM_SCAN_UNREGISTERED_SENTENCES=0 CLAIM_SCAN_LEAKS=0`, exit 0; scripted section-vs-mapping comparison counting non-header table rows → 39 and 22; `grep -n 'idempoten\|retries\|exit\|Rate limit\|jq\|gh CLI\|Prerequisite' 00-control/claim-and-disclosure-register.md` → 3 hits, all in CL-0009/0047/0048, none covering guide text |

## High

| Finding | Problem and required repair | Verifier evidence |
|---|---|---|
| **C3 · D7 · `06-public/*.md` header + body**<br>Claim: "Applies to product version 4.10.0" together with "Each entry's advertised version is checked against its source in continuous integration." | Both guides are labelled `product-version: 4.10.0` and both state the CI version check as a present safeguard. The tag `v4.10.0` resolves to `c0fb802`, and neither `scripts/check-plugin-versions.sh` nor `.github/workflows/marketplace-manifest.yml` exists in that tree — both arrived after the tag. The register pins `CL-0045` and `CL-0046` to `Applicable version: 7ee4923`, a commit reachable only from `docs/dossier-refresh`. Substituting a product version for that commit pin silently rebased every claim onto a release where at least this one is false. An operator who installs the current release is told a safeguard covers them that is not in what they installed. Every other headline fact I re-derived does hold at the tag (8 manifest entries, flow 32/23/9/14, 63 tags), which is what makes this one hard to notice.<br>**Repair:** label the guides with a version at or after the commit that introduced the check, or remove the CI-check sentences from any document labelled 4.10.0.<br>**Evidence still needed:** none. | `git rev-parse v4.10.0^{commit}` → `c0fb802`; `git ls-tree -r --name-only v4.10.0 \| grep -c 'check-plugin-versions\|marketplace-manifest'` → 0; same against `origin/main` and `7ee4923` → 2; executed 2026-09-10 |
| **C4 · D7 · `06-public/customer-product-and-trust-guide.md:§Supported platforms and regions`**<br>Claim: "Platforms — Supported: macOS and Linux, with bash 3.2 or later" | The package records internally that both published releases, v4.9.0 and v4.10.0, ship the pre-fix flow runtime scripts that commit `16b4dc4` repaired; that the defect is a macOS / bash 3.2 failure in shipped runtime scripts rather than tests; and that "what an operator on macOS actually experiences at v4.10.0 is not established by any row" [EV-0144]. The guides carry `product-version: 4.10.0`, list macOS as supported without condition, and their limitation tables — the tables a customer reads to find exactly this — omit it. The macOS operator installing the current release is the reader most likely to be harmed and the least informed by these documents.<br>**Repair:** add the known-defective-release condition to both limitation tables, register it, and obtain approval; or re-label the guides to a version carrying the fix.<br>**Evidence still needed:** approver decision on the new limitation row. | `04-operating/operations-and-incident-response.md:§Standard procedures`, third row and the paragraph following; `00-control/evidence-ledger.md:EV-0144`, corroborated independently: `git diff --stat v4.10.0 HEAD -- plugins/flow/hooks/scripts/record-quality-run.sh plugins/flow/bin/flow-quality-ledger.sh` → +39 −1 across the two scripts; customer guide lines 41 and 130-136 and partner guide `§Known limitations` — no mention in either |
| **C5 · D7 · `06-public/customer-product-and-trust-guide.md:§Your data`, `§Key journeys`**<br>Claim: "Where is data stored — Nowhere. The project has no store of any kind." / "Nothing of yours was held anywhere." | The internal source is narrower. `02-architecture/data-and-ai.md` states "What state exists is agent-authored: files in this repository and files on the operator's machine" and raises `.flow/` state in a consuming repository as an open question (AQ-0016); `.decisions/` is agent-written durable state in the operator's own tree. The public sentences generalize "the marketplace runs no service" into "nothing is stored anywhere" — the simplification-broke-truth pattern the disclosure contract names. An operator uninstalling a workflow plugin is told nothing of theirs was held while plugin-written directories remain in their repository. `CL-0019`, the approved row, says only "the marketplace holds no personal data"; both sentences are broader than it and sit in sections with no register mapping.<br>**Repair:** scope the sentences to the marketplace's own infrastructure and state that plugins write files into the operator's repository, or withdraw them.<br>**Evidence still needed:** an evidence row enumerating operator-side paths written by each plugin. | `02-architecture/data-and-ai.md:19, 33, 58, 236`; `00-control/terminology-and-ownership.md:TM-0032`; customer guide `§Key journeys` row 3 and `§Your data` table |
| **C6 · D7 · `06-public/*.md` §Prerequisites and access / §Supported platforms and regions**<br>Claim: `— (omission)` | Both guides list prerequisites — Claude Code, `git`, `jq`, `gh` — and neither names `python3` with PyYAML. Internally this is required, not optional: onboarding marks it **Required** for flow's journal, FlowRun and FlowGoal machinery [EV-0189]; operations rates it criticality **high** and records that `hooks/scripts/session-end-state.sh` reaches the requirement "without the operator invoking anything, so a machine without it is affected before any command is typed". The claim that would disclose it, `CL-0048`, is `pending` and so unpublishable — but that leaves an affirmative prerequisites table that is wrong by omission, which is a different defect from silence. A partner following the guide provisions an environment in which flow fails at session end.<br>**Repair:** present `CL-0048` for approval and add the dependency to both prerequisite lists, or state in both guides that the list is not exhaustive. An affirmative list that omits a known runtime requirement cannot stand.<br>**Evidence still needed:** approver decision on `CL-0048`. | `04-operating/onboarding-and-local-development.md:§Prerequisites`, PyYAML row; `04-operating/operations-and-incident-response.md:§Service and dependency inventory`, `pyyaml` row; register `CL-0048` status `pending` |
| **C8 · D4 · `02-architecture/data-and-ai.md:33, 58`**<br>Claim: "11 records tracked in this repository" / "11 decision records written by the flow plugin" | Two documents disagree about the same fact and a reader cannot tell whether one journal is being described or two. `04-operating/operations-and-incident-response.md` says 21, twice; `00-control/terminology-and-ownership.md:TM-0032` — which the index names as the source of truth for components — says 21; the tracked tree has 21. `data-and-ai.md` cites `EV-0046`, observed 2026-07-26 at `06b1586`, while carrying `last-verified: 2026-09-10`. The stale figure is exactly what that header asserts was re-checked, and it is the demonstrated consequence of C14.<br>**Repair:** correct both occurrences and re-observe `EV-0046`, or downgrade the document's `last-verified`.<br>**Evidence still needed:** a re-observed `EV-0046`. | `git ls-files '.decisions/*' \| wc -l` → 21, executed 2026-09-10; `00-control/evidence-ledger.md:EV-0046` — `06b1586`, 2026-07-26; `04-operating/operations-and-incident-response.md:131, 174`; `TM-0032` |
| **C9 · D5 · `07-verification/documentation-verification-report.md` (whole document)**<br>Claim: "Project version or commit `06b1586` on `fix/gate-verdict-integrity`" | The one document that tells a reader how much the package is worth is pinned to a commit that no ref in a clone reaches — `git cat-file -t 06b1586` fails locally, though the GitHub API still serves it as a squashed-away branch commit dated 2026-07-26. Nothing in the report can therefore be re-verified from a clone. It was the only internal document not re-stamped while 20 others were, and it describes a six-round run over 56 evidence rows and a 17-condition gate against a live package of 191 rows and a 19-condition gate (`TM-0011`). The index routes the diligence reader here for the gate verdict, and the due-diligence report lists it as a dependency; both land on a self-assessment of a different package.<br>**Repair:** re-run the verification for this commit and rewrite the report, or mark it `draft` and state at the top that it describes a superseded run at a commit unreachable from any ref.<br>**Evidence still needed:** the current round's verification results. | `git cat-file -t 06b1586` → `fatal: Not a valid object name`; `gh api repos/synaptiai/synapti-marketplace/commits/06b1586` → `06b1586245ea…`, 2026-07-26, "fix(dossier): stop the gate passing over a condition it never evaluated"; report header `last-verified: 2026-07-26` |

## Medium

| Finding | Problem and required repair | Verifier evidence |
|---|---|---|
| **C7 · D6 · `04-operating/onboarding-and-local-development.md:§Setup` step 3, `§First day, week, and month`, `§Troubleshooting`**<br>Claim: "Verifies success by `TOTAL pass=… fail=0`" / "Both suites run green locally" | I followed the documented day-one route literally from a clean public clone. Steps 1, 2, 4 and 5 reproduce exactly as written, including the no-submodule and no-`agent-capability-standard` claims and step 5's `8 plugin(s) checked, 0 failed, 0 unverifiable`. **Step 3 returned `TOTAL pass=2335 fail=1`, exit 1**, twice. Isolating the variable: the flow tree is byte-identical between the clone and the working tree apart from one README line, the miner produces identical output in both, and the cause is the absolute checkout path. The test asserts on a table cell rendered by `cell(transcript_path, 200)`; the fixture path is 105 characters under the maintainer's checkout and 205 under mine, so the substring the assertion looks for is truncated away. The suite's pass/fail result is therefore a function of where a contributor clones, and neither the setup table, the first-day criterion, nor the troubleshooting table says so — a contributor who lands on a deep path sees a red suite the document gives no route out of. The recorded green figure is honest for short paths and I reproduced it at the working-tree path; it is the unconditional phrasing of the criterion that is wrong.<br>**Repair:** state the condition on step 3 and add a troubleshooting row for it, or fix the truncation so the assertion does not depend on checkout depth.<br>**Evidence still needed:** none. | Clean clone at `0e79776`, macOS 25.6, `/bin/bash` 3.2.57, 2026-09-10: `plugins/flow/tests/run.sh` → `TOTAL pass=2335 fail=1`, exit 1, twice; working tree at `c9b2a24` same day → `TOTAL pass=2336 fail=0`; `git diff --stat origin/main HEAD -- plugins/flow` → `plugins/flow/README.md \| 2 +-`; fixture path lengths 205 vs 105 characters against `cell(c['transcript_path'], 200)` at `plugins/flow/bin/flow-mine-corrections.sh:445`; miner output identical in both checkouts |
| **C10 · D7 · `00-control/claim-and-disclosure-register.md:CL-0047`; `06-public/customer-product-and-trust-guide.md:§Security, privacy, reliability, and accessibility`**<br>Claim: `— (omission)` — nothing about vulnerability scanning appears in either guide. | The withholding is recorded honestly and the register's stated control ("neither subject appears in `06-public/**` in any form, so the guides make no statement a reader could invert") is literally true. It does not hold in context. The customer guide presents security as an enumerated posture table — three `What is true` rows with a scope column — and both guides disclose weakness after weakness by name: no branch protection, no security policy, no private disclosure channel, four plugins untested, Windows broken. A trust reviewer reading a document that candid concludes the enumeration is complete, and "no dependency-vulnerability scan has been run, and the shell scripts sit outside every configured CodeQL language" is precisely the row that table has taught them to expect. The silence reads as absence of the problem. This is a judgment about reader inference, not about the approver's right to withhold.<br>**Repair:** re-present `CL-0047`, or add a scoping sentence stating that the security table enumerates named controls and is not a complete security assessment.<br>**Evidence still needed:** approver decision. | register `CL-0047` (pending) and `:110`; customer guide security table, 3 rows; partner guide `§Security and data responsibility`, 5 rows — no scanning row in either |
| **C11 · D9 · `06-public/technical-partner-guide.md:§Behaviour a partner must handle`**<br>Claim: "Scaffolding never overwrites an existing file; installs and marketplace syncs are idempotent" / "Re-invoke; every operation is a read or an idempotent write" | Two defects in one table. `never` appears without stated scope or evidence, which the disclosure policy prohibits at every policy level. More consequentially, the guide tells a partner that re-invoking is always safe because every operation is a read or an idempotent write — while the plugins this guide describes perform merges, releases, pushes and issue creation, which the project's own safety model classifies as Tier 2 and Tier 3. A partner building automated retry on that sentence retries a merge. Neither sentence maps to a register row (C2).<br>**Repair:** scope the idempotency claim to the scaffolding and read paths it was measured on, name the operations that are not idempotent, and register both sentences.<br>**Evidence still needed:** an idempotency determination per command class. | `grep -n -E '\bnever\b' 06-public/technical-partner-guide.md:120`; `.claude/CLAUDE.md` safety model, Tier 2 and Tier 3; register mapping table omits this section |
| **C12 · D4 · `06-public/*.md` (both bodies)**<br>Claim: "`.claude-plugin/marketplace.json`", "`plugins/*/.claude-plugin/plugin.json`", "the `main` branch", "`bin/`" | `references/package-contract-06-public.md:11` prohibits repository paths, file names and branch names in public documents, and `references/disclosure-policy-levels.md` repeats the prohibition in its never-crosses list without scoping it by policy level — I checked, and the `public` level does not relax it. Both guides carry several instances, and the approved wording of `CL-0013` embeds a branch name. The case for keeping them is strong, since the repository is public and the manifest path is the integration surface, but no exception is recorded and my run of `dossier-claim-scan.sh` returns `LEAKS=0`, so nothing enforces or documents the decision. A reader cannot tell a waiver from an oversight.<br>**Repair:** record an explicit exception in the register naming each path that is part of the public integration surface, or remove them.<br>**Evidence still needed:** none. | `bash plugins/dossier/bin/dossier-claim-scan.sh --output-root docs/dossier` → `CLAIM_SCAN_LEAKS=0`; `package-contract-06-public.md:11`; `disclosure-policy-levels.md`, "What must never cross the boundary" |
| **C13 · D4 · `06-public/*.md` header `product-version: 4.10.0`**<br>Claim: "Applies to product version 4.10.0" | Setting C3 aside, a reader still cannot tell which version of any plugin the guides describe. `4.10.0` is the marketplace's own version; the guides' concrete facts are per-plugin ("The flow plugin ships 32 skills, 23 commands, 9 agents, and 14 hook scripts"; "the dossier plugin's post-merge automation"), and the partner guide itself says the marketplace "carries a separate version" from the per-plugin semantic versions. Its only anchor is the circular sentence "the most recent release is v4.10.0". A partner pinning to a plugin version cannot map these counts onto it.<br>**Repair:** state the plugin versions the per-plugin facts were measured at, adjacent to those facts.<br>**Evidence still needed:** none. | Partner guide `§What ships in each plugin` and `§Versioning and compatibility`; `jq -r '.metadata.version' .claude-plugin/marketplace.json` → `4.10.0`; the per-plugin counts do hold at the tag, so the defect is the missing mapping, not the numbers |
| **C14 · D1 · 20 internal documents carrying `last-verified: 2026-09-10`**<br>Claim: "last-verified: 2026-09-10" | `references/document-headers.md` defines `last-verified` as the date the document's *claims were checked against evidence*, explicitly not the date the file was edited — so body churn does not settle the question. Testing evidence vintage instead: the ledger holds 191 rows, 135 observed 2026-09-10 and 56 observed 2026-07-26, the latter being the whole of the previous ledger carried forward unrefreshed. Every document rests substantially on September rows, but between 16% and 55% of each document's cited rows were never re-observed: `terminology-and-ownership.md` 51% (23 July rows), `system-architecture.md` 53%, `technical-due-diligence-report.md` 45%, `components-and-codebase.md` 45%, `data-and-ai.md` 43%, `assets-dependencies-and-licenses.md` 38%. The package demonstrates the honest form itself — `operations-and-incident-response.md` dates 17 citations inline and names the seven rows no September row re-checks — but 11 of the 20 carry neither an inline date nor a vintage caveat, including the terminology register the index names as a source of truth. C8 is what that produces. One premise conflict to record for reconciliation rather than adopt: this audit was told seventeen headers were re-stamped in one pass. Twenty carry 2026-09-10, the twenty-first still reads 2026-07-26, and the stamps landed across five commits, not one.<br>**Repair:** carry the operations document's convention across the package: date July-vintage citations inline, or name the unrefreshed rows in a preamble.<br>**Evidence still needed:** none. | Scripted join of each document's cited `EV-####` set against the ledger's observed-date column, all 22 documents; `grep -c 'observed 2026-07-26'` and a vintage-caveat grep per document, executed 2026-09-10 |
| **C15 · D6 · `04-operating/onboarding-and-local-development.md:§Prerequisites`, `§Troubleshooting`**<br>Claim: "`pip install --user pyyaml`" | The only remedy the document gives for a prerequisite it marks **Required** is not executable as written on the platform it measures itself on. `pip` is not on `PATH` on this macOS at all. The nearest equivalent depends on an interpreter the document never names: under Homebrew's `python3`, which is what `python3` resolves to here, `python3 -m pip install --user pyyaml` is refused with the PEP 668 externally-managed-environment error; under Apple's `/usr/bin/python3` it is accepted. The document is precise about interpreter identity elsewhere (it names `/bin/bash` 3.2.57 in five places), and both instances of this instruction are marked verified. The failure is recoverable — the error message names `--break-system-packages` — which is why this is Medium and not higher.<br>**Repair:** name the interpreter, and give a remedy executed on it with environment and date.<br>**Evidence still needed:** an executed install under each interpreter, and on Linux. | `which pip` → not found; `python3 -m pip install --user --dry-run pyyaml` → `error: externally-managed-environment … PEP 668`; `/usr/bin/python3 -m pip install --user --dry-run pyyaml` → `Requirement already satisfied`; macOS 25.6, executed 2026-09-10 |

## Low

| Finding | Problem and required repair | Verifier evidence |
|---|---|---|
| **C16 · D2 · `docs/dossier/00-control/.scope.json`**<br>Claim: `— (omission)` | The resolved scope this audit was directed to is not present anywhere under the package root, so the package does not record the scope it was produced under. Without it a later reader cannot distinguish "does not apply" from "was not inspected", which is the distinction several `N/A` sections depend on.<br>**Repair:** write the resolved scope into the package, or state in the index where it lives.<br>**Evidence still needed:** none. | `find docs/dossier -name '.*'` → no output; `ls -la docs/dossier/00-control/` → five `.md` files only |
| **C17 · D10 · `07-verification/documentation-verification-report.md`, `00-control/claim-and-disclosure-register.md`, 6 others**<br>Claim: `— (omission)` | `dossier-prose-lint.sh`, run independently by this pass over all 23 files, reports hard-category violations in 8 of them: 19 in the verification report (9 long sentences, 8 long paragraphs, 2 semicolons), 11 in the register, 8 in the assumptions document, then 5, 4, 4, 2, 1, 1, 1. G18 requires none in the package. Both public guides carry hits (5 and 4). None coincided with a comprehension failure in the reader simulations — the prose is readable throughout — so this earns Low, recorded here as an independently executed measurement rather than as a style judgment.<br>**Repair:** clear the hard-category hits, starting with the verification report's eight long paragraphs.<br>**Evidence still needed:** none. | `bash plugins/dossier/bin/dossier-prose-lint.sh --file <path> --json`, all 23 files, executed 2026-09-10 |

## Reader simulations

| Reader | Entry point obvious? | Hops to critical info | Task completed? | Blocking gap |
|---|---|---|---|---|
| New engineer | yes — `README.md` → index → `04-operating/onboarding-and-local-development.md` | 3 | yes, on the second attempt | Followed end to end from a clean clone. Steps 1, 2, 4 and 5 reproduce exactly as documented, including the no-submodule claims and step 5's exact output — this is the strongest part of the package. Step 3's stated success criterion is conditional on checkout path in a way the document does not say, and I hit the failing side of it (C7). The prerequisite remedy for PyYAML is not executable as written (C15). Three cells read "Ledger row requested", so a material claim carries no locator |
| Diligence lead | yes — index → `05-due-diligence/technical-due-diligence-report.md` → `07-verification/…` | 3 | partly | Navigation is clean and the ranked verdict is genuinely decision-shaped. The route terminates at a verification report pinned to a commit unreachable from any ref, describing 56 evidence rows and a 17-condition gate against a live 191-row package and a 19-condition gate (C9). Whether the report's conclusions are supported is pass A's question, not mine |
| New product manager | yes — index reader route to `01-project/*` | 2 | yes | Implemented, planned and absent are visibly distinct: `Recommendation:` prefixes proposals, `Unknown:` and `Inferred:` mark states, `N/A` carries reasons. No defect found |
| On-call operator | yes — index → `04-operating/operations-and-incident-response.md` | 2 | yes, the correct answer being that there is nothing to respond to | Honest and useful: `N/A` rows carry reasons, the deploy row states its blast radius and that no precondition is enforced, escalation cells are empty with the reason rather than an invented owner. Its documented false-positive class fired against this audit's own tooling, which corroborates it. The gap it names — nine failure modes, one exercised runbook, no incident channel — is a project fact, not a documentation defect |
| Security or privacy reviewer | yes — index → `03-assurance/security-privacy-and-compliance.md` → `02-architecture/system-architecture.md` | 3 | partly | The install → hook registration → execution flow traces end to end and the trust boundary is stated. It stops at what is not scanned: the reviewer must reach `CL-0047` in the register to learn that no dependency-vulnerability scan exists and that the shell scripts lie outside every configured CodeQL language, and that reaches no public document (C10) |
| Technical partner | yes — index → `06-public/technical-partner-guide.md` | 1 | **no** | Auth, errors, rate limits, versioning and sandbox path are all present and unusually candid — and almost entirely unregistered (C2). Retry guidance is wrong for the operations the plugins perform (C11), the prerequisite list omits a runtime dependency (C6), and the version claimed lacks a capability asserted (C3) |
| Customer | yes — index → `06-public/customer-product-and-trust-guide.md` | 1 | **no** | Limitations are present, specific and unflattering, which is the right shape. But macOS is listed as supported while the package knows the current release ships a macOS defect (C4), the data section overstates to "nothing is held anywhere" (C5), and the security enumeration reads as complete while a known gap is withheld (C10) |

## Disclosure result

UNSUPPORTED_PUBLIC_SENTENCES=61  LEAKAGE_HITS=0  SIMPLIFICATION_DEFECTS=4

`UNSUPPORTED_PUBLIC_SENTENCES` is a floor: 60 declarative table rows plus one prose sentence in sections with no mapped register row, excluding mapped sections where rows outnumber claims (C2). `LEAKAGE_HITS=0`: `dossier-claim-scan.sh` reports none, and four contract-prohibited classes I found by reading — repository path, manifest file name, branch name, internal `bin/` path — are recorded as C12 rather than counted here, because in substance they disclose nothing an installer cannot read in the public repository. `SIMPLIFICATION_DEFECTS` are C3 (version scope lost), C4 (platform condition lost), C5 (storage scope lost), C11 (idempotency scope lost). `CL-0038` is explicitly **not** among them: the partner guide carries its environment qualification verbatim.

**Judgment on the two withheld claims, as asked.** They do not behave the same way. `CL-0047` (no dependency-vulnerability scan) is reported above as C10: the guides enumerate security posture in a table whose candour teaches the reader it is exhaustive, so the silence is read as absence of the problem. `CL-0043` (maintainer concentration) is **not** a finding. The guides never state or imply a team: *Commitments* is a single row reading `none`, *Response expectations* and *Escalation path* read "None stated" and "None defined", *Supported versions* reads "The current one. No prior version is maintained", *Security fix* notice reads "none — a commit", and the customer guide's role table gives the project exactly one capability. A reader assembling those rows arrives at thin maintenance capacity without being told the headcount, which is the correct belief. The withholding costs specificity, not accuracy.

## Checks

| Check | Result | Reason if not executed |
|---|---|---|
| `dossier-package-check.sh --output-root docs/dossier` | `FILES_PRESENT=23 HEADERS_OK=23 POINTERS_CHECKED=23 DIAGRAMS_PRESENT=6 FINDINGS=0 RESULT=CLEAN`. Note `LINKS_CHECKED=0` — the script checked no links | — |
| `dossier-claim-scan.sh --output-root docs/dossier` | `LEAKS=0 PROHIBITED_VOCABULARY=0 UNREGISTERED_SENTENCES=0`, exit 0. Blind to table content by its own documentation — C2 | — |
| `dossier-prose-lint.sh --file … --json`, all 23 files | 8 files with hard-category hits — C17 | — |
| `related:` entry resolution, all 23 files | 0 dangling (run independently, since the package check reports 0 links checked) | — |
| Package-relative document paths named in prose | 0 dangling | — |
| Header fields, enums, date formats, all 23 files | All present and valid. `public-v1` files carry exactly the five permitted keys, no internal keys, titles match their H1 | — |
| `project-version` pins resolve | `7ee4923` yes, `691bcdb` yes, `06b1586` **no** locally, present via the GitHub API — C9 | — |
| Public claims re-derived at the stated product version | `v4.10.0` = `c0fb802`: 8 manifest entries ✓, flow 32/23/9/14 ✓, 63 tags ✓, CI version check **absent** ✗ — C3 | — |
| `last-verified` vs cited-evidence vintage, all 22 citing documents | 16–55% of each document's cited rows never re-observed since July; 11 of 20 carry no vintage caveat — C14 | — |
| Index canonical table vs headers, 23 rows × 5 columns | 75 of 115 cells disagree — C1 | — |
| Register arithmetic | 48 `CL-####` rows + 4 rejected; the register's own summary agrees; the index's "35 approved / 56 evidence rows" does not — C1 | — |
| Day-one route, steps 1–5, from a clean public clone | Clone `0e79776` ✓, manifest `8` ✓, flow suite `2335 fail=1` exit 1 ✗ (twice, cause isolated to checkout path — C7), dossier suite `1951 fail=0` ✓, `check-plugin-versions.sh` → `8 plugin(s) checked, 0 failed, 0 unverifiable` exit 0 ✓ | — |
| Working-tree flow suite, for comparison | `TOTAL pass=2336 fail=0` at `c9b2a24` | — |
| End-to-end plugin install from the marketplace | not executed | Requires a Claude Code client profile and a network install, outside this pass's read-only scope |

## Scorecard — round 4, pass C

| # | Dimension | Weight | Score | Weighted | Min required | Meets min | Justification (finding IDs) |
|---:|---|---:|---:|---:|---:|---|---|
| 1 | Evidence grounding and freshness | 18 | 6 | 10.8 | 14.4 | no | C1 (index body stale under a current stamp), C14 (16–55% of each document's cited evidence never re-observed, 11 of 20 without a caveat), C8 (a July row producing a wrong current figure), C9 (report pinned to an unreachable commit) |
| 2 | Coverage and completeness | 12 | 8 | 9.6 | 9.6 | yes | C16 (no resolved scope recorded). All 23 canonical files and all 6 diagrams present |
| 3 | Technical correctness | 15 | 8 | 12.0 | 12.0 | yes | C11 (idempotency claim wrong for the operations described). Every headline count I re-derived independently — 8 manifest entries, flow 32/23/9/14, 63 tags, 21 decision records, 19 hook scripts, the version-check output, the EV-0144 diff — matched the package |
| 4 | Cross-document consistency | 10 | 4 | 4.0 | 8.0 | no | C1 (75 of 115 index cells contradict the headers the reference names authoritative), C8 (11 vs 21 in two documents), C12, C13 |
| 5 | Due-diligence decision value | 10 | 7 | 7.0 | 8.0 | no | C9 — the package's own verification report describes a different package at a commit no clone can reach, and it is where both the index and the due-diligence report route the reader |
| 6 | Onboarding and operability | 10 | 7 | 7.0 | 8.0 | no | C7 (the first-day success criterion is conditional on checkout path and stated unconditionally, with no troubleshooting row), C15 (the only remedy for a required prerequisite is not executable as written). Measured setup time, per-step environments and dates, and an executed starter task are real strengths and hold the rest of the dimension up |
| 7 | Security, privacy, and disclosure safety | 10 | 5 | 5.0 | 8.0 | no | C3, C4, C5, C6 — four public statements that lose a condition present in the internal source, three of them about what executes on or is required by a reader's own machine. C10, C12 |
| 8 | Reliability and verification depth | 5 | 5 | 2.5 | 4.0 | no | C2 (the zero-unregistered result rests on a scanner that cannot see tables, and the guides are tables), C7 (a recorded suite result whose environment sensitivity no check caught). The register discloses the scanner's limitation itself, which is why this is not lower |
| 9 | Public usefulness and claim integrity | 5 | 3 | 1.5 | 4.0 | no | C2 (the guides' opening trust sentence is false as written), C3, C4, C5, C11. The guides are candid and well-written, which raises rather than lowers the cost of the unregistered content |
| 10 | Clarity and maintainability | 5 | 7 | 3.5 | 4.0 | no | C17 (hard-category prose hits in 8 files, 19 in one), C14 (the vintage convention is applied in some documents and not others) |

TOTAL=62.9/100  MIN_DIMENSION_MET=no

Two unresolved Critical and six unresolved High findings. On this pass's evidence alone G02, G03, G04, G07, G09, G10, G13 and G18 do not pass. This pass does not issue a gate verdict.

---

## Round 4 — reconciled findings (pre-repair)

Published before any repair, and never rewritten. A package that quietly fixes what it found and reports only the clean end state has destroyed its own audit trail.

Raw: A 20, B 15, C 17 — 52. After dedupe: 31. Deduplication merged descriptions rather than discarding the second pass's reasoning; where two passes reached the same conclusion by different routes, both routes are recorded in the Problem column.

Corroboration is stamped `N/3` and **orders repair work only**. A 1/3 finding is not downgraded for being lonely: the lenses differ by design, and pass C is the only one that ran the onboarding route on a clean clone, so a defect only it could see is correctly 1/3 and no less real.

| ID | Sev | N/3 | Location | Problem | Class | Status |
|---|---|---|---|---|---|---|
| R4-01 | Critical | 3/3 | `00-control/documentation-index.md` | The package entry point was re-stamped `project-version: 7ee4923` / `last-verified: 2026-09-10` / `status: verified` while its body was never re-derived. The stamping commit `c9b2a24` changes four lines of the file, two of them the stamp. Body reports package 1.0.4 at `06b1586` on a deleted branch, evidence cutoff 2026-07-26, 56 evidence rows against 191, 35 approved claims against 33, and a 17-condition gate against 19. Pass C compared the canonical table cell by cell against the real headers: 75 of 115 cells disagree — 22 `last-verified`, 18 `status`, 17 `audience`, 16 `confidentiality`, 2 `owner`. Every reader route begins here | Agent-repairable | Open |
| R4-02 | Critical | 3/3 | `06-public/**` | Claim-bearing sentences in both guides map to no register row. Pass B named five security sentences in bullets, verbatim, including "There are no binaries and no bundles; you can read every hook before you install it." Pass C counted 61 declarative rows across the sections the register's mapping table does not cover — prerequisites, auth, retries, idempotency, rate limits, timeouts, exit codes, troubleshooting, AI features, support. This falsifies CL-0031, which is printed twice in each guide and asserts the opposite | Needs-owner | Open |
| R4-03 | Critical | 2/3 | `plugins/dossier/bin/dossier-claim-scan.sh:243` | The control that should detect R4-02 structurally cannot. The sentence selector skips empty lines, headings, table rows, `---`, comments, `- ` bullets, `* ` bullets and blockquotes before any registration test. The guides are largely tables and bullets. It reports `UNREGISTERED_SENTENCES=0`. **The canary run recorded earlier in this round as falsifying that zero appended a paragraph**, so it exercised the only line class never at risk and proved nothing about the finding | Agent-repairable | Open |
| R4-04 | Critical | 3/3 | `07-verification/documentation-verification-report.md` | Not refreshed before this round. Header pinned `06b1586`, `2026-07-26`, `status: verified`; body reports "Two of seventeen conditions fail" and "All 35 claims approved" against a 19-condition gate and 33 approved claims. Pass C adds that `06b1586` is unresolvable by `git cat-file` in any clone — it is a squashed branch commit the GitHub API still serves. No AQ or CT row recorded that the file was left behind | Agent-repairable | Open |
| R4-05 | Critical | 1/3 | `00-control/evidence-ledger.md` EV-0035, and 20 consuming passages | "All commits across all refs carry one author identity … 370 commits" is state `V`, `Public use: yes`, and false. Measured at this HEAD: 480 commits across all refs, two identities — Daniel Bentes 458, Claude 22. EV-0163 was added earlier in this refresh recording the correct figure for `main` only (214, one identity); the wrong row was never demoted. It still drives a due-diligence fact, a red flag, a rank-4 risk, and the licensing document's inbound-rights position | Agent-repairable | Open |
| R4-06 | Critical | 1/3 | `06-public/**` headers; CL-0046 | Both guides are headed `product-version: 4.10.0` and both assert that every marketplace entry's advertised version is checked in CI. Neither `scripts/check-plugin-versions.sh` nor `.github/workflows/marketplace-manifest.yml` exists in the `v4.10.0` tree — confirmed by `git ls-tree -r --name-only v4.10.0`. CL-0046 was approved on 2026-09-10 against `7ee4923`, where the check does exist. The claim is true of the branch and false of the product version the guides advertise | Needs-owner | Open |
| R4-07 | High | 1/3 | `security-privacy-and-compliance.md:142`; both enforcement hooks | The action ceiling is inert unless `<outputRoot>/00-control/.scope.json` exists. `enforce-allowed-actions.sh:53` and `enforce-output-root.sh:39` both `exit 0` when it is absent. The file is gitignored and does not exist here, so **neither control enforced anything during this engagement**. The absence appears in the ledger as a run-record fact (EV-0091, EV-0124) but never reaches the control inventory, the threat table, or the risk register | Agent-repairable | Open |
| R4-08 | High | 1/3 | AQ-0012; ledger notes on EV-0126, EV-0127, EV-0131; `system-architecture.md:25,34` | The package says read-only GitHub reads were "observed outside the action ceiling" because dispatched collectors could not perform them, `networkAccess` being false. `enforce-allowed-actions.sh:441-454` denies network clients, `git push/remote add/clone`, and mutating `gh` verbs, and comments "Read-only gh and git commands are permitted". Pass B executed it: `gh api` → 0, `curl` → 2. The premise of AQ-0012 is false and three provenance qualifiers misdescribe the control | Agent-repairable | Open |
| R4-09 | High | 1/3 | `assets-dependencies-and-licenses.md`; `technical-due-diligence-report.md`; ranked risk 6 | Both state that no `shellcheck` runs in CI and that the operator-facing shell surface has no automated security analysis. `.github/workflows/dossier-tests.yml:134-138` installs shellcheck v0.11.0 and runs it at `-S warning` over dossier's bin, hooks and tests. The package's own EV-0165 records this; the correction never reached the two diligence documents | Agent-repairable | Open |
| R4-10 | High | 1/3 | `06-public/**`; CL-0041 | "Both externally sourced plugins are pinned to a commit sha, so two installs performed on different days resolve the same tree" is published as fact. Both manifest entries carry `"ref": "main"` **and** `"sha"`, and no evidence row observes the client resolving an install to determine which wins. The assets document states the same sentence marked `Inferred:`. An inferred claim published as fact, and the sole basis for clearing the floating-ref red flag | Needs-owner | Open |
| R4-11 | High | 2/3 | `06-public/**` | Both guides list macOS as supported and neither limitation table mentions that v4.9.0 and v4.10.0 both ship the pre-fix flow macOS scripts. The package records this internally at EV-0144; pass C corroborated it independently at +39 −1 between the tag and HEAD | Needs-owner | Open |
| R4-12 | High | 3/3 | `06-public/**` prerequisites | Both public prerequisite lists omit `python3` with PyYAML. `onboarding-and-local-development.md:42` marks it **Required** for flow's journal, FlowRun and FlowGoal machinery; no manifest declares it, so an operator has no other way to learn it. Its absence silently opens the FlowGoal gate (R4-13) | Needs-owner | Open |
| R4-13 | High | 2/3 | `security-privacy-and-compliance.md:338` | The package treats hook enforcement as one posture. Two opposite postures exist. Fail-closed: flow's three blocking hooks and dossier's three exit 2 with BLOCKED when `jq` is missing. Fail-open and silent: `verify-task-completion.sh:43` and `session-end-state.sh:47-48` exit 0, and `flow-goal-evaluator.sh:41` emits `{"decision":"approve"}` with no warning at all. The inventory records neither the split nor any silent-disable condition | Agent-repairable | Open |
| R4-14 | High | 1/3 | `technical-due-diligence-report.md` | The report names three different assessed trees: `7ee4923` in the header, `691bcdb` in the scope row, `af6e632` in the headline facts | Agent-repairable | Open |
| R4-15 | High | 1/3 | `technical-due-diligence-report.md` | Calls merge gating "not re-checked, `networkAccess` is false" and lists branch protection as unknown, while EV-0131 records a live 2026-09-10 re-check. Pass A reproduced EV-0131 independently by `gh api` | Agent-repairable | Open |
| R4-16 | High | 1/3 | package-wide | The repository root `tests/` directory — 29 tracked files, 10 executable checks — appears nowhere in the package, which describes the tree's verification as exactly two plugin suites | Agent-repairable | Open |
| R4-17 | High | 1/3 | `00-control/assumptions-questions-and-contradictions.md:53,82,110` | CT-0019 states "Nine internal document headers carry `project-version: 06b1586`". One does. AQ-0027 and remediation item 4 restate the nine-document scope, and the affected-documents column lists eleven paths that no longer carry the pin. The companion 38-row ledger figure in the same row is correct | Agent-repairable | Open |
| R4-18 | Medium | 1/3 | CT-0015 | The row that exists to correct stale evidence counts carries one: "the ledger holds 174 `EV-` rows". It holds 191 | Agent-repairable | Open |
| R4-19 | Medium | 2/3 | `00-control/documentation-index.md` evidence-coverage table | "Every row defined is cited" is false: 29 rows are cited nowhere outside the ledger. Per-document counts are wrong in every row checked — brief 24 against 49, security 27 against 76, due-diligence 28 against 51, onboarding 17 against 35 | Agent-repairable | Open |
| R4-20 | Medium | 2/3 | CL-0033, printed at `customer-product-and-trust-guide.md:11` | A census of all 33 approved claims against the ledger's state column finds 32 citing only `V` or `C`, and CL-0033 citing no evidence row at all. It is published verbatim, in the same file as CL-0031's assertion that every statement is backed by a verified evidence row | Needs-owner | Open |
| R4-21 | Medium | 2/3 | `06-public/**` hook-readability bullet | "There are no binaries and no bundles" is half false. Pass B downloaded a release asset and confirmed it holds plain text only — but every release from v4.7.0 carries 71 `.zip` bundles from `release-desktop-skills.yml`, and the partner guide lists "Release assets — Skill packages for Claude Desktop" eleven lines above the denial | Needs-owner | Open |
| R4-22 | Medium | 2/3 | `customer-product-and-trust-guide.md` Key journeys | "The plugin stops loading. Nothing of yours was held anywhere." The plugins write durable state into the operator's own repository and uninstalling removes none of it: `.flow/`, `.decisions/`, `docs/dossier/**`, `.dossier/**`. This is the package's only deletion statement | Needs-owner | Open |
| R4-23 | Medium | 1/3 | `06-public/**`; `interfaces-and-integrations.md` | "Timeouts — None" published, and "no timeouts in this project's own code" internally. Falsified by `dossier-scan-security.sh:113` and `dossier-scan-quality.sh:90` (300s default, TERM then KILL) and the evaluator hook's `timeout`/`gtimeout` path | Agent-repairable | Open |
| R4-24 | Medium | 1/3 | EV-0044 | The refutation of EV-0041 is incomplete. EV-0044 is state `V`, `Public`, derives from the refuted row, and its own claim — "Markdown, JSON, and shell" — is falsified by 53 tracked `.py` files. It supports approved public claim CL-0007 and is cited for two claims it does not entail | Agent-repairable | Open |
| R4-25 | Medium | 1/3 | CL-0016, EV-0037 | The public credential-scan claim rests on a row observed 2026-07-26 at `06b1586` with freshness basis "every commit", not listed as stale, and the public sentence drops the date every internal citation carries | Needs-owner | Open |
| R4-26 | Medium | 2/3 | `data-and-ai.md` | States 11 decision records, twice. Operations, the terminology register and the tree all say 21 | Agent-repairable | Open |
| R4-27 | Medium | 1/3 | `06-public/**`; `data-and-ai.md` | "Nowhere. The project has no store of any kind" and "Nothing of yours was held anywhere" against `data-and-ai.md`'s "files on the operator's machine" and 21 tracked `.decisions/` records | Needs-owner | Open |
| R4-28 | Medium | 1/3 | `assets-dependencies-and-licenses.md` inventory | 57 tags, v4.6.2 and 7 plugin manifests recorded against 63, v4.10.0 and 6 measured live | Agent-repairable | Open |
| R4-29 | Medium | 1/3 | `04-operating/decisions-technical-debt-and-risks.md` config-drift row | Asserts `expectedPluginVersion` is `"1.0.0"`. `jq` reads `1.2.0` at the stamped tree — the drift was fixed in `d5a5166` earlier in this refresh and the row was never updated | Agent-repairable | Open |
| R4-30 | Medium | 1/3 | `04-operating/onboarding-and-local-development.md` | The PyYAML remedy is not executable as written on the platform the document names. `pip` is not on `PATH`; `python3 -m pip install --user pyyaml` is refused under PEP 668 by Homebrew's interpreter and accepted by Apple's `/usr/bin/python3`, which the document never names though it names `/bin/bash` 3.2.57 in five places | Agent-repairable | Open |
| R4-31 | Medium | 1/3 | `plugins/flow/tests/` | The flow suite returns `pass=2335 fail=1` on a checkout whose absolute path exceeds ~200 characters: a test asserts on a cell rendered by `cell(transcript_path, 200)` and the fixture path is truncated. The published 2336 is honest for short paths and pass C reproduced it — but the criterion is stated unconditionally with no troubleshooting row, so a contributor on a deep path gets a red suite and no route out | Agent-repairable | Open |

### Cross-pass disagreements promoted

None. Phase 3 promotes incompatible conclusions about the same claim to Critical in their own right; the three passes disagreed nowhere. Two apparent conflicts resolved to compatible scopes on evidence rather than on vote:

- Pass A measured 479 commits across all refs; the figure recorded earlier in this refresh was 214 on `main`. Both are correct and measure different ref sets. This is R4-05's substance, not a disagreement — the defect is that EV-0035 asserts the all-refs figure wrongly and was never demoted.
- Pass A recorded `CHECK_LINKS_CHECKED=0` as a hole in the structural check; pass C reported that links and `related:` entries resolve. Compatible: pass C resolved them with its own resolver, which is what makes pass A's point — the mechanical check verified nothing there.

### Premise corrections to this round's own dispatch

Recorded because the audit prompts carried them and a later reader should not inherit them:

- The dispatch prompts stated that seventeen internal headers carry `last-verified: 2026-09-10`. Twenty do. Seventeen is the number this run's re-stamp script changed; four were already stamped by earlier commits in the refresh, and one was reverted. Pass C caught the difference.
- `/dossier:audit` requires all three passes dispatched in one message. Pass A was dispatched alone, then B and C together. No pass output had entered the orchestrator's context when B and C were spawned, so the isolation the rule protects held in effect — but the rule as written was not followed.

