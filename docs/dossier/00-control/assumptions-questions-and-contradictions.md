---
dossier-header: internal-v1
title: Assumptions, Open Questions, and Contradictions
purpose: Tells a reader which parts of this package rest on something unresolved, so they can weight the rest accordingly.
audience: Reviewer, Maintainer
confidentiality: Public
owner: Daniel Bentes
status: partially verified
project-version: 7ee4923
last-verified: 2026-09-10
review-trigger: Any new evidence that settles an open row, or any new source disagreement discovered during refresh
related: [00-control/evidence-ledger.md, 00-control/claim-and-disclosure-register.md, 04-operating/decisions-technical-debt-and-risks.md]
---
# Assumptions, Open Questions, and Contradictions
<!-- contract: references/package-contract-00-control.md#assumptions-questions-and-contradictions -->

## Method

Two registers. `AQ-####` holds assumptions the package relies on and questions that must be answered; `CT-####` holds disagreements between sources. Column definitions and identifier grammar are in the plugin reference `references/register-schemas.md`.

A contradiction is never resolved by choosing the convenient side. Both sides are recorded with their evidence and authority level, the decision impact is named, and the row stays `unresolved` until evidence settles it or scoping makes both true. While a contradiction is open, both underlying evidence rows drop to at most `R`.

## Assumptions and open questions

| ID | Kind | Statement | Why it matters | Working position | Evidence needed | Proposed owner | Blocking | Affected docs | Due | Status | Resolution |
|---|---|---|---|---|---|---|---|---|---|---|---|
| AQ-0001 | question | The `agent-capability-standard` Python test suite passes at the pinned commit | It is the only plugin with executable logic beyond shell, and the only one carrying a dependency | Unknown. The suite exists; its result is not observed | `pytest` run in an environment with `pyyaml` installed | Daniel Bentes | no | 03-assurance/testing-quality-and-delivery.md, 05-due-diligence | — | open | — |
| AQ-0002 | question | The dossier post-merge refresh workflow works end to end in a live repository | The plugin's headline capability. Its three-job privilege split, patch validation, and rolling-branch publishing are unit-tested but never executed together | Unverified. Structure is tested (EV-0009); behaviour is not | One scratch repository, one API-key secret, one merged pull request, one observed docs pull request | Daniel Bentes | **yes** | 03-assurance, 04-operating, 05-due-diligence, 06-public | before dossier is described as working in any public document | open | — |
| AQ-0003 | question | The marketplace installs cleanly from a **clean** Claude Code profile | Every claim about how an operator gets the plugins rests on it | Resolution and install are observed working on one machine [EV-0051], [EV-0052]; a first-time install on an unconfigured profile is still unobserved | An install performed from a profile with no marketplace configured, ideally in CI | Daniel Bentes | no | 04-operating/onboarding-and-local-development.md, 06-public | — | partially resolved | The `add` → resolve → install → cache path is verified for an existing profile; the cold path is not |
| AQ-0004 | assumption | The marketplace has installing users other than its maintainer | Determines whether stale README facts are a real reader-harm or a private inconvenience | Assume yes and treat reader-facing accuracy as material. The repository is public and releases are published | Install telemetry, which does not exist for plugin marketplaces | — | no | 01-project, 06-public | — | open | Unresolvable from this repository; recorded so no document silently assumes a user base |
| AQ-0005 | question | The `prompt-decorators` entry's description matches its actual contents | It is published to installers from this manifest, but its source is not in this repository | Reported only. Every statement about it in this package is attributed to the marketplace entry | Inspection of `synaptiai/prompt-decorators` at the `claude-code-plugin` subdirectory | Daniel Bentes | no | 02-architecture, 05-due-diligence | — | open | — |
| AQ-0006 | question | What the 2 commits between tag `v1.2.0` and the pinned submodule commit change | The marketplace advertises `agent-capability-standard` as 1.2.0 while shipping a tree that is not 1.2.0 | Treat the advertised version as approximate | `git log v1.2.0..9e2f65b` in the upstream repository — the pointer moved when the submodule became a pinned `github` source [EV-0058] | Daniel Bentes | no | 02-architecture, 05-due-diligence | — | open | — |
| AQ-0007 | question | Whether the contribution path works for anyone but the maintainer | A public repository with 8 published plugins and no contribution guidance, no CODEOWNERS, and no external contributor to date | Assume untested | One external pull request reviewed and merged, or a documented contribution process | Daniel Bentes | no | 04-operating, 05-due-diligence | — | open | — |
| AQ-0008 | question | The real Windows and Git Bash portability surface of the 26 shipped shell scripts | The scripts run on the operator's machine. Two open issues (#100, #130) assert defects, and neither CI workflow runs on Windows | Assume defects exist as reported; the scope is unmeasured | A CI matrix job on `windows-latest` running both suites | Daniel Bentes | no | 03-assurance, 04-operating, 06-public | — | open | The figure of 26 is the count observed 2026-07-26 [EV-0007]. At `d3fc744` the tracked tree holds 37 shell scripts under plugin `bin/` plus 19 hook scripts [EV-0162], so the surface this row leaves unmeasured is larger than its statement implies — see CT-0016 for the shell-versus-all-files counting mismatch |
| AQ-0009 | assumption | Skill and command counts derived by file-counting are the counts an operator experiences | Every artifact-count claim in this package uses `find … -name SKILL.md` | File-counting is authoritative; directory-counting is not, because an empty directory is not a skill (EV-0029) | — | Daniel Bentes | no | 02-architecture, 06-public | — | closed | Confirmed: `plugins/flow/skills/learned/` holds only `.gitkeep`, which is exactly the 33-vs-32 discrepancy in CT-0002 |
| AQ-0010 | assumption | Assessment on an unmerged feature branch is representative of the project | This package was produced at `06b1586` on `fix/gate-verdict-integrity`, not on `main` | State it in every header via `project-version`, and treat `main`-only claims as out of scope | Re-run after merge | Daniel Bentes | no | all | — | open | Version 4.7.0 and the entire dossier plugin exist only on this branch (EV-0033) |
| AQ-0011 | question | GitHub's repository-level licence detection reports Apache-2.0 | The badge a visitor sees, and the field every automated licence scanner reads | Unobservable from here. The `/license` endpoint returns 404 for any non-default ref, and repository-level detection is computed from the default branch, where `LICENSE` does not yet exist | One `gh api repos/synaptiai/synapti-marketplace --jq .license` after this branch merges | Daniel Bentes | no | 05-due-diligence, 06-public | on merge | closed | Resolved 2026-09-10: GitHub reports `Apache-2.0` for the repository [EV-0132] |
| AQ-0012 | question | Whether this package's evidence may rest on GitHub reads the engagement's own action ceiling forbids | `dossier.engagement.allowedActions.networkAccess` is false (CHK-64), so every dispatched collector recorded CI results and the marketplace version check as unknown. The session operator then ran both directly, and both came back clean (EV-0126, EV-0127) | Record the rows, and name in each one that it was observed outside the ceiling. The alternative — carrying a package that says "Linux CI unverified" when one command answers it — is worse for a reader | A decision on whether to raise `networkAccess` for read-only GitHub queries, or to keep the ceiling and accept operator-observed rows as a distinct class | Daniel Bentes | no | 00-control, 03-assurance | before the next refresh | open | — |
| AQ-0013 | question | Whether `dossier-evidence.sh` should drop, rather than emit, a placeholder pull-request record when the number it scraped from a commit subject is not a pull request | It reads every `#N` in a commit subject as a pull-request number. Four issue references produced four empty records in this run's bundle (EV-0128). A drafter reading the bundle sees what looks like a pull request with no title, body, author or state | The four records are false rows, not lost evidence — all 15 real pull requests came back through the API. Treat `pull-requests.json` entries with an empty `title` as absent | A decision on the fix: drop the record, or keep it and mark it `unresolved` so the miss stays visible | Daniel Bentes | no | 00-control, 03-assurance | before the dossier plugin's next release | open | — |
| AQ-0014 | question | Whether the package's project model should be an artifact or stay implicit in `terminology-and-ownership.md` | The dossier `project-modeling` skill mandates `00-control/.project-model.json` and `dossier-doc-drafter` names it as a required input, but the file has never existed here (EV-0130) and no script checks for it. Falsification pass B is told to execute the traces the model records; the package carries one trace against the skill's required three | Treat `terminology-and-ownership.md` as the model of record and say so, rather than citing an artifact that is not there | A decision on whether to generate the artifact or amend the skill contract to name the terminology register | Daniel Bentes | no | 00-control, 03-assurance, 07-verification | before the dossier plugin's next release | open | — |
| AQ-0015 | question | Whether the dossier blast-radius trigger map can route correctly for a repository whose entire source tree is `plugins/` | `bin/dossier-blast-radius.sh` reaches `00-control/terminology-and-ownership.md` only through the `interface`, `decision` and `product-capability` events, and `product-capability` matches the prefixes `src`, `lib`, `app`, `packages`, `services` and similar — none of which exist here. The terminology register therefore never entered the affected set for this refresh, and its entries had drifted: a plugin recorded as a git submodule that is now a pinned `github` source, a floating ref that is now sha-pinned, 4 workflows against 5, and two wrong hook-script counts (EV-0129) | The register was corrected by hand this run and the correction is recorded. The map is unchanged | A decision on whether the trigger map should carry a repository-shape override, or whether `00-control/terminology-and-ownership.md` belongs in the `always` class | Daniel Bentes | no | 00-control, 03-assurance, 07-verification | before the dossier plugin's next release | open | — |
| AQ-0016 | question | How the flow 3.3.0 goal-evaluation loop works as a model-facing subsystem, and what `.flow/` holds in a repository that installs flow rather than authors it | It is the newest model-facing surface in the marketplace and the `ai-model` blast-radius event points at it. The artifacts are inventoried — six goal-lifecycle skills and one judge agent [EV-0136], and this repository's own `.flow/goals` and `.flow/runs` [EV-0137] — but no row describes the loop's contract, its deterministic check report, or what a consuming repository receives | Every statement about the loop's behaviour is written `Unknown:`; only the artifact inventory is asserted | A read of `goal-evaluator`, `goal-contract-capture` and `agents/goal-evaluator-judge.md`, plus one flow run observed in a repository that installs the plugin | Daniel Bentes | no | 02-architecture | before the next refresh | open | — |
| AQ-0017 | question | What each of the five security fixes merged into the dossier plugin in this range changed, and what each hardened control now rejects | The package could name the controls but not their post-fix behaviour, and describing a control from a commit subject is the failure this method exists to prevent | — | A read of each fixed script at HEAD with the shipped assertion that covers it | Daniel Bentes | no | 03-assurance, 04-operating | — | closed | Resolved 2026-09-10 by EV-0145 to EV-0161, each grounded in a test that was executed [CHK-85]–[CHK-90]. Two commit bodies were found to describe controls that do not ship, because a later bullet in the same squash superseded an earlier one [EV-0148], [EV-0150]. Two claims remain `R`: GitHub's acceptance of the Basic header form on a private repository [EV-0159], and every "the pre-fix script executed the denied command" claim [EV-0161] |
| AQ-0018 | question | Which languages CodeQL analyses at HEAD, and whether the shell the repository actually ships is covered | Static analysis coverage bounds what a green CodeQL run can be taken to mean | — | A read of the workflow's language matrix and the alert count | Daniel Bentes | no | 03-assurance | — | closed | Resolved 2026-09-10: the matrix is `actions` and `python` with `build-mode: none` [EV-0134], all 53 tracked `.py` files sit under `plugins/`, and three analyses across the range each report 0 results with 0 open alerts [EV-0140]. 37 shell scripts are outside every configured language; the other 3 files under plugin `bin/` are Python in flow, which the matrix does configure [EV-0175], [EV-0176] — this row originally read 40 shell scripts and is corrected here, see CT-0020. What covers the shell is shellcheck in one workflow, over one plugin [EV-0165] — see AQ-0019 |
| AQ-0019 | question | Whether shellcheck should cover flow's scripts as well as dossier's | `dossier-tests.yml` lints dossier's 20 `bin/` and 5 hook scripts; nothing lints flow's 20 `bin/` and 14 hook scripts [EV-0165], [EV-0162]. Flow's hooks run as blocking `PreToolUse` calls on a contributor's machine, and flow's portability defects are exactly what 16b4dc4 fixed [EV-0144] | State the coverage as dossier-only rather than asserting that nothing lints the shell | One CI run of `shellcheck -S warning` over `plugins/flow/bin/*.sh` and `plugins/flow/hooks/scripts/*.sh` | Daniel Bentes | no | 03-assurance, 04-operating | before the next flow release | open | — |
| AQ-0020 | question | Whether a malformed or unresolvable `marketplace.json` entry would be caught before publication | `scripts/check-plugin-versions.sh` compares advertised versions and requires a `sha` on external sources [EV-0080]; nothing asserts that the file parses and that every source resolves. The manifest is the highest-blast-radius file in the repository — a malformed one breaks discovery for all eight entries at once | Assume it is uncaught | The check run against a deliberately broken fixture | Daniel Bentes | no | 02-architecture, 03-assurance | before the next release | open | — |
| AQ-0021 | question | Whether `scripts/package-desktop-skills.sh` produces byte-identical archives across two runs of the same tree | The build table's reproducibility column has carried `unknown` since the July baseline and nothing has tested it. `release-desktop-skills.yml` uploads with `--clobber` and records no producing commit [EV-0133], so an asset on a release cannot be traced to a tree from the asset alone | State unknown rather than yes | Run the script twice into separate directories and compare checksums | Daniel Bentes | no | 02-architecture, 05-due-diligence | before the next release | open | — |
| AQ-0022 | question | Whether the July-observed security controls that no 2026-09-10 row re-checks still hold | Branch protection, rulesets and repository secrets were re-read live and had not changed [EV-0131], and CodeQL's scope and findings are now observed [EV-0134], [EV-0140]. What remains unrefreshed from 2026-07-26 is the tracked-file secret sweep and the workflow-permission scan for the four workflows other than codeql | Carry those rows forward with their observation date written into the sentence | One re-run of the secret-detector sweep and the workflow-permission scan at HEAD | Daniel Bentes | no | 03-assurance | before the next refresh | open | — |
| AQ-0023 | question | Whether these plugins function under any agent client other than Claude Code | Vendor dependency is total: the manifest, the skills, the commands, the agents and the hooks are all Claude Code constructs, and no row establishes that any artifact is portable. For a diligence reader the exit path is the first question | Treat lock-in as total and the exit path as unestablished | One artifact loaded and executed under a second client | Daniel Bentes | no | 02-architecture, 05-due-diligence | — | open | — |
| AQ-0024 | question | Whether prompt behaviour is evaluated for any plugin other than flow | flow ships the marketplace's only behavioural evaluation of prompt output [EV-0095], [EV-0096], covering its own authored cases. Nothing must let that stand for the other seven entries | Scope every evaluation statement to flow by name | An evals directory and a retained summary for a second plugin | Daniel Bentes | no | 03-assurance, 05-due-diligence | open | open | — |
| AQ-0025 | question | What delivery, ordering and blocking semantics the Claude Code client applies to hook execution | The package's entire control-point argument rests on a non-zero hook exit blocking a tool call, and no evidence establishes it. 19 hook scripts across two tracked manifests [EV-0162], plus the pinned external plugin's own hooks [EV-0168], are described as controls on that basis | Every statement about hooks blocking a tool call is written `Inferred:` with its chain, never asserted | Client documentation, or one observed hook rejection | Daniel Bentes | no | 02-architecture, 03-assurance | before the next refresh | partially resolved | Partially resolved 2026-09-10: the client refused three Bash tool calls after flow's `block-destructive.sh` exited non-zero at `PreToolUse` [EV-0172], which is the first observation in this package that a non-zero hook exit blocks a tool call. It is one hook, one plugin version, one client, on one machine — delivery, ordering and the general client contract remain unestablished, so statements about them stay `Inferred:` |
| AQ-0026 | question | Whether flow's `block-destructive.sh` should parse shell grammar rather than match raw command text | All three refusals observed during this refresh were false positives [EV-0173]: one took the first token of a multi-line script as a branch name, and two matched denied strings inside heredoc bodies where they were document text. A guard that fires on text it has not parsed will keep producing the next false positive as each one is excluded | Treat the hook as firing correctly and matching incorrectly; the client honours its exit [EV-0172] | A parser-based matcher, or a recorded decision that raw-text matching is the accepted trade | Daniel Bentes | no | 02-architecture, 03-assurance | before the next flow release | open | — |
| AQ-0027 | question | What commit a document's `project-version` should name once the branch it was drafted on has been squash-merged | One document header and 38 evidence rows still pin `06b1586`, which no clone of this repository resolves (CT-0019). The other 22 documents were re-stamped to `7ee4923` on 2026-09-10 — the last commit that changed the project rather than the documentation, which is what the header schema defines the field to mean. The field exists so a document can be re-verified against the thing it describes | Stamp the commit the documents describe, not the commit that last edited them. Applied on 2026-09-10 to 22 documents; the verification report is deliberately left at `06b1586` until a round re-verifies it | A rule: the commit the claims were last checked at, the package HEAD at publication, or both fields | Daniel Bentes | no | all | before the next refresh | needs-owner | — |
| AQ-0028 | question | Whether the two public documents may keep sentences whose claim rows are `superseded` while the replacement rows sit `pending` approval | Twelve `CL-` rows are `pending` and ten are `superseded`. The published guides still carry the superseded wording in at least four places (CT-0011, CT-0012, CT-0013), so the approval gate is holding corrections out of the public documents rather than holding errors out | Leave the public documents untouched and record each surviving sentence as a contradiction. Nothing in this package may approve a claim on the owner's behalf | An approval or rejection decision on CL-0036, CL-0037, CL-0039, CL-0042, CL-0044 and CL-0047 | Daniel Bentes | **yes** | 00-control/claim-and-disclosure-register.md, 06-public | before either public document is published or linked again | needs-owner | — |
| AQ-0029 | question | Which sense of "vendored" the package keeps, given three referents in current use | TM-0007 lists `vendored plugin` as an alias for an in-tree plugin — all six of which are first-party original work. `05-due-diligence/assets-dependencies-and-licenses.md` uses "vendored" in the opposite and industry sense, third-party code carried in-tree, and categorises it as borrowed IP. `plugins/dossier/schema.json` fixes a third as a config enum value for `dossier.ci.instructionSource`. Calling first-party code vendored inside a licensing document is a wrong provenance claim, not a style wobble | Treat the due-diligence sense as canonical and the TM-0007 alias as the defect, without editing the terminology register from here | A ruling on whether TM-0007's alias is deprecated with a pointer, and whether the other two senses get their own rows | Daniel Bentes | no | 00-control/terminology-and-ownership.md, 02-architecture/components-and-codebase.md, 05-due-diligence/assets-dependencies-and-licenses.md | before the next refresh | needs-owner | Raised by the project-modeling pass this run; the collision is recorded here rather than resolved, because renaming a term is an owner decision |
| AQ-0030 | question | Whether this range's two interface-level changes belong in `02-architecture/interfaces-and-integrations.md`, which does not mention either | That document is the package's register of contracts and their stability, and it is the most stale file in the package — pinned to `06b1586` at 2026-07-26, and outside this refresh's affected set. Two changes in the range move interface boundaries: the marketplace-manifest CI job now calls the GitHub API to resolve external pins, which is an outbound integration the document does not list, and `agent-capability-standard`'s resolution moved from an in-tree path to a client-side `github` fetch, which moves an integration boundary off this repository | Treat the document as covering the contract set as of 2026-07-26 and say so, rather than reading its silence as an absence of integrations | A read of `marketplace-manifest.yml`'s network calls and of the manifest's three resolution mechanisms against the document's interface table | Daniel Bentes | no | 02-architecture/interfaces-and-integrations.md | before the next refresh | open | Raised by the project-modeling pass this run. Contradictions already recorded against the same document are CT-0008, CT-0010 and CT-0023; this row is the omission the three do not cover |
| AQ-0031 | question | Whether the evidence ledger should distinguish a row whose locator is an executed check or a file from one whose locator is another agent's prose, so the lint can count the second class | Five times in this refresh a count travelled between passes in a message and was asserted without anyone re-running the command that produced it. Two were this register's, three the ledger's, and each was caught only when someone re-measured by hand: 51 pinned rows against 38, eleven pinned documents against nine, both public headers reported as carrying no pin when they carry it under a different key, and two contradiction rows that cited a schema or a bare count with no row behind them until [EV-0177] and [EV-0180] were written. A reader of this package cannot currently tell which class a row is in | Treat every relayed figure as unmeasured until a `cmd:CHK-###` or file locator carries it, and re-run rather than cite a peer | A decision on whether the distinction is worth recording, and if so where — a locator class, a Notes convention, or a provenance column | Daniel Bentes | no | 00-control/evidence-ledger.md | before the next refresh | needs-owner | The remedy is a change to the dossier plugin's own ledger schema rather than a defect in this package, so it belongs as an issue against the plugin. This row records the question and the five instances behind it; it does not choose the answer. Raised jointly with the evidence-ledger pass this run |
| AQ-0032 | question | PyYAML and `jsonschema` are runtime requirements of the flow and dossier plugins, and nothing in the repository declares either. Should they be declared, or should the degradation paths stay the only answer? | Every caller already preflights [EV-0190], so the failure is not a crash. It is quieter than that: eight `bin/` scripts exit 2 with a named install command, but four hook scripts exit 0, and two of those — `flow-goal-stop.sh` and `flow-goal-evaluator.sh` — emit `{"decision":"approve"}`, so FlowGoal enforcement stops gating. The evaluator carries no warning at all. An operator whose interpreter lacks PyYAML gets a gate that approves everything and says nothing | The requirements are real and undeclared [EV-0186], [EV-0187], [EV-0191]. This documentation branch records them; it does not change the tree it describes | A decision, not evidence. The evidence is complete: [EV-0186], [EV-0187], [EV-0189], [EV-0190], [EV-0191]. Tracked as issue #175, scheduled for the flow 3.3.1 patch release | Daniel Bentes | no | 01-project/executive-project-brief.md, 02-architecture/interfaces-and-integrations.md, 03-assurance/security-privacy-and-compliance.md, 04-operating/onboarding-and-local-development.md, 04-operating/operations-and-incident-response.md, 05-due-diligence | — | open | — |
| AQ-0033 | question | Whether `tests/agentteams-gate`'s three failures are a regression in the agentTeams opt-in gate or drift in the test's expectations | The three assertions all concern precedence between a `HOME`-scoped settings file and the plugin root when the HOME-side value is malformed or null. The test expects no fall-through; the implementation falls through. One of those readings is a bug in a security-adjacent gate and the other is a stale test, and they call for opposite fixes [EV-0204] | The contract is recoverable from the assertion names, so this is answerable without archaeology. It is not answerable by this documentation branch | A decision on which side encodes the intended behaviour | Daniel Bentes | no | 03-assurance/testing-quality-and-delivery.md, 04-operating/onboarding-and-local-development.md | — | open | Tracked as issue #177 |
| AQ-0034 | question | What the repository-root `tests/` directory is for, and whether it should be wired into CI or removed | Eleven executables, 140 assertions, and nothing has run them since 2026-05-08 [EV-0203]. A test set that no workflow reaches decays silently, which is how #177 stayed red with no observed start date. `tests/cascade-resolve/test.sh` also looks like a duplicate of `plugins/flow/tests/cascade-resolve.test.sh`, which would make part of the directory dead weight rather than uncovered coverage | Keep both readings open. The package now records the directory's existence and state rather than describing the tree as having exactly two suites | A decision, and if the answer is "keep", a workflow filter that reaches it | Daniel Bentes | no | 03-assurance/testing-quality-and-delivery.md | — | open | Tracked as issue #177 |
| AQ-0035 | question | Whether `flow-tests.yml` should path-filter on `plugins/flow/hooks/**` | The filter names five `plugins/flow/` subpaths and `hooks` is not among them [EV-0203], so a change to a hook script does not trigger the flow suite. Flow's hooks include the three blocking `PreToolUse` scripts that fail closed [EV-0197] — the highest-consequence shell in the plugin is the shell whose changes do not run the tests | The omission looks unintentional rather than reasoned, but nothing in the tree records the intent | A decision from the owner, or a commit message that explains the original scoping | Daniel Bentes | no | 03-assurance/testing-quality-and-delivery.md, 02-architecture/infrastructure-and-deployment.md | — | open | — |
| AQ-0036 | question | Whether the 178 unread claim fragments in the public guides should be registered, or the guides restructured so their claims sit where the registration check can read them | The check reads 38 per cent of the claim-length fragments in these documents [EV-0249]. The 178 it does not reach are capability tables, permission and role tables, the data question-and-answer, the versioning policy, the exit-code table and troubleshooting — the reference material a partner or operator most relies on. None is false as far as reading establishes; none is registered, so nothing mechanical would catch it if one became false | Both guides are marked **reviewed by reading, partially checked by machine**, which is what they are. Registering 194 fragments and restructuring two documents are different-sized answers to the same finding, and the choice is the owner's | A decision. The measurement is complete | Daniel Bentes | no | 06-public/technical-partner-guide.md, 06-public/customer-product-and-trust-guide.md, 00-control/claim-and-disclosure-register.md | — | open | Tracked as issue #176 |

## Contradictions

| ID | Claim A | Claim B | Likely cause | Decision impact | Resolution | Resolution basis | Owner | Next verification | Affected docs |
|---|---|---|---|---|---|---|---|---|---|
| CT-0001 | `README.md` presented an MIT licence badge linking to `LICENSE`, and its closing section stated the project was MIT-licensed [EV-0020] | No `LICENSE` file existed and GitHub detected no licence for the repository | A badge and a licence sentence were written without the file being added | **High while open.** Absent a licence file, default copyright applied and no installer had a grant of rights to the six MIT-declared plugins, whatever the badge said | **resolved** | Evidence, not choice: a `LICENSE` file carrying the canonical Apache-2.0 text was added at the repository root and pushed [EV-0019], and every declaration was aligned to it — 7 plugin manifests, 8 marketplace entries, the README badge and licence section, 5 plugin READMEs [EV-0021], [EV-0020]. The owner chose Apache-2.0 | Daniel Bentes | On merge, confirm `gh api repos/…` reports `apache-2.0` — detection is computed from the default branch and cannot be observed from a feature branch (AQ-0011) | 05-due-diligence/assets-dependencies-and-licenses.md, 06-public |
| CT-0002 | The flow plugin's README claims 33 skills [EV-0028] | 32 `SKILL.md` files exist in the flow tree [EV-0004] | `plugins/flow/skills/learned/` is a reserved placeholder holding only `.gitkeep`; it counts as a directory but is not a skill [EV-0029] | Low. A reader comparing the README to the installed tree finds one skill they cannot locate | resolved | Both sides are true under different counting rules. The file count is the one an operator experiences, so 32 is the number a document may state. The README was corrected to 32 in commit 7ee4923 [EV-0170], which closes the reader-facing half | Daniel Bentes | On any change to the flow skill set | 02-architecture/components-and-codebase.md, 04-operating |
| CT-0003 | The `ai-first-org-design-kit` marketplace description says "Fourteen opinionated skills" [EV-0027] | 15 `SKILL.md` files exist in that tree [EV-0004] | The set grew by one without the description being updated; the description also enumerates exactly fourteen capabilities | Low, but the description is what an installer reads before installing | resolved | The description was corrected to "Fifteen opinionated skills" in commit 7ee4923 [EV-0170]; 15 is the file count an installer receives | Daniel Bentes | On the next skill addition | 04-operating/decisions-technical-debt-and-risks.md |
| CT-0004 | The repository's own dossier CI template forbids `${{ github.event.* }}` interpolation inside a `run:` body, and a shipped test enforces it | `release-desktop-skills.yml` line 24 does exactly that with `github.event.release.tag_name` [EV-0015] | The rule was written for the artifact being shipped to others and never applied to the repository's own workflows | Medium. Release tag names are set by whoever can publish a release, so exploitation requires maintainer-level access — but the repository fails a standard it publishes | unresolved | — | Daniel Bentes | Re-run the `awk` scanner (CHK-06) over all workflows | 03-assurance/security-privacy-and-compliance.md, 04-operating |
| CT-0005 | `02-architecture/components-and-codebase.md` records `plugins/agent-capability-standard/**` as vendored code regenerated with `git submodule update --remote` at pointer `95f7ac2` | No `.gitmodules` exists [EV-0059], the path is gitignored [EV-0060], and the marketplace entry is a `github` source pinned to `9e2f65b` [EV-0058] | The document predates PR #165, which removed the submodule so the directory would never be committed as a gitlink again | A reader following the documented regeneration command operates on a submodule that is not there, and a diligence reader counts a vendored tree the repository does not carry | resolved-B | Evidence, not choice. `02-architecture/components-and-codebase.md` at `d3fc744` records the path as neither generated nor vendored, names the `github` pin, and cites the rationale commit [EV-0058], [EV-0059], [EV-0060], [EV-0167]. This row covers that document only; the same description survives in six others, which is CT-0009 | Daniel Bentes | Settled. Successor row CT-0009 carries the remaining locations | 02-architecture, 05-due-diligence |
| CT-0006 | `02-architecture/components-and-codebase.md` states that bumping a plugin version has **nothing automated** behind it and that **nothing validates the manifest** | `scripts/check-plugin-versions.sh` compares every entry's advertised version against its source and runs in CI on manifest changes, weekly, and on demand [EV-0080], [EV-0081], and passes at HEAD [EV-0127] | The document predates PR #166, which automated exactly the gap it names | The document tells a maintainer a safety net is absent that now exists, which is the kind of statement that makes a reader distrust the rest of the package | resolved-B | Evidence, not choice. `02-architecture/components-and-codebase.md` at `d3fc744` describes the check, its CI triggers and its stated limit [EV-0080], [EV-0081], [EV-0127]. This row covers that document only; the same statement survives in four others, which is CT-0008 | Daniel Bentes | Settled. Successor row CT-0008 carries the remaining locations | 02-architecture, 04-operating |
| CT-0007 | `plugins/dossier/bin/cascade-resolve.sh` states in its own header that it is "a byte-for-byte behavioural twin of plugins/flow/bin/cascade-resolve.sh" | The two files differ by 66 lines at HEAD [EV-0142], including three behavioural differences: dossier decides absence with `jq -e`, drops a git-tracked `.local.json` layer with a stderr warning, and exits 2 when `mktemp` fails where flow falls back to a `/tmp` path | Dossier's copy received three fixes that were never ported back, and the header comment was not updated with them | A maintainer reading either file is told the other behaves identically, so the tracked-`.local.json` guard — a security-relevant fix — looks already present in flow when it is not | unresolved | — | Daniel Bentes | On the next flow release | 02-architecture, 03-assurance |
| CT-0008 | Four documents state that nothing validates the marketplace manifest: `02-architecture/interfaces-and-integrations.md` line 23 ("**not validated anywhere in this repository**"), `01-project/product-and-domain.md` lines 72 and 125 ("nothing validates the manifest"; validated "**nowhere**"), `04-operating/decisions-technical-debt-and-risks.md` R-08 ("**none — nothing checks it**"), `03-assurance/reliability-performance-and-observability.md` line 127 ("**none automated**") | `scripts/check-plugin-versions.sh` compares every entry's advertised version against its source, runs in CI on manifest changes, weekly and on demand [EV-0080], [EV-0081], and passed at HEAD with 8 entries checked and 0 failures [EV-0127] | Staleness. None of the four entered the refresh's affected set, so the correction that reached `02-architecture/components-and-codebase.md` never reached them (AQ-0015) | A maintainer is told a safety net is absent that now exists, and a diligence reader counts an unmitigated risk that has a mitigation. It is also self-inconsistent within the package: `02-architecture/data-and-ai.md` line 146 now says the opposite in the same directory | unresolved | — | Daniel Bentes | Re-read the five locations after `/dossier:reconcile` repairs them; the check's own state is already established by [EV-0127] | 01-project/product-and-domain.md, 02-architecture/interfaces-and-integrations.md, 03-assurance/reliability-performance-and-observability.md, 04-operating/decisions-technical-debt-and-risks.md |
| CT-0009 | Six documents still describe `agent-capability-standard` as a git submodule: `06-public/technical-partner-guide.md` line 17 ("one git submodule"), `01-project/product-and-domain.md` lines 82, 103 and 183, `04-operating/operations-and-incident-response.md` lines 40 and 165, `04-operating/decisions-technical-debt-and-risks.md` D-03, TD-07 and the near-term roadmap row, `03-assurance/reliability-performance-and-observability.md` line 73, `05-due-diligence/technical-due-diligence-report.md` line 120 reads the dependency at the old pointer, and CL-0039 in `00-control/claim-and-disclosure-register.md` blends the sense in a claim still pending approval | No `.gitmodules` exists in the tracked tree [EV-0059], `git ls-files` returns 0 entries for the path [EV-0060], and the entry is a `github` source pinned to sha `9e2f65b` [EV-0058]. Commit `efb4f75` records why the submodule was removed [EV-0167] | Staleness. The blast-radius map did not route the change to these six, so only `02-architecture` and parts of `05-due-diligence` were corrected (AQ-0015) | A partner reading the public guide is told the distribution shape is a submodule, which changes how they expect to consume the entry; a maintainer is given remediation actions — "move the submodule pointer to a tagged commit" — for a submodule that does not exist. One of the six is a published document, which makes this materially misleading rather than an internal inconsistency | unresolved | — | Daniel Bentes | Re-read the six locations after `/dossier:reconcile` repairs them; the tree state is already established by [EV-0058], [EV-0059], [EV-0060] | 00-control/claim-and-disclosure-register.md, 01-project/product-and-domain.md, 03-assurance/reliability-performance-and-observability.md, 04-operating/decisions-technical-debt-and-risks.md, 04-operating/operations-and-incident-response.md, 05-due-diligence/technical-due-diligence-report.md, 06-public/technical-partner-guide.md |
| CT-0010 | Five documents state the marketplace metadata version as 4.7.0 and describe it as advertising a release that does not exist: `02-architecture/interfaces-and-integrations.md` line 23, `04-operating/operations-and-incident-response.md` line 163, `01-project/product-and-domain.md` line 170, `07-verification/documentation-verification-report.md` line 324, `00-control/claim-and-disclosure-register.md` line 104, and AQ-0010 in this file | `metadata.version` is 4.10.0 across 8 entries [EV-0057], and releases v4.9.0 and v4.10.0 were both published on 2026-09-10 [EV-0066] | Staleness. 4.7.0 was branch state on a branch that has since merged; the tag it lacked now exists | The gap the five documents name — a manifest version with no corresponding release — is closed. A reader weighting release discipline is shown a defect that has been fixed, and a reader checking the manifest finds a different number than the package states | unresolved | — | Daniel Bentes | Re-read the six locations after repair; the manifest and release state are established by [EV-0057], [EV-0066] | 00-control/claim-and-disclosure-register.md, 01-project/product-and-domain.md, 02-architecture/interfaces-and-integrations.md, 04-operating/operations-and-incident-response.md, 07-verification/documentation-verification-report.md |
| CT-0011 | `06-public/technical-partner-guide.md` line 111: "The marketplace has published 57 tags; the most recent release is v4.6.2, dated 2026-05-29" — the published form of CL-0011 [EV-0032] | The most recent release is v4.10.0, dated 2026-09-10 [EV-0066], and the repository holds 63 tags at `d3fc744` [EV-0177]. CL-0042 records both figures and is uncommitted working-tree state, `pending` approval | The document was routed and never re-drafted. `06-public/technical-partner-guide.md` is in this refresh's own affected set, and `git log` shows it unchanged since 2026-07-26. The replacement claim is not committed either — CL-0042 exists only in the working tree at the time of this sweep, and is `pending` approval within it | A partner evaluating whether the marketplace is maintained reads a release history that stops three and a half months short. Release recency is one of the few maintenance signals a partner has from outside | unresolved | — | Daniel Bentes | Approve or reject CL-0042, then re-read the guide. The tag and release state is established by [EV-0066] | 00-control/claim-and-disclosure-register.md, 06-public/technical-partner-guide.md |
| CT-0012 | Both public documents state: "The published `main` manifest carries seven entries; the eighth exists only on the branch this guide was produced from" — `06-public/technical-partner-guide.md` line 15 and `06-public/customer-product-and-trust-guide.md` line 15, the published form of CL-0024 [EV-0001], [EV-0054] | The published `main` manifest carries all eight entries at both `d3fc744` and `origin/main` [EV-0181]; dossier reached `main` and shipped in v4.9.0 [EV-0057]. CL-0044 records the correction and is uncommitted working-tree state, `pending` approval | The same shape as CT-0011: both guides are in the refresh's affected set and neither has been touched since 2026-07-26, and CL-0044 is uncommitted working-tree state pending approval | An operator deciding what to install is told that one of the eight plugins is not available to them. The eighth is `dossier`, the entry the rest of this package is about | unresolved | — | Daniel Bentes | Approve or reject CL-0044 **first**. Only then delete the italic qualifier; the preceding sentence already says eight plugins and is correct. The edit is gated on that approval and must not be made ahead of it — CL-0024 is superseded and CL-0044 is pending, and nothing in this package may approve a claim on the owner's behalf (AQ-0028). The manifest state is established by [EV-0057], [EV-0181] | 00-control/claim-and-disclosure-register.md, 06-public/customer-product-and-trust-guide.md, 06-public/technical-partner-guide.md |
| CT-0013 | `06-public/technical-partner-guide.md` line 61: "The flow plugin ships 32 skills, 23 commands, 9 agents, and 12 hook scripts" — the published form of CL-0004 [EV-0004], [EV-0038] | flow ships 14 hook scripts, not 12 [EV-0129], [EV-0162], recorded as CL-0037, which is `pending` approval | The guide was routed and never re-drafted, so nothing re-derived the sentence. The skill, command and agent figures are still right; only the hook count moved. CL-0037 is uncommitted working-tree state pending approval | A partner sizing the install-time surface undercounts the scripts that run on their machine at lifecycle events by two. The hook surface is the part of the guide a security-minded partner reads most closely | unresolved | — | Daniel Bentes | Approve or reject CL-0037, then re-read the guide. The count is established by [EV-0129], [EV-0162] | 00-control/claim-and-disclosure-register.md, 06-public/technical-partner-guide.md |
| CT-0014 | `00-control/documentation-index.md` canonical-document table records `verified` and `2026-07-26` for all 23 documents, and its package block pins the package to `06b1586` at package version 1.0.4 | Ten document headers read `status: partially verified` with `last-verified: 2026-09-10`, at `7ee4923`, `691bcdb` or `e104483` | Staleness. The index is drafted first and finalized last, and this refresh re-drafted the documents without returning to it | The index is the package's entry point and states up front how much the package is worth. A reader who trusts it takes ten partially-verified documents as fully verified and the evidence cutoff as six weeks older than it is. The header schema settles which side is right: "when they disagree, the header wins and the index is the defect" | unresolved | — | Daniel Bentes | Re-derive the index table from the 23 headers after `/dossier:reconcile` repairs it. Which side is right is not in question — the header schema settles it at `plugins/dossier/references/document-headers.md` rule 5 — but the index still carries the wrong cells, and the index is the entry point | 00-control/documentation-index.md |
| CT-0015 | `00-control/documentation-index.md` evidence-coverage table: 56 evidence rows, 0 rows past their freshness expiry ("all observed 2026-07-26"), and per-document citation counts summing against that total. Its unresolved-items table lists AQ-0011 as open | The ledger holds 193 `EV-` rows, of which many carry 2026-07-26 observation dates against a 90-day freshness threshold, and AQ-0011 was closed on 2026-09-10 by [EV-0132] | Staleness, same cause as CT-0014, but a distinct claim: this one is about how much evidence exists and how fresh it is, not about document status | A diligence reader uses the coverage table to decide where to discount. At 56 rows they discount the wrong documents, and "0 rows past freshness" tells them nothing needs re-checking when the register's own AQ-0022 exists because some rows do | unresolved | — | Daniel Bentes | Re-derive the coverage table from the ledger after repair. The ledger is the source of truth for its own row count and freshness by the index's own dependency table, so the index figures are the defect; they are still in the file | 00-control/documentation-index.md, 00-control/evidence-ledger.md |
| CT-0016 | The repository ships 40 shell scripts under plugin `bin/` directories, and all 40 are outside every CodeQL-configured language — stated in AQ-0018 in this file, in CL-0047, in the note on [EV-0140], and as "40 plugin scripts" in `01-project/executive-project-brief.md` line 105, resting on [EV-0162] | `git ls-files 'plugins/*/bin/*'` returns 40 files but `git ls-files 'plugins/*/bin/*.sh'` returns 37; flow's `bin/` holds 3 Python files alongside 17 shell scripts, and CodeQL's matrix includes `python` [EV-0134], [EV-0093] | Counting-basis mismatch. EV-0162 counted every file in `bin/`; the sentences built on it read that total as a shell-script count. The same mismatch makes EV-0162's claim to supersede EV-0093's figure of 37 wrong — `plugins/*/bin/*.sh` is 37 at `15bcb24`, `7ee4923` and `d3fc744` alike, so nothing grew | A security reader is told the static-analysis gap covers 40 scripts when it covers 37, and that 3 files sit outside a scanner that in fact analyses them. CL-0047 is a public-facing claim about scan coverage, which makes the direction of the error — overstating the gap — the reader's problem either way | both-scoped | Settled by evidence, not by choice. [EV-0175] establishes that 40 and 37 count the same tree two ways — every tracked file under `plugins/*/bin/` against those carrying a `.sh` extension — so neither figure is wrong for what it measures and neither is growth; the `.sh` count is 37 at `15bcb24`, `7ee4923` and `d3fc744` alike. [EV-0176] settles the coverage half: `codeql.yml`'s own header comment states the python analysis scans this repository's python under `plugins/flow/`, which is where all three `bin/*.py` files live. Whether the uploaded analyses actually included those three files is not observed and is not asserted | Daniel Bentes | Settled. Successor row CT-0020 carries the three sentences that still state the merged figure | 00-control/claim-and-disclosure-register.md, 00-control/evidence-ledger.md, 01-project/executive-project-brief.md, 02-architecture/components-and-codebase.md, 03-assurance |
| CT-0017 | `01-project/product-and-domain.md` line 26 counts 16 hook scripts, and `07-verification/documentation-verification-report.md` line 285 lists "16 hooks" among the artifact counts its cross-document consistency check reports as agreeing everywhere | 19 hook scripts exist across the two tracked manifests — 14 in flow and 5 in dossier [EV-0129], [EV-0162] | Staleness in the first location. In the second the count was never re-derived, and the consistency check reported agreement because every document it compared carried the same stale number | The install-time surface is the security reader's first count, and it is understated by three. The second location is worse than a wrong number: it is a verification result asserting that a wrong number was checked | unresolved | — | Daniel Bentes | Re-run the artifact-count consistency check against `git ls-files 'plugins/*/hooks/scripts/*'`; the count is established by [EV-0129], [EV-0162] | 01-project/product-and-domain.md, 07-verification/documentation-verification-report.md |
| CT-0018 | `07-verification/documentation-verification-report.md` line 285 lists "112 skills" among the artifact counts, with the cross-document consistency result "yes" and drift "none — unchanged across all three rounds" | 71 `SKILL.md` files are tracked [EV-0093], [EV-0162]. The 112 figure counted the untracked, gitignored `agent-capability-standard` residue on the assessment machine [EV-0060], [EV-0094] | Environment difference frozen into a verification result. The count was taken with `find` on a machine holding working-tree residue rather than with `git ls-files` | A diligence reader takes 112 as the content volume of the repository when 41 of those files are not repository content. The verification report is the document a reader consults to decide how much the rest is worth, so a wrong figure certified as consistent damages more than the figure itself | unresolved | — | Daniel Bentes | Re-derive with `git ls-files '*/SKILL.md' \| wc -l`; the tracked count is established by [EV-0093], [EV-0162] and the residue by [EV-0060], [EV-0094] | 07-verification/documentation-verification-report.md |
| CT-0019 | One internal document header carries `project-version: 06b1586`, 38 evidence rows carry it in their version column [EV-0178], and `00-control/documentation-index.md` names it in prose as the project version the package describes | `06b1586` resolves to no object in this repository: `git cat-file -t 06b1586` returns "Not a valid object name". The branch `fix/gate-verdict-integrity` it lived on was squash-merged and deleted | Squash-merge. `06b1586` was a head-branch commit of PR #133 on `fix/gate-verdict-integrity`, which merged on 2026-07-26 as `525cca5` — a different sha, so the merge squashed — and the head branch now returns 404 [EV-0178]. Nothing re-pinned the documents that did not change | The observation date of 38 evidence rows, and one remaining document header, are pinned to a commit that no clone of this repository can resolve, so none of them can be re-verified locally against the thing they claim to describe — which the header schema names as the whole purpose of the field. It also silently defeats any freshness or drift check that resolves the pin with git. The pin is not wholly unreachable: GitHub still serves the commit, so a reader with network access can check a pinned row while a reader working offline from a clone cannot [EV-0178]. The commit is unreferenced and not yet garbage-collected, so the pin may stop resolving at any time without notice [EV-0178] | unresolved | — | Daniel Bentes | `git cat-file -t 06b1586` at HEAD, then a decision on what the stale documents should be re-pinned to (AQ-0027) | 07-verification/documentation-verification-report.md, and the 38 evidence rows whose version column carries the pin |
| CT-0020 | Three sentences state that the repository ships 40 shell scripts and that all 40 sit outside every CodeQL-configured language: `02-architecture/components-and-codebase.md` line 56 ("The 40 `bin/` scripts supersede the tree-wide figure of 37"), AQ-0018's resolution text in this file, and CL-0047's evidence column in `00-control/claim-and-disclosure-register.md` | 37 of the 40 are shell; the other 3 are Python in flow's `bin/` [EV-0175], and `codeql.yml`'s python analysis is configured to scan `plugins/flow/` [EV-0176] | The counting-basis mismatch settled as CT-0016 reached the ledger and not the sentences built on it | A security reader is told the static-analysis gap covers 40 scripts when it covers 37, and that 3 files sit outside a scanner configured to analyse them. CL-0047 is cleared for publication to partners, so this one leaves the package | resolved | Corrected 2026-09-10 at all four sites. AQ-0018, CL-0047 and the note on EV-0140 now read 37 shell scripts under plugin `bin/` plus 19 hook scripts outside every configured language, with the 3 Python files under `bin/` inside it [EV-0175], [EV-0176]. The executive brief's "40 plugin scripts" is a file count and is correct as written | Daniel Bentes | Re-read the three locations after `/dossier:reconcile` repairs them; the counts and the workflow configuration are already established by [EV-0175], [EV-0176] | 00-control/assumptions-questions-and-contradictions.md, 00-control/claim-and-disclosure-register.md, 02-architecture/components-and-codebase.md |
| CT-0021 | Five documents describe the README and marketplace-description defects that commit `7ee4923` fixed as live today: `04-operating/decisions-technical-debt-and-risks.md` TD-02 ("**Four wrong facts live today**: badge says 6 plugins where 8 exist, flow 3.2.0 vs 3.2.2, prompt-decorators 0.1.0 vs 0.1.1, dossier absent"), `04-operating/operations-and-incident-response.md` line 164 ("**Already failed four times**"), `01-project/product-and-domain.md` lines 62, 81, 176 and 178 to 181, `03-assurance/testing-quality-and-delivery.md` line 85 ("Description off by one"), and `00-control/terminology-and-ownership.md` TM-0024 ("Its published description says fourteen") | All four were corrected at `7ee4923` [EV-0170] and all are gone at `d3fc744` [EV-0179]: the README badge reads 8, flow reads 3.3.0, prompt-decorators 0.1.1, dossier is present at 1.2.0, flow's README claims 32 skills against 32 tracked, and the marketplace description reads "Fifteen opinionated skills" | Staleness for the four documents outside the blast radius. `03-assurance/testing-quality-and-delivery.md` is different and worse: it was re-drafted this run and pinned to `7ee4923`, the very commit that fixed the defect it reports, which means the drafter worked from the ledger rather than the tree | A maintainer is handed a remediation backlog of four defects that no longer exist, and a reader weighting documentation discipline counts drift that has been repaired. The terminology register is the package's naming authority, so its copy propagates | unresolved | — | Daniel Bentes | Re-read the six locations against the tree at HEAD; the corrections are established by [EV-0170] and their present absence by [EV-0179] | 00-control/terminology-and-ownership.md, 01-project/product-and-domain.md, 03-assurance/testing-quality-and-delivery.md, 04-operating/decisions-technical-debt-and-risks.md, 04-operating/operations-and-incident-response.md |
| CT-0022 | Both public documents state that five of the seven in-repository plugins ship no automated test suite — `06-public/customer-product-and-trust-guide.md` line 40 and `06-public/technical-partner-guide.md` line 147, the published form of CL-0006 [EV-0010] — and `03-assurance/reliability-performance-and-observability.md` line 73 says "5 plugins remain untested" | Six plugins are in-repository, not seven, since `agent-capability-standard` left the tree [EV-0058], [EV-0059], and four of the six carry no `tests/` directory — recorded as CL-0039, which is `pending` approval | Both guides were routed and never re-drafted, and the reliability document was never routed at all. The denominator moved when the submodule was removed and neither figure was re-derived. CL-0039 is uncommitted working-tree state pending approval | Both numbers are wrong in the same direction, and the ratio a customer reads — five of seven — understates the coverage they actually get. It is the only quality figure either public guide offers | unresolved | — | Daniel Bentes | Approve or reject CL-0039, then re-read both guides and the reliability document; the tree state is established by [EV-0058], [EV-0059], [EV-0162] | 00-control/claim-and-disclosure-register.md, 03-assurance/reliability-performance-and-observability.md, 06-public/customer-product-and-trust-guide.md, 06-public/technical-partner-guide.md |
| CT-0023 | `02-architecture/interfaces-and-integrations.md` states flow's interface version as 3.2.2 in four places — the `/flow:*` command row, the settings row, the produced-artifact row, and the manifest excerpt at line 71 | flow is at 3.3.0 in both its own `plugin.json` and the marketplace manifest [EV-0065], [EV-0057] | Staleness. The document did not enter the refresh's affected set, and it is the one document whose subject is version-bearing interface contracts | An integrator reads a stability contract pinned to a version two minor releases old and cannot tell whether the contract still holds. The manifest excerpt is quoted as if copied from the file, which invites a reader to trust it without checking | unresolved | — | Daniel Bentes | Re-read the four rows against `.claude-plugin/marketplace.json` and `plugins/flow/.claude-plugin/plugin.json`; the version is established by [EV-0057], [EV-0065] | 02-architecture/interfaces-and-integrations.md |
| CT-0024 | Both public headers carry `product-version: 06b1586` — `06-public/technical-partner-guide.md` and `06-public/customer-product-and-trust-guide.md` | The public header schema defines `product-version` as "the **product** version the document applies to, not the commit", and states plainly that "a commit SHA in a public header is a repository detail leak" (`plugins/dossier/references/document-headers.md`). Both headers are observed at [EV-0180] | The public headers were written by copying the internal `project-version` value rather than translating it to a product version. The value is also the orphaned commit of CT-0019, so it identifies a branch that no longer exists | Two documents cleared for publication leak an internal repository identifier, and neither tells its reader which released product version it describes — which is the only version an external reader can act on. The gate's leakage check passes, so nothing else in the package catches it | resolved | Both headers were re-derived on 2026-09-10 to `product-version: 4.10.0`, the marketplace `metadata.version`, with `last-updated: 2026-09-10`. Both now validate against every `public-v1` rule. The choice of the marketplace version over the release tag is recorded here as the decision it was; a reader still cannot tell which *plugin* version the per-plugin facts describe, which is registered separately | Daniel Bentes | Decide what product version the guides describe — the marketplace `metadata.version`, or the release tag — and re-read both headers against the schema's public-v1 rules | 06-public/customer-product-and-trust-guide.md, 06-public/technical-partner-guide.md |
| CT-0025 | An approved public claim, CL-0009, states that the repository's only declared third-party runtime dependency is `pyyaml`, required by the agent-capability-standard plugin | No dependency manifest of any kind is tracked in the repository [EV-0186], and agent-capability-standard is no longer part of the tree [EV-0162]. PyYAML is required at runtime by two of the flow plugin's Python entrypoints and is declared nowhere an operator would see [EV-0187] | The manifest that declared `pyyaml` left the tree when agent-capability-standard became an externally sourced plugin. The claim was approved on 2026-07-26 against a tree that still carried it, and nothing re-checked it when the plugin moved out | **High while open.** A diligence reader was being told the dependency surface is declared and singular. It is neither: it is undeclared, and an operator installing the flow plugin discovers the requirement when a Python entrypoint fails to import. Only the two CI workflows pin the version | resolved | CL-0009 was superseded on 2026-09-10 and its qualification CL-0022 with it. The replacement CL-0048 states both halves — no declared dependencies, and an undeclared runtime requirement — and is pending approval, so the dependency subject leaves the public documents rather than shipping in a weaker form | Daniel Bentes | When a dependency manifest is added, or when CL-0048 is presented for approval | 05-due-diligence/assets-dependencies-and-licenses.md, 03-assurance/security-privacy-and-compliance.md, 06-public |
| CT-0026 | This session's own EV-0189, as first written, stated that a missing PyYAML raises an unguarded `ImportError` from `hooks/scripts/session-end-state.sh`, a hook reached without operator action | `session-end-state.sh:48` runs `python3 -c "import yaml" >/dev/null 2>&1 \|\| exit 0` before the heredoc that imports it. Eleven further callers preflight the same way [EV-0190] | The module `_journal_atomic.py:51` genuinely does import `yaml` unguarded. That property was generalised to the callers without reading them, and the generalisation was written into an evidence row, an open question, and a public GitHub issue before three drafters independently refused it | **Recorded rather than merely fixed.** The false version made the defect look like a crash, which is loud and self-reporting. The true version is worse and quieter: the enforcement hooks fail open. Had the first reading stood, the fix would have targeted the wrong layer | resolved | EV-0189 was rewritten to scope the unguarded import to the module, EV-0190 added to record what each caller actually does, and issue #175 corrected in a public comment. The check that should have caught it, CHK-116, read the module and its importers but not their preflight blocks; CHK-117 reads those | Daniel Bentes | When a caller's degradation path changes | 00-control/evidence-ledger.md, 02-architecture/interfaces-and-integrations.md, 04-operating, 05-due-diligence |

## Blocking decisions

| ID | Decision required | Who must decide | What is blocked | Consequence of proceeding without it | Deadline |
|---|---|---|---|---|---|
| AQ-0002 | Whether the dossier post-merge automation may be described as working before it has been observed working | Daniel Bentes | Any public statement that the plugin refreshes documentation automatically after a merge | The package would assert a capability whose only evidence is that its parts are unit-tested — the precise failure mode this documentation standard exists to prevent | Before the dossier plugin is announced or its pull request is merged |
| AQ-0028 | Whether to approve or reject the six pending public claims, so the published guides can be corrected | Daniel Bentes | Any republication or linking of `06-public/technical-partner-guide.md` and `06-public/customer-product-and-trust-guide.md` | Both guides keep sentences their own register marks superseded: a release history that stops on 2026-05-29, a statement that one of the eight plugins is unavailable, and a hook count two low. The approval gate is currently holding the corrections out rather than holding errors out | Before either public document is published or linked again |
| AQ-0027 | What commit a document's `project-version` should name after a squash-merge | Daniel Bentes | Re-verification of one document and 38 evidence rows | They pin `06b1586`, which no clone of this repository resolves, so nothing resting on them can be checked locally against the commit it describes. The other 22 documents were re-stamped to `7ee4923` on 2026-09-10 | Before the next refresh |
| AQ-0029 | Which sense of "vendored" the package keeps | Daniel Bentes | Terminology repair in three documents | A licensing document categorises borrowed IP as vendored while the terminology register uses the same word for first-party in-tree plugins, so first-party work reads as third-party | Before the next refresh |
| CT-0001 | ~~Whether to add a `LICENSE` file, and which licence it states~~ | Daniel Bentes | — | **Discharged 2026-07-26.** Apache-2.0 chosen; file added; all declarations aligned | — |

## Recommended evidence requests

Ordered by decision impact, highest first. Each names the smallest artifact that would settle its row.

| Rank | Request | Smallest sufficient artifact | Resolves | Decision unblocked | Asked of |
|---:|---|---|---|---|---|
| 1 | Approve or reject the six pending public claims | One decision per row on CL-0036, CL-0037, CL-0039, CL-0042, CL-0044 and CL-0047 | AQ-0028, CT-0011, CT-0012, CT-0013 | Whether the two published guides may be corrected, or must keep their superseded sentences | Daniel Bentes |
| 2 | Run the post-merge refresh once, end to end, and keep the run URL | One GitHub Actions run link plus the docs pull request it opened | AQ-0002 | Whether the plugin's headline capability may be stated as fact | Daniel Bentes |
| 3 | Re-run the whole package against `d3fc744`, not only the ten documents the blast radius reached | One refresh whose affected set is the package | CT-0008 to CT-0024 | Whether any document outside the blast radius can be relied on. Sixteen of the eighteen contradictions open today are the same defect: a correction that reached one document and not the others saying the same thing | Daniel Bentes |
| 4 | Decide what `project-version` a stale document is re-pinned to | One rule, written into the header schema | AQ-0027, CT-0019 | Whether the 38 evidence rows still pinned to it can be re-verified from a clone | Daniel Bentes |
| 5 | Correct the three sentences that still merge the shell and Python counts | One edit each to `components-and-codebase.md` line 56, AQ-0018's resolution text, and CL-0047's evidence column | CT-0020 | What the static-analysis gap actually covers, in a claim cleared for publication | Daniel Bentes |
| 6 | Add a `windows-latest` job to both plugin test workflows | One CI run, pass or fail | AQ-0008 | The scope of issues #100 and #130 | Daniel Bentes |
| 7 | Run the `agent-capability-standard` Python suite once at the pinned sha | `pytest` output | AQ-0001 | Whether the marketplace's only dependency-bearing plugin is tested | Daniel Bentes |
| 8 | Read the 2 commits past `v1.2.0` in the upstream repository | `git log v1.2.0..9e2f65b --oneline` — the current pin, not the former submodule pointer `95f7ac2` | AQ-0006 | Whether the advertised version is accurate | Daniel Bentes |
| 9 | Inspect the `prompt-decorators` plugin subdirectory | A file listing plus its `plugin.json` | AQ-0005 | Whether an entry published from this manifest is described accurately | Daniel Bentes |

## Open items summary

Counted from the two tables above at `d3fc744` on 2026-09-10.

| Measure | Count |
|---|---|
| `AQ-` rows | 31 |
| Open assumptions | 2 |
| Open questions | 19 |
| Partially resolved | 2 |
| Owner-decision items | 4 |
| Closed | 4 |
| Open `blocking` items | 2 — AQ-0002 and AQ-0028 |
| `CT-` rows | 24 |
| Unresolved contradictions | 18 |
| Items with no proposed owner | 1 — AQ-0004, which no evidence available from this repository can settle |
| Items past due | 0 |

Any open `blocking` item, and any unresolved contradiction that could materially mislead a diligence, onboarding, integration, operational, security, or customer decision, fails the release gate regardless of package score. Two blocking items are open: AQ-0002, and AQ-0028, which is new this run.

Eighteen contradictions are unresolved, and sixteen of them were opened by this run's package-wide sweep. They come from three mechanisms, not one, and the three want different repairs.

**Five documents were never routed.** `00-control/terminology-and-ownership.md`, `01-project/product-and-domain.md`, `02-architecture/interfaces-and-integrations.md`, `03-assurance/reliability-performance-and-observability.md` and `04-operating/operations-and-incident-response.md` are the five of twenty-three that the refresh's own blast radius did not name. The trigger map keys on changed source paths, so a change that alters a fact asserted in eight documents reaches only the documents whose paths matched an event. Four of the six documents still calling `agent-capability-standard` a submodule are in this group. The repair is a fan-out keyed on facts rather than on paths.

**Six documents were routed and then left untouched.** The blast radius named eighteen documents; thirteen were re-drafted, one of them — the terminology register — from outside the radius. `00-control/documentation-index.md`, `00-control/claim-and-disclosure-register.md`, `04-operating/decisions-technical-debt-and-risks.md`, `07-verification/documentation-verification-report.md` and both public guides were identified as affected and never opened. Neither public guide has changed since 2026-07-26. This is not a routing failure at all: the refresh did not finish its own affected set, and nothing recorded that it had not. CT-0011, CT-0012, CT-0013, CT-0014, CT-0015 and CT-0022 all sit here.

**Four claim rows exist only in an uncommitted working tree.** CL-0037, CL-0039, CL-0042 and CL-0044 correct the four wrong sentences in the published guides, and at `d3fc744` none of them is committed. They are also `pending` approval, so even once committed they cannot reach the guides until AQ-0028 is decided.

The first mechanism is already recorded as AQ-0015, and this sweep raises what that row costs. Propagation is keyed on paths because the fact-to-document index that would key it on facts — the project model at `00-control/.project-model.json` mandated by the plugin's own skill contract — has never existed here (AQ-0014). AQ-0014 and AQ-0015 are therefore the structural cause of most of this table, not two housekeeping items beside it.

A partial refresh does not leave a package partly fresh. It leaves a package that disagrees with itself, and a reader cannot tell which half is current.
