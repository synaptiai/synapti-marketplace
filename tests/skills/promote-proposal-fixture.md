---
name: "test-promote-fixture"
description: "[flow-learned] Test fixture used by bin/promote-proposal.sh smoke tests. Documents the canonical proposal shape so the validator has a known-good payload to validate against."
source-sessions:
  - "2026-05-06 fixture-session-1"
  - "2026-05-06 fixture-session-2"
evidence-count: 3
status: proposal
proposed: "2026-05-06"
---

# Test Promote Fixture

## Pattern Detected

This is a fixture for `bin/promote-proposal.sh` testing. It demonstrates the canonical proposal shape: frontmatter has all required fields (name, description, source-sessions, evidence-count, status, proposed), status is `proposal` (the validator rejects anything else), the body has all five required sections in order.

## Knowledge

The fixture exists so the smoke test for `bin/promote-proposal.sh --dry-run` can validate against a known-good payload without polluting the real `~/.claude/flow-proposals/` directory. Real proposals are generated by `/flow:learn`; this fixture mirrors their shape.

### When This Applies

When testing the promotion script in CI or in a smoke test, use this fixture (or a copy of it) as the input. The test asserts that `--dry-run` exits 0 and prints the validation-passed line.

### What To Do

Run `bin/promote-proposal.sh --proposal tests/skills/promote-proposal-fixture.md --dry-run` and assert exit 0.

### What To Avoid

Do not commit a real `bin/promote-proposal.sh` invocation to CI without `--dry-run` — promotion creates a branch, commit, push, and PR. Always use `--dry-run` in automated tests.

## Evidence

### Journal Citations

This fixture is synthetic — it has no real journal entries. The `source-sessions` field is filled with placeholder values that satisfy the schema but do not point at real sessions.

- `(synthetic)` — fixture-session-1: validation reference
- `(synthetic)` — fixture-session-2: validation reference

### Example

```
Before: no test fixture for the promotion script
After: tests/skills/promote-proposal-fixture.md serves as the canonical input
```

## Verification

- [ ] `bin/promote-proposal.sh --proposal tests/skills/promote-proposal-fixture.md --dry-run` exits 0
- [ ] The dry-run output contains the line "DRY-RUN: validation passed for 'test-promote-fixture'"
- [ ] No file is created under `plugins/flow/skills/learned/test-promote-fixture/` after the dry-run

## Promotion Checklist

- [ ] Reviewed by human (this fixture is intentionally NEVER promoted in production — it lives in tests/)
- [ ] Evidence is compelling (n/a — fixture)
- [ ] Knowledge is general (n/a — fixture)
- [ ] Doesn't duplicate existing skills (n/a — fixture)
- [ ] Fits within context window budget (n/a — fixture)
- [ ] Copied to `plugins/flow/skills/learned/{name}/SKILL.md` (DO NOT do this for the fixture)
- [ ] Committed and PR created (DO NOT for the fixture)
